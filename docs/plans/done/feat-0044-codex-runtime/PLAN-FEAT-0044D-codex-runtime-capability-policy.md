# Enforce Codex Runtime Capability Policy for Existing Hook Events


This ExecPlan is a living document maintained in accordance with `docs/plans/PLANS.md`. Product Context, Progress, Surprises & Discoveries, Decision Log, Outcomes & Retrospective, and Related Domain Knowledge Documents must remain current. Author: Joe-Degler (GitHub identity verified).

**Originating Feature:** `docs/planned/FEAT-0044-cross-agent-hooks.md`.

**Coordinating Epic:** `docs/epics/EPIC-0044-cross-agent-hook-portability.md`.

**Status:** COMPLETE. M0 through M4 have final milestone GO. Final units 2,192/0, full E2E 787/0 and static checks passed on matching source; code/QA and image/cleanup audit are complete. Selective documentation archival is authorized. Source/tests remain frozen. P/L remain absent; release confidence is not established.

**Execution:** Both bounded acquisition routes are recorded. Exact tag object `042fb41b7c813ac7999105e886b2b7aa715b5081` peels to `3d2ee51ca2d5db578f328aa75e20aa22c0197c9a`; all 4,177 inspected manifest files match. Probe 1 stopped before cargo because the cached image lacks cargo/rustc; zero upstream tests executed. The bounded retry stopped during dependency preparation: `cargo fetch --locked` refused an upstream Cargo.lock update. No upstream tests ran and no P evidence was obtained. Retry exhausted; no lock modification or third attempt. M2 validation is parent-run disposable Docker only. This close-out edit owns Plan D and ATTENTION; Lagrange owns M2 evidence/matrix and E2E, while Lorentz owns domain documents, epic and findings.

## Purpose / Big Picture


The validated M2 path lets registered custom or vendored TypeScript hooks handle Codex PreToolUse through existing Claude-shaped methods, with per-result checks and event-specific output. M3 now normalizes and translates the other nine agreed events with reviewed and validated input/result/turn and ordinary-error handling. Final documentation review and milestone GO are complete. Unsupported capabilities remain visible even when a later hook would otherwise overwrite their results.

M0, M1 and M2 are complete with final milestone GO. M3 passed full units 2,168/0, full E2E 774/0 and static checks exit 0 with 112 lint warnings on matching source. Final documentation review and milestone GO are complete. Prior full-suite failures remain recorded, including an unexplained M2 parallel-fixture timeout; the later pass is not a root-cause diagnosis. These Clooks unit/compiled tests are distinct from absent upstream executor and live-native evidence.

## Product Context


**Problem we are solving:** Runtime portability must preserve author intent without silently discarding unsupported results or applying their effects before capability checks.

**Metrics this may affect:** Correctness of portable hook execution, predictable failures, and preservation of existing Claude behavior.

**Feature alignment:** The feature text still names Cursor, Windsurf, and VS Code Copilot and asks Cursor-specific questions. The epic and explicit user direction approve Codex-first sequencing; this plan implements the common-config custom/vendored-hook use case with that approved target. This is recorded alignment, not an instruction to rewrite the feature. Claude plugin discovery and plugin delivery remain Claude-only.

Scope is exactly SessionStart, SubagentStart, PreToolUse, PermissionRequest, PostToolUse, PreCompact, PostCompact, UserPromptSubmit, SubagentStop, and Stop. Do not add SessionEnd, Interrupt, native background handlers, a new public provider field, new decision methods, or a trust database. Clooks assumes the repository is trusted and intentionally executes merged home/project/local configuration. Native activation remains external behavior, not an additional Clooks repository authorization gate.

## Progress


- [x] (2026-09-08) Post-completion documentation cleanup: removed the two isolated timing incidents from active findings and living domain/epic status; retained failed and successful attempts in archived plan and milestone evidence without claiming a root-cause fix. Kept one source-supported subprocess diagnostic gap and condensed readiness to remaining native conformance, recipient readability and Claude quoting limitations. `git diff --check` passed; targeted search found no repeated incident references in the affected living documentation. No production/test changes or execution; staging and commit remain outside this cleanup.
- [x] (2026-09-08) Final closure link review returned GO. Godel completed the fresh inventory summary without tools or lookups. The requested verbal clarification is best-effort history tracking, not deduplication; no behavior/documentation-contract change is required. All gates and closure work are complete; Plan D remains complete.
- [x] (2026-09-08) Final M4 code/QA milestone GO granted; Plan D is complete. Before archive edits, record the authorized selective move of only Plan D and runtime ATTENTION to docs/plans/done/feat-0044-codex-runtime/. Replace active ATTENTION with a brief pointer, not duplicate content. Preserve all A/B/C plans and shared sidecars in place, make archived shared references explicit repository-relative paths, and do not touch source/tests or stage/commit.

### Closure Snapshot History


All pending closure/audit/archive statements in the dated snapshots below describe earlier states and are superseded by final completion and archive authorization above.

- [x] (2026-09-08) Final parent audit confirms no owned M4 containers and final code/execution QA GO. Source manifest 11f1bc064814471e5c2edc41996eeae742d430ca06472c22717ae222f87e47eb and separate wrapper manifest 6ed9aa7f7fe33319d8fa656fcb2580d40e064a5f788425d2cc6a15fdfcaa6990 each match before/after across final gates. Unit image sha256:3fe2f24dd9d7a515277d3b3f50cfe42ca935f97c2b7b610ad4ee5ed2f3de33f5; E2E/static image sha256:93365cd1bc4b9222702b2bec14a250a83d05ea098132c8f82f5ffde99c318b53. Owned docs frozen for final closure review; prior pending-audit statements below record the earlier report and are superseded here.
- [x] (2026-09-08) Record parent-supplied final M4 evidence, test-only deviation/outcomes and archive proposal in Plan D/ATTENTION before documentation close-out. Final units 2,192/0 (69 files, 8,229 assertions, 3.33 seconds), full E2E 787/0 (47 files, 9,268 assertions, 104.18 seconds), and typecheck/lint/format exit 0 with 112 lint warnings/0 errors. Parent verified matching source-manifest hash 11f1bc064814471e5c2edc41996eeae742d430ca06472c22717ae222f87e47eb; all outputs report unchanged source/wrappers. Image/cleanup audit remains parent-owned and pending confirmation.
- [ ] Final M4 documentation review/milestone GO remains; source/tests are frozen. No archive moves until approved. Retain shared A/B/C artifacts and active downstream evidence; no P/L or public-release claim.

### M4 Implementation Snapshot History


Earlier M4 records below retain the work and validation states at their respective snapshots. Current final results are above; earlier pending implementation/validation statements are historical.

- [x] (2026-09-08) Parent authorized mechanical recovery of the final three-test formatting export. Independently inspected exactly the three allowlisted regular relative-path members, with no traversal, links or extras, and computed SHA-256 3af3c583e51d1342b182147a2af43fb141ff8bfe18093ed7883aced7cc851bcc for tmp/codex-runtime-m4/20260908T092938Z-format-export-2jCho0/export/formatted-owned-files.tar. Runner metadata reports format exit 0 and unchanged source/wrappers. Recover formatting only, then immediately refreeze for full gates; no production edits or archive moves.
- [x] (2026-09-08) Parent reports first M4 focused Docker units 923/0 and E2E 204/0; runner inactive, manifests unchanged and cleanup confirmed. These are focused results only, not full M4 acceptance.
- [ ] (2026-09-08) Before the next freeze, strengthen unit oracles: null resolver seeds/checks both same-session provider files; epoch replacement proves the fresh record exists before stale commit; actual no-hook/no-match SessionStart prunes stale Codex files while preserving aged Claude files. Test-only changes; production remains unchanged.
- [ ] (2026-09-08) First coherent M4 unit snapshot frozen for parent Docker compiler/focused feedback. Changed only src/engine/run.agent-adapter.test.ts and src/engine/turn-state.provider.test.ts plus owned Plan D/ATTENTION. Covers actual no-hook/no-match boundaries, null resolver without fallback, two sessions/two children/equal Claude IDs, inline Stop history/rejection, generation stale-writer controls, equal-generation epoch replacement after prune, and concurrent/idempotent commits. No production change was needed or made. Remaining failure-counter/storage/CLI and no-match-prune integration coverage follows feedback; no M4 test result claimed. Lagrange's first four E2E cases are separately frozen/reviewed.
- [x] (2026-09-08) Parent granted final M3 code/QA milestone GO and authorized M4 production/unit work plus Plan D/ATTENTION; Lagrange owns E2E and Lorentz owns domains/epic/findings.
- [ ] (2026-09-08) Planned M4 work before edits: add Codex engine no-hook/no-match boundary and explicit-null resolver controls; verify two sessions/root/two children/Claude equal IDs; test stale epoch/generation commits against positive preserved-boundary controls, concurrent acknowledged writes, storage-failure empty history and foreign-state preservation. Extend real-history inline handoff/rejection coverage and bidirectional config/load/hook failure isolation without duplicating shared storage algorithms. Audit CLI signals/exits/bootstrap read-only; propose production changes only for a concrete defect. Request early coherent parent Docker freeze; no host execution.
- [ ] Final M4 review/QA, matching-snapshot full unit/E2E/static gates, same-milestone domain updates and interface deviation check remain required. Coordinate completion/archive moves first so shared A/B/C artifacts are retained; no moves or commits now.

### M3 Closure History


The M3 closure records below are complete as confirmed by the M4 authorization above; their then-pending final-review statements are historical.

- [x] (2026-09-08) M3 implementation and final code review complete, with no code findings. Reviewer/QA approved migrated tests; the exit-trap correction preserves actual adapter selection and all payload assertions. Reconcile current Plan D/ATTENTION status now, retaining earlier snapshots as labeled history; source/tests remain frozen.
- [x] (2026-09-08) Record final parent-run Docker validation and actual interface deviation check in owned Plan D/ATTENTION. Full units passed 2,168/0, 69 files, 8,035 assertions; full E2E passed 774/0, 46 files, 8,781 assertions in 98.17 seconds. Typecheck/lint/format exited 0; lint 112 warnings/0 errors. Matching before/after source manifest 8224ddbd16af5dc77eb6e0cc29db315026378c2d0327c1166fbf5093c88e70ad and unchanged wrappers; no source changes or owned containers remain, as confirmed by parent.
- [ ] Final M3 documentation review is the only remaining milestone-closure gate. M4 has not started and requires final M3 milestone GO. No P/L evidence or native enforcement claim.

### M3 Implementation Snapshot History


The following dated snapshot records retain then-pending work and failed attempts. Current implementation/review/validation status is above; only final documentation review remains pending.

- [ ] (2026-09-08) Parent reports formatted full units 2,167/1 (8,021 assertions, 69 files); the sole failure is the migrated selection test's throwing process.exit trap being caught by runEngine and converted from intended exit 0 to 2. Full E2E passed 774/0 and static typecheck/lint/format exited 0 with 112 warnings on that prior snapshot. Unit artifact: tmp/codex-runtime-m3/20260908T085412Z-units-8yk4Aw/output.log. Authorized test-only correction selects the actual environment adapter, asserts Codex identity, then uses the core exit trap; preserve all valid/malformed payload assertions. Refreeze for full gates on matching final manifests; no production fix or final milestone GO claimed.
- [x] (2026-09-08) Parent reports reviewer/QA GO for the migrated SessionStart unit expectations. Independently verified formatting archive SHA-256 86469d1f4763ef1f914ac8e48b4180eb9a2063b7855c740fa3464ef0e0d5daea and exactly 12 regular relative-path members matching format-owned-files.txt, with no extra entries, traversal or links: tmp/codex-runtime-m3/20260908T085208Z-format-export-MiPTwd/export/formatted-owned-files.tar. Authorized recovery is mechanical formatting only, followed by immediate source/test refreeze for full Docker validation. No new test result is claimed; M4 remains unstarted.
- [ ] (2026-09-08) Parent reports second M3 Docker compiler/build success and focused 898 passes/3 failures, all stale M2 event-gate unit expectations; log tmp/codex-runtime-m3/20260908T084758Z-focused-u3XKTK/output.log. Compiled focused E2E passed 178/0, 3 files, 3,503 assertions, exit 0; parent confirms inactive runner and unchanged source/wrappers. Authorized correction is unit-only: replace the three gated-event expectations with valid source-shaped SessionStart controls and malformed negatives, preserving explicit selection/no-sniffing and plugin-isolation assertions. Production unchanged; formatting/full validation remain pending.
- [ ] (2026-09-08) Corrected compiler tables and implemented reviewed ordinary-error deferral; second coherent snapshot frozen for parent Docker feedback. Added real Codex executeHooks sequential/parallel continue/trace/block, below/at/disabled thresholds, recovery, Stop continuation/termination and bounded pre-cutoff/late-accounting units. Existing captured-accounting assertions remain unchanged. No execution of this snapshot claimed; formatting, broader event-edge coverage and full review/gates remain pending.
- [x] (2026-09-08) Reviewer approved the narrow deferred ordinary-error approach conditionally: keep raw capture and pre-observed cutoff accounting unchanged; defer engine-error audit only, never author or parallel-contract checks. Load errors retain their separate existing threshold-first path. Implement optional private deferRuntimeErrorAudit and execution regressions; this is an interface refinement from the first snapshot's no-new-signatures expectation, not a public API change.
- [ ] (2026-09-08) First M3 Docker attempt failed typecheck only: two readonly event tuples selected the wrong test.each overload and caused unknown-type cascades; zero tests ran. Artifact: tmp/codex-runtime-m3/20260908T084107Z-focused-RcykT2/output.log. Parent confirms runner inactive and source unchanged. Correct the two tables with mutable spread copies, preserving event literals.
- [ ] (2026-09-08) Reviewed ordinary-error regression: rejecting generated blocks during capture bypasses onError and circuit-breaker accounting. Proposed private refinement, awaiting reviewer approach feedback: optional invocation-policy deferRuntimeErrorAudit flag, omitted by Claude/injected policies and enabled by Codex. Author results remain immediately audited; sequential ordinary errors are audited only after configuration/accounting selects a block, and parallel selected generated blocks only after captured-outcome accounting is complete. Preserve captured-before-abort counters and ignore genuine late settlements. Add sequential/parallel continue, trace, threshold and Stop author-continuation versus runtime-termination units before refreezing.
- [ ] (2026-09-08) Before the first Docker run, correct reviewed normalization gaps: preserve supplied model on the existing SessionStart public context only; validate typed tool fields using the canonical public name so exec_command receives Bash validation. Add valid/invalid controls across tool events while preserving PreToolUse's stricter command-only codec. Refreeze after these narrow corrections; no execution yet.
- [x] (2026-09-08) Parent granted final M2 milestone GO, including the final bash-entrypoint domain correction.
- [ ] (2026-09-08) M3 assigned: extend existing Codex normalization, policy, translation and adapter turn policy for the remaining nine events; preserve PreToolUse and Claude. Add focused event/input/result/turn units and request an early coherent parent-run Docker compile freeze. Lagrange owns E2E; Lorentz owns domains/epic/findings. No M4, public API, configuration, registration or README changes.
- [ ] (2026-09-08) Planned private interfaces before code: reuse normalizeInvocation, createResultPolicy, composeDiagnostics, translateFinalOutput and resolveTurnPolicy without new public contracts. SessionStart may resolve reset/prune with a valid session and null native turn; root prompts advance all scopes, child prompts and stop continuations preserve. Approval input decoding is event-specific, not the PreToolUse command-only codec. Generated block origins use event-aware failure translation; generated trace text uses human systemMessages when the event has no context channel.
- [x] (2026-09-08) M0 and M1 have final milestone GO; their dated history and evidence are retained below.
- [x] (2026-09-08) M2 production and unit implementation complete: PreToolUse runtime, MCP/command codecs, strict pre-effects policy, early configured validation, provider state isolation, inline handoff and reviewed compatibility corrections. Code/QA GO is conditional on final validation and documentation review.
- [x] (2026-09-08) Verified SHA-256 d7c71a30b7ad1c46d4860e16ef40009d3e31b137095228120a596772215a11ff and exact 27 regular-file members of tmp/codex-runtime-m2/20260908T075919Z-format-export-hRlbEL/export/formatted-owned-files.tar; recovered Docker-produced mechanical formatting only. Source/tests remain frozen.
- [x] (2026-09-08) Parent reports full units 2,047 pass/0 fail; typecheck, lint and format checks all exit 0, lint 112 warnings/0 errors. Initial full E2E 673 pass/1 fail; only existing real-GitHub add-pack smoke failed. Bounded GitHub-focused rerun passed 11/0 without a source change.
- [x] (2026-09-08) Final full-E2E confirmation passed 674/0, 45 files, 6,714 assertions, exit 0: tmp/codex-runtime-m2/20260908T081921Z-e2e-5bJ5J5/output.log. Parent confirms the same frozen source as the 2,047-unit pass and successful typecheck/lint/format checks (supplied fingerprint prefix e8a92...).
- [x] (2026-09-08) Actual internal interface deviation check and resolved review issues are recorded below; Plan D/ATTENTION current sections are reconciled. No production/test changes during final validation or this docs update.
- [x] (2026-09-08) Final M2 bash-entrypoint domain correction and documentation review completed under parent coordination; final milestone GO granted.
- [ ] (2026-09-08) First coherent M3 source/test snapshot frozen for parent Docker compiler/focused feedback. Existing normalize/policy/translate/adapter modules now cover the remaining events; events.test.ts covers producer shapes, result/output cells, generated failure distinctions and turn policy. No new private signatures were required. Broader engine-edge coverage, formatting, full gates and independent review remain pending. No M3 execution is claimed.

### Implementation Snapshot History


The dated records below describe earlier snapshots, including gates that have since passed. Their pending/disabled statements are historical, not the current milestone status above.

- [x] (2026-09-08) M0 complete source-backed with accepted best-effort ordinary prompt history; internal Review limitation documented/nonblocking. P/L remain absent.
- [x] (2026-09-08) M1 implementation and reviewed interface clarifications complete. Raw-getter P1, ordinary crash-counter preservation and stale E2E observation artifacts fixed; original engine.test.ts expectation retained.
- [x] (2026-09-08) Final full units: 2,000 pass, 0 fail, 64 files, 7,207 assertions, exit 0. Final compiled E2E: 603 pass, 0 fail, 44 files, 5,369 assertions, exit 0.
- [x] (2026-09-08) Final Docker typecheck/lint/format:check each exit 0; lint 112 warnings, 0 errors. Final U/D/static before/after manifests match fingerprint 5cbfed02f56ad973d59e9338de7ba4116dd1ace81a687526dd8ee3a7c648cb23 (manifest, not Git commit); source unchanged across runs, no owned containers remain.
- [x] (2026-09-08) Final evidence update completed after recording the edit plan: durable docs/plans/feat-0044-cross-agent-portability/codex-runtime-m1-evidence.md contains exact commands, versions/images, artifacts, fixed failures and U/D versus S/P/L limits; matrix maps named cases. Code/QA conditional GO covers reviewed fixes.
- [x] (2026-09-08) Domain writer reports final domain updates saved and frozen with all validation results. Plan, matrix and M1 evidence are frozen for final documentation review; no source changes after formatting or new execution in this update.
- [x] (2026-09-08) Parent confirms final M1 code/QA/documentation milestone GO.
- [ ] (2026-09-08) M2 production/unit implementation assigned and in progress. ATTENTION updated before code. Parent coordinates Docker execution and reviews; Lagrange owns E2E, Lorentz owns domains/findings/epic. M3/M4 are not authorized by this assignment.
- [ ] (2026-09-08) First small source snapshot frozen for parent Docker compiler feedback: provider-qualified failure/turn helpers and command-only codecs with focused units. Runtime integration remains unfinished; Codex remains disabled. No M2 execution result is claimed. MCP rewrite scope awaits source-contract review before being finalized.
- [x] (2026-09-08) Parent reports the first helper snapshot passed Docker typecheck/compile and 716 focused tests, zero failures, 21 files, 2,317 assertions; exit 0, unchanged source. Artifact: tmp/codex-runtime-m2/20260908T072239Z-focused-0Q4PAh/output.log. This predates runtime integration and does not validate the next snapshot.
- [ ] (2026-09-08) Second coherent snapshot frozen for parent compiler/focused feedback: Codex PreToolUse adapter enabled, MCP codec correction, strict shape preflight, early configured normalization before state accounting/imports, event-aware refusal, private turn policy and inline handoff wired. Focused normalization/policy/translation and early-state-preservation units added; broader tests, final formatting and reviewer closure remain pending. No execution of this snapshot claimed.
- [ ] (2026-09-08) Second-run handoff refreshed after parent clarified no runner was active: descriptor-first raw validation rejects accessors without evaluating them; real Codex executeHooks tests cover dense/sparse/extra-property/accessor MCP updates, detachment, history and no handoff effects. Typed Read/WebSearch options now have valid controls and capability assertions; normalization detachment mutates existing nested data. Normalization/config diagnostics include runtime hook attribution and capability with the agreed native disposition. Source/tests now ready for parent focused units and compiled runtime E2E; no second-run result claimed.
- [x] (2026-09-08) Parent reports integrated focused units passed 743/0, 22 files, 2,458 assertions. Runtime E2E reported 66 passes and six failures across 72 tests; parent attributes five to stale diagnostic expectations and one to home-hook fixture ownership, delegated to the E2E writer. Logs: tmp/codex-runtime-m2/20260908T074038Z-focused-hmQWmT and tmp/codex-runtime-m2/20260908T074058Z-focused-e2e-ZBbjzS. Source unchanged during execution; no full milestone GO.
- [ ] (2026-09-08) Storage correction snapshot ready for parent validation: explicit-root Codex failure I/O, component/file link refusal and atomic publication; read-only Codex turn-parent validation; regular sentinel link tests and real-policy parallel raw-shape/late-settlement cases. No tests executed on this correction yet. Final formatting, broader review and milestone gates remain pending.
- [ ] (2026-09-08) Parent storage-run feedback was typecheck-only failure at four existing fixture helpers deriving dirname from the new FailureLocation union; zero tests ran, source unchanged. Artifact: tmp/codex-runtime-m2/20260908T074950Z-focused-WmxODN/output.log. Corrected only helper path extraction, preserving assertions. Added shared nonblocking FIFO reader correction, deadline-bounded Docker child tests, aged prune sentinels with owned-deletion controls, and bounded parallel-finally cleanup. This revised snapshot is ready for parent validation; no new execution claim.

### Historical Progress


The dated entries below preserve earlier drafting and coordination states. Pending gates in these records were superseded by the accepted history decision above; they are not current blockers.
- [x] (2026-09-07) History acceptance edit plan recorded before edits and completed: exact user prompt "Then ignore it and move on." recorded; ordinary prompt history accepted with Review documented/nonblocking. Current gates and M4 reconciled, M0 complete source-backed and M1 authorized/in progress. Historical records preserved and labeled; no P/L, production/test edits, execution or commits by this writer.
- [x] (2026-09-07) Final coordination update planned and recorded on parent confirmation: domain code re-review returned Docs GO with no findings. Source audit, bounded probe dispositions, reconciliation, docs, code/QA reviews and fresh no-lookup summary are complete. Only the unanswered history policy decision keeps production entry/M1 closed. No implementation, tests, P/L evidence or further summary required.
- [x] (2026-09-07) Status-only edit plan recorded before edits and completed: Ramanujan's fresh no-lookup summary is complete; domain fixes are saved with re-review pending. Current status aligned in both owned documents. No repeat summary for status metadata; history gate unchanged.
- [x] (2026-09-07) Final status update planned and recorded on confirmed code structural GO and QA GO: mark both reviews complete; retain only history product gate and parent-owned final domain/summary closure. No production entry, staging or tests.
- [x] (2026-09-07) Status reconciliation recorded before edits and completed: completed source/probe/reconciliation/QA work is distinct from pending code review, history decision and summary. Parent-supplied preservation/artifact checks recorded without staging. Final status update held until code review.
- [x] (2026-09-07) Bounded probe work and dispositions complete: initial missing-Rust blocker followed by exhausted retry blocked at cargo fetch --locked because upstream Cargo.lock required updating. Zero tests/no P; no lock modification or third attempt. Completed attempts do not mean successful executable validation.
- [x] (2026-09-07) Contract reconciliation complete across plan/matrix, including final QA P2/P3 corrections. Final QA returned GO on the refrozen bundle with no findings.
- [x] (2026-09-07) Parent reports tracked src/test/schema/README diff fingerprint unchanged (supplied prefix bb767e...), untracked registration-state files unchanged, and direct whitespace scan of ignored artifacts clean. These are supplied checks, not independently recomputed fingerprints by this writer.
- [x] (2026-09-07) Current code structural review returned GO for the plan/matrix; final QA GO remains confirmed with no findings.
- [x] Historical history-scope gate was pending at this point; resolved by the user acceptance recorded in current Progress and Decision Log.
- [x] (2026-09-07) Ramanujan completed the fresh documentation summary from the supplied inventory with no tools or lookup; current inventory and future docs were summarized accurately, retaining the M0/M1 history gate. No additional summary is required for status-only metadata updates; earlier drafting summary remains historical evidence.
- [x] (2026-09-07) Parent reports both small domain alignment fixes saved; this writer did not edit those files.
- [x] (2026-09-07) Final domain code re-review returned Docs GO with no findings; plan/matrix code structural GO and final QA GO remain confirmed. All documentation/review work is complete.
- [x] (2026-09-07) QA P2/P3 corrections completed after recording the edit plan: all PreToolUse allow reasons now use human systemMessage with explicit recipient loss, including rewrite-allow literal expectations. Confirmed parse_pre_tool_use retains decision reasons only for deny; corrected the source ledger to core/src/tools/handlers/unified_exec/exec_command.rs. Both owned documents refrozen for review. Retry remains exhausted with zero tests/no P; history choice and M0/M1 review gates remain pending.
- [x] (2026-09-07) Retry-result reconciliation plan recorded before edits and completed: updated only this plan and acceptance matrix from the parent's final retry report. cargo fetch --locked refused an upstream Cargo.lock update during dependency preparation; zero tests/no P. Retry exhausted, no lock modification or third attempt. Bundle frozen again for review; history decision and M0/M1 review gates remain pending.
- [x] (2026-09-07) Reconciliation writer plan recorded before edits and completed: only this plan and docs/plans/feat-0044-cross-agent-portability/codex-runtime-acceptance-matrix.md reconciled against final release-bound source reports. Corrected mappings, failure/diagnostic tuples, codecs, nullable fields, identity boundaries and evidence statuses; historical draft evidence retained. Frozen for independent QA. Direct trailing-whitespace scan found none; git diff --check returned clean but these files are absent from git ls-files, so it supplies no file-content validation. Parent owns epic/domain/findings and retry; no implementation, test execution or commits. M0/M1 still gated on user history answer and reviews.
- [x] (2026-09-07) M0 acquisition, semantic audit and corrected capability mappings completed after execution approval. Parent owns same-milestone evidence/domain/findings updates. Final QA is complete; remaining code review, history decision and summary are tracked separately above, not treated as unfinished source/probe work.
- [x] (2026-09-07) Independent wire and identity source audits complete; exact-tag route verified the initial ambiguous lookup's commit and all 4,177 manifest files match. Both attempt records remain historical. Reconciliation review and history decision remain open. Retry disposition is final: dependency preparation blocked by cargo fetch --locked refusing an upstream Cargo.lock update; zero tests/no P, no lock modification or third attempt.
- [x] (2026-09-07, Joe-Degler) Read CLAUDE.md and docs/plans/PLANS.md before creating the required full skeleton.
- [x] (2026-09-07, Joe-Degler) Record the requested latest user prompt verbatim before further research or edits.
- [x] (2026-09-07, Joe-Degler) Read epic, A/B/C plans, archived C1 plan and ATTENTION, capability matrix, continuation review, feature, and relevant domain documents and findings.
- [x] (2026-09-07, Joe-Degler) Read durable current-official research and QA matrix; incorporated code/product reviews, target 0.153.4, unavailable-string and non-record-input policies, pre-abort auditing, and provider failure-state paths.
- [x] (2026-09-07, Joe-Degler) Read current adapter, engine orchestration, lifecycle, discovery, public result/context types, Docker harness, continuation review, historical matrix, A/B/C context and C1 closure/ATTENTION, relevant domain documents and all six findings categories.
- [x] (2026-09-07, Joe-Degler) Incorporated public result limits, sessionTitle rejection, stale feature alignment, historical C1 evidence, and current versus future documentation scope.
- [x] (2026-09-07, Joe-Degler) Drafting-time documentation completion confirmed: docs/domain/config/discovery.md now distinguishes homedir()/options.homeRoot from runtime CLOOKS_HOME_ROOT; docs/domain/cross-agent-hooks.md records current documentation-only classifications while preserving the runtime placeholder; readiness findings and index are consolidated. No discovery code changes or tests occurred.
- [x] (2026-09-07, Joe-Degler) Coordinating review confirms historical capability-matrix header pointer, epic and active ATTENTION updated for target 0.153.4, provider failure isolation, pre-abort policy and event-aware failure limits. These are documentation changes, not implementation evidence.
- [x] (2026-09-07, Joe-Degler) Completed concrete candidate milestones, interfaces, capability/failure tuples, danger zones, domain updates and QA mappings; froze for independent review without test execution.
- [x] (2026-09-07, Joe-Degler) Independent product and code reviews returned GO for gated M0 with no blockers. M1 remains gated on M0; live confidence remains a separate release gate. No user decision is required now.
- [x] (2026-09-07, Joe-Degler) Applied final QA clarifications: preserve existing PreToolUse skip-context reduction, separate M1 injected-policy unit proofs from real Claude compiled regressions, and limit failure ordering to audited pre-abort settlements. Product/code GO for gated M0 remains recorded above.
- [x] (2026-09-07, Joe-Degler) Final QA reviewed the saved plan/report/matrix and returned GO with all three clarifications resolved and no blockers. Code re-review GO and product GO are confirmed. Draft complete; approval permits starting M0 only, with M1 gated on M0 and no release-confidence claim.
- [x] (2026-09-07, Joe-Degler) Final documentation summary completed from the supplied inventory only, without tools, lookup or new research. It distinguishes new D plan/research/matrix and current metadata/domain/findings updates from future implementation docs; no README/code/test changes are claimed. Reported whitespace check passed and original source/test fingerprints, including untracked registration state, remain unchanged.
- [x] Historical future-execution entry superseded: M0 entry gate is now satisfied; current M1-M4 progress is tracked above.

## Surprises & Discoveries


M4 actual deviation check: no production files or private/public API signatures changed. M2/M3 already supplied the required runtime integration; M4 adds unit coverage in src/engine/run.agent-adapter.test.ts and src/engine/turn-state.provider.test.ts, plus Lagrange's compiled codex-state-delivery suite. This is the plan's permitted minimal-integration outcome, not a deferred implementation claim. Native required-field rejection, explicit-null resolver semantics, Claude defaults/paths, configured failure accounting, inline handoff and bootstrap behavior are unchanged. CLI/signal/exception audit found no demonstrated production defect requiring a change.

M4 unresolved-cause validation finding: the initial full-unit run reported 2,191/1 because existing turn-state.io.test.ts:404 measured 38.53868899999998 ms against its unchanged >50 ms held-lock expectation. Artifact: tmp/codex-runtime-m4/20260908T093414Z-units-CpazQA/output.log. The implementation budgets Date.now() while the test measures performance.now(); read-only review found no M4 clock/timer mock leak, but neither a clock jump nor another cause was proved. The previously passing focused run and one QA-approved unchanged-source full-unit confirmation do not establish a root cause. Final confirmation passed 2,192/0 without assertion changes or a production fix. Retain this as unexplained, not a flake or proven infrastructure issue.

M3 actual interface deviation check: the first snapshot expected no new private signatures. Review found that rejecting generated ordinary-error blocks during capture bypassed onError and threshold accounting. Optional InvocationResultPolicy.deferRuntimeErrorAudit now opts Codex into auditing only selected ordinary crash blocks after configuration/accounting; omitted Claude/injected behavior is unchanged. Parallel raw capture, pre-observed cutoff, immediate author/parallel-contract validation and load-error threshold-first handling are preserved. executeHooks still uses its optional tenth policy argument, with no eleventh argument or public context/result/configuration/registration/schema change. SessionStart's existing public model is preserved; canonical Bash optional validation uses the public tool name. No generic storage or authorization framework was introduced.

M3 resolved failures remain recorded: the first compiler pass rejected readonly test.each tables; spread copies preserved literal event types. Three stale M2 event-gate assertions were replaced by valid SessionStart and malformed negative controls without dropping plugin/no-sniff checks. The prior full-unit 2,167/1 failure was a test trap: runEngine caught mocked ExitCalled(0) and emitted exit 2. The test now selects/asserts the actual Codex adapter and uses the core trap, without changing production. Final matching-manifest gates passed these corrections. M2's unexplained timeout remains a separate unresolved-cause finding, not retroactively diagnosed by M3 passes.

M1 code review found a throwing raw-getter exception that could orphan parallel batch completion. The fix and named regression passed its full-unit gate. The original ordinary parallel crash-counter expectation is preserved, with pre-abort versus late-settlement regressions. These resolved findings and completed M1 acceptance are detailed in docs/plans/feat-0044-cross-agent-portability/codex-runtime-m1-evidence.md.
M1 reviewed clarifications remain in force: final failure selection belongs to the engine, so a latched policyFailure selects translateFailure even when diagnostic composition or adjustment returns allow. Configured no-hooks/no-matches/no-result paths always finalize through the adapter and retain its exit, even without messages; the no-config bypass still avoids normalization/finalization. ComposeDiagnosticsInput returns ComposedDiagnostics (result, stderr, systemMessages), keeping the callback free of I/O. M1 has final documentation approval.

M2 review resolved three boundary defects before final gates: lodash could erase invalid array shape before policy inspection, provider-qualified paths could still alias foreign state through links, and a FIFO could block the shared turn reader before its file-type check. Descriptor-first raw validation, explicit-root managed failure I/O, read-only Codex turn-parent validation and O_NONBLOCK address those defects. Further corrections validate known-tool optional fields and command patch key presence, validate configured input before any counter read/write/clear, retain MCP full-record rewrites, and strengthen positive controls, stale sentinels and bounded parallel cleanup. Four existing fixture helpers now extract string paths from FailureLocation; their assertions were preserved.

Unexplained prior full-suite timeout finding: tmp/codex-runtime-m2/20260908T081120Z-e2e-CpEi3W/output.log reported 673/1 at circuit-breaker-advanced.e2e.test.ts:257, first parallel crash/allow invocation, with mapped exit 2 after 15.7 seconds and Bun's 5-second test-timeout notice. The original child stderr and raw signal/exit were not logged; sandbox.run maps a missing child exit code to 2. The focused file subsequently passed 9/0 in 1.57 seconds, and the final full suite passed unchanged source. Read-only comparison found the parallel capture/abort/accounting implementation unchanged from the final M1 artifact, but that does not establish a cause. Keep the finding unexplained, not classified as a flake or proven infrastructure failure. Parent owns any separately captured bounded-reproduction evidence; no unreported reproduction result is asserted here.
Historical Plan A evidence is documentation-backed and runtime-unverified because its runtime attempt received HTTP 401. The May matrix and `test/fixtures/codex/events/` remain historical documentation-shaped examples, not captured input. The earlier refresh was documentation-only; M0 now supplies exact-release source evidence through the two audit sidecars, without executed parser or live evidence.

Source deltas: bare PreToolUse allow is invalid; no-decision preserves native policy. PermissionRequest reserved-field failure returns Failed/no decision and ordinary review, not fail-closed enforcement. Successful stderr is discarded. Stop continuation retains its turn and emits no UserPromptSubmit, while root steering can emit a new prompt on the same turn ID. Internal Review can emit synthetic prompts without agent_id, so absence of that field cannot prove a genuine user boundary. These findings change proposed mappings below, not the public method surface.

The pre-M1 audit identified handoff and sequential input mutation before reduction, losing result fields, post-execution diagnostics and ignored adapter exits. M1/M2 now audit before those result effects, retain a separate failure latch, compose diagnostics through the adapter and honor configured final exits. The legacy PreToolUse vote/context reducer is unchanged.

`src/lifecycle.ts` returns beforeHook block/skip immediately, runs afterHook as an observer of the raw handler result, and only permits afterHook passthrough. No new afterHook override semantics are needed. A beforeHook meta-gate is not the same surface as a handler decision; audit both without expanding the handler's methods.

The current unknown-tool contexts require `Record<string, unknown>` input while PostToolUse response is already `unknown`. The documentation-only refresh describes wire input and response as JSON values, which may be arrays, scalars, or null. A cast cannot repair this mismatch. The bounded compatibility policy below rejects unrepresentable inputs before hook execution and preserves arbitrary response values.

Current C1 full checks are registration baseline only. Archived closure records 1,957 Docker unit and 600 E2E passes, and the unresolved fixture coverage-threshold issue. Those checks were not rerun for this draft and establish no new runtime verification.

## Decision Log


Decision: Archive only this completed Plan D and its runtime ATTENTION under docs/plans/done/feat-0044-codex-runtime/. Retain active ATTENTION as a brief pointer and leave shared A/B/C plans, matrices and source/evidence sidecars at their existing paths. Use explicit repository-relative shared references in the archived plan. Rationale: final M4 GO authorizes closure without disrupting downstream consumers or duplicating runtime documentation. Date: 2026-09-08. This decision is recorded before the moves; no source/test/staging changes are authorized.

Decision: Add O_NONBLOCK to the existing shared bounded turn-state reader. O_NOFOLLOW alone can block while opening a FIFO, before fstat can reject it. This incidental correction preserves regular-file behavior for both providers and the documented requirement that unusable history cannot wedge an invocation; it avoids threading another provider flag through locks and readers. Validate with deadline-killed disposable Docker child processes, valid regular-file controls and untouched FIFO identity. Existing unit fixture adapters extract the string path from FailureLocation only to derive their handoff root, without changing assertions. Date: 2026-09-08.

Decision: Provider-qualified filenames alone do not isolate state when a managed parent or final file is a symlink. Before the storage correction, record a narrow internal FailureLocation union: legacy string paths remain unchanged; Codex carries its path and actual project/home root together. Existing readFailures/writeFailures consume that location, check every managed component, refuse linked/nonregular files, read through a no-follow descriptor and publish through a private atomic staging file. executeHooks keeps the same argument count and accepts this location in its existing failure-path slot. Codex readTurnState receives optional actual home/provider metadata to validate managed parents before the existing bounded no-follow file read. No authorization gate, public config or generalized store abstraction is added; existing per-component/atomic patterns bound this fix, not an adversarial-filesystem race guarantee. Date: 2026-09-08.

Decision: The raw JSON-shape check must precede lodash detachment because sparse arrays and extra array properties can be changed or discarded by cloning. Add optional InvocationResultPolicy.validateRawResult(value): RuntimePolicyFailure | undefined inside checkDetachedResult's protected boundary, before cloning. It is validation-only: it cannot accept a result or authorize effects; the detached checkResult remains mandatory. Omitted preflight preserves Claude and injected-policy behavior. Codex rejects lossy array shapes and validates candidate JSON again after materializing patches. MCP record rewrites are enabled from the retained source-backed full-Value transport; downstream server acceptance remains separate, as patch grammar validation is separate from a command-string codec. Date: 2026-09-08.

Decision: For Codex config-error accounting, determine project ownership from project clooks.yml presence while excluding projectRoot === homeRoot, matching loadConfig's isSameConfig rule. Use the same provider path computation before loading and on recovery; a malformed home config can fail before project parsing. This refines the unconditional project-path wording below: genuine home-only failures use the already-planned provider-qualified home/project-hash path. Claude's hardcoded config-error path remains unchanged. Date: 2026-09-08.

Decision: Thread an optional internal provider argument through failure and turn path/directory/commit/boundary/prune helpers, defaulting to Claude. Keep the actual home root unchanged. Codex configured input is read and normalized before hook preparation/import; no-config remains a bypass. Recognize all ten event names for failure translation, but normal execution is limited to PreToolUse until M3. Handoff eligibility is internal to the invocation policy and defaults to legacy eligibility when omitted. No public author fields or methods change. Date: 2026-09-08.

Decision: Make normalization timing and turn interpretation adapter-owned, not provider-ID branches in run.ts. Add optional private AgentAdapter.inputStage: 'before-hooks' | 'after-hooks' (omitted means the existing late Claude stage), and AgentAdapter.resolveTurnPolicy(invocation): InvocationTurnPolicy | null. InvocationTurnPolicy carries sessionId, scopeKey, boundary ('reset' | 'advance' | null), and prune (boolean). Omitted resolver uses existing Claude scope/boundary rules; null explicitly disables history and boundary effects for an invocation with unreliable identity. The engine executes the returned storage instructions using adapter.id only as the provider argument to shared namespace helpers. Codex PreToolUse supplies its validated private session and child identity with no invented turn boundary; remaining event execution stays gated until M3. Configured early input is cached once and rejected before hook preparation/import, while config absence remains a bypass. This is an internal refinement of planned timing/turn ownership, not public API expansion or an eleventh executeHooks argument. Date: 2026-09-08.

Decision: Make createResultPolicy(invocation) the invocation-bound policy factory; expose ExecutionResult.policyFailure internally and let the engine select failure translation. Use pure composeDiagnostics(input): ComposedDiagnostics and central engine output ownership, including unconditional configured-noop finalization. Share checkDetachedResult in src/engine/result-policy.ts to deep-clone policy inputs and accepted outputs while preserving undefined and opaque keys. Rationale: reviewed M1 clarifications implement private policy isolation, sticky refusal and deterministic output without callback I/O; preserve the no-config bypass and Claude legacy behavior. Date: 2026-09-07. Implementation and U/D/static acceptance are now complete pending final documentation review.
Decision: Reconcile native mappings against the exact-release wire and identity source reports. Plain PreToolUse allow becomes no-decision with optional context; its reason becomes inline human systemMessage, explicitly losing the original allow-reason recipient/permission semantics. Never invent a grant, denial or no-op rewrite to transport it. A validated rewrite retains native allow plus complete replacement; its reason uses the same human systemMessage/recipient-loss policy. The parser accepts but discards permissionDecisionReason on allow, retaining it only for deny. PermissionRequest plain allow remains an actual grant. Claude's legacy allow can bypass approvals and remains unchanged; Codex's native approval/sandbox policy still applies. Rationale: source consumer behavior, not a permission-policy redesign. Date: 2026-09-07.

Decision: The user's exact prompt was "Then ignore it and move on." This accepts best-effort ordinary root/ThreadSpawn prompt history with the internal Review limitation documented and nonblocking. Keep history enabled: reset all scopes on root UserPromptSubmit, including same-ID steering; preserve on child prompts and Stop/SubagentStop continuation. Synthetic Review without agent_id can be classified as a root prompt; accept this limitation without workaround scope, fabricated identity or a full genuine-user-parity claim. This resolves the history product gate and completes source-backed M0; M1 was authorized by this decision; implementation and validation are now complete pending final documentation review. Date: 2026-09-07.
Decision: Draft only this new plan file, preserve all uncommitted C1 work and TODO entries, and make no commits. Rationale: Explicit task boundary and worktree safety. Date/Author: 2026-09-07, Joe-Degler.

Decision: Audit capabilities for each individual result before input mutation, handoff effects, or reduction. Preserve private runtime metadata, use opaque tool codecs, specify event-aware failures, isolate turn/delivery/provider identity, and define inherited CLAUDE_PROJECT_DIR handling while preserving explicit CLOOKS_PROJECT_ROOT. Rationale: Required runtime-policy boundaries. Date/Author: 2026-09-07, Joe-Degler.

Decision: All future init, uninstall, tests, and spikes run only in Docker, with no real HOME/config/auth/trust access or global binary replacement. No executions are needed during drafting. Rationale: Explicit environment isolation requirement. Date/Author: 2026-09-07, Joe-Degler.

Decision: Preserve handler result sets exactly: SessionStart/SubagentStart use skip with optional injectContext; PostCompact is skip-only; PreCompact uses allow/block/skip. Native continue:false does not authorize a public method expansion. Audit UserPromptSubmit.sessionTitle on allow/block/skip as unsupported unless the selected-release evidence explicitly establishes a supported mapping. Rationale: Parent source audit and unchanged Claude authoring contract. Date/Author: 2026-09-07, Joe-Degler.

Decision: Treat unsupported control and mutation, including sessionTitle, as an invocation policy failure that cannot disappear through reduction or circuit-breaker degradation. The initial draft's cosmetic-title strip policy is superseded by sticky rejection to match the consolidated acceptance matrix. Unsupported well-typed injectContext on an otherwise valid author arm is the sole declared communication-loss case: retain the supported decision, drop that field, and report the exact lost delivery to the human; invalid arms/field types still fail. Rationale: No mutation request may silently disappear, and explicit context-loss handling must not become a general downgrade path. Date/Author: 2026-09-07, Joe-Degler.

Decision: Codex discovery ignores inherited CLAUDE_PROJECT_DIR through an invocation-local environment view; nonempty explicit CLOOKS_PROJECT_ROOT keeps its existing relative/absolute resolution and precedence. Do not mutate process.env. Rationale: Global Codex must discover its current project while intentional Clooks overrides and Claude defaults remain intact. Date/Author: 2026-09-07, Joe-Degler.

Decision: Keep arbitrary JSON in the private wire envelope, accept only honest public context shapes, and keep tool input keys opaque except an explicit evidence-backed codec. Missing/null transcriptPath, agentTranscriptPath, customInstructions, compactSummary and nullable lastAssistantMessage map to the documented empty-string unavailable sentinel, never a filesystem path or fabricated summary; original null versus absence stays private. Supplied strings, including empty strings, remain unchanged; hooks cannot distinguish unavailable from genuinely empty values. Other types reject. Non-record tool input with no valid codec fails normalization before hooks run. Rationale: Product review approves this bounded compatibility policy without public type expansion; it does not waive required identity/tool/prompt validation. Date/Author: 2026-09-07, Joe-Degler.

Decision: Preserve existing PreToolUse context composition. A supported raw skip+context emits context only when selected; losing skip context is omitted without an unsupported warning, and all-skip chooses the last vote. Rationale: Native per-result support does not authorize a new shared aggregation policy. Exact unit and Docker tuples pin this distinction. Date/Author: 2026-09-07, Joe-Degler.

Decision: M1 uses injected-policy units for Codex-specific boundary proofs and real Claude compiled regressions for its E2E gate; actual Codex compiled acceptance starts in M2/M3. Rationale: Codex remains disabled in M1, so its runtime paths cannot honestly be claimed as compiled coverage. Date/Author: 2026-09-07, Joe-Degler.

Decision: First policy rejection latches abort; primary failure ordering applies only to audited pre-abort failures. Late settlements are ignored, and different schedules may select different valid primaries. Rationale: Waiting for an unavailable configured-earlier failure would contradict immediate abort and the no-late-effects contract. Date/Author: 2026-09-07, Joe-Degler.

## Outcomes & Retrospective


Final closure: selective archive links received GO and Godel's fresh no-tools/no-lookups inventory summary is complete. The verbal history-tracking clarification does not introduce delivery deduplication or a new guarantee. No source, behavior or validation changes accompanied this final completion record.

Plan D is complete with final M4 milestone GO. Selective archival preserves all shared A/B/C artifacts and downstream sidecars. The milestone retrospectives below retain their earlier closure snapshots; their pending-review/audit statements are superseded by final completion.

M4 outcome: existing runtime interfaces required no production changes. New unit controls verify actual no-hook/no-match boundaries and pruning, explicit-null tracking without legacy fallback, two sessions/root/two children/equal Claude IDs, inline Stop history and rejected delivery, generation stale writers, equal-generation epoch replacement, and concurrent/idempotent tracker commits. QA strengthened both-provider byte sentinels and the fresh-epoch positive control. Parent-run final full units/E2E/static passed on matching source. Final documentation/milestone GO and parent image/cleanup audit remain before closure.

M4 retrospective: focused verification avoided reimplementing already-correct boundaries and storage. Seeded foreign state and explicit fresh-record assertions made negative checks distinguish actual isolation from empty fixtures. Preserve the unexplained held-lock timing failure alongside the passing unchanged-source confirmation; a later pass is not diagnosis. Remaining P/L, recipient readership and native activation belong to downstream conformance/release work, not this test-only milestone.

Archive proposal, not authorization: do not move the entire active feat-0044-cross-agent-portability folder. It contains shared A/B/C plans, ATTENTION and source/evidence referenced by downstream work. After final milestone GO, coordinate a reference inventory with the parent and document owners, then archive only completed Plan D and any proven D-exclusive artifacts under docs/plans/done/feat-0044-cross-agent-portability/, repairing affected relative links in the same documentation operation. Keep shared ATTENTION, M0 source sidecars and cross-plan acceptance/evidence in place unless their consumers are explicitly migrated. No moves, copies, staging or commits occur during this close-out.

M0/M1/M2 have final milestone GO. M3 completes all nine remaining events with no final code findings and matching-manifest validation: 2,168 units/0 failures, 774 E2E/0 failures and successful static checks, lint 112 warnings/0 errors. Only final documentation review remains before M3 milestone GO. M4 is not started; no P/L or release-confidence claim is made.

M3 retrospective: ordinary runtime errors and unsupported author results require different timing. Deferring only selected ordinary crash auditing preserved configuration and captured failure accounting without reopening late effects. Early compiler feedback caught test-table typing; real execution regressions covered modes/thresholds and Stop termination, while positive SessionStart fixtures preserved adapter selection/plugin isolation. No production workaround was added for test-trap behavior. Remaining work belongs to M4 cross-event integration acceptance after milestone GO, not new native probing or expanded author APIs.

Retrospective: early compiler runs caught missing dangling arrays in two mocks and four fixture helpers that needed string-path extraction. Runtime replays exposed diagnostic expectation and fixture-layer/order mismatches, corrected by the E2E owner without weakening production semantics. Code review strengthened lossy-shape validation and managed-state isolation before full gates. The initial real-GitHub smoke failure and later unexplained parallel timeout are retained rather than erased by the final passing run. No source fix was inferred from those final-suite failures without a reproduced cause.

## Context and Orientation


The orientation below describes the historical pre-M1 baseline. Current M1/M2 working-tree interfaces and reviewed changes are recorded in Interfaces and Dependencies; Codex PreToolUse is now implemented, while the other nine runtime events remain gated until M3.
Clooks is a Bun/TypeScript CLI. `src/cli.ts` chooses engine mode for piped stdin with no command arguments. `src/engine/run.ts::runEngine()` calls `src/agents/select.ts::selectAgentAdapter()` using CLOOKS_AGENT: unset/empty and claude-code select Claude; codex selects a placeholder whose supportsRuntime is false; unknown selectors fail without payload sniffing. `src/agents/types.ts` defines the shared adapter contract. `src/agents/claude-code/adapter.ts` owns recursive Claude key normalization, plugin preparation/advisories, final output, notify-only routing, and ConfigChange policy_settings downgrade. The Codex placeholder never executes TypeScript hooks.

`runEngineCore()` currently discovers root and loads config before reading normal stdin. `src/config/index.ts` merges home `.clooks/clooks.yml`, project `.clooks/clooks.yml`, and project `.clooks/clooks.local.yml`; project hooks replace same-name home hooks, and local settings override. `src/loader.ts` imports registered hooks; `src/engine/match.ts` and `src/ordering.ts` select and order them. No auto-discovery or separate repository permission check is introduced here. If config is absent, the engine no-ops, including malformed unused stdin. With config present, normalization and turn boundaries run before no-hook/no-match exits. Preserve that distinction deliberately.

`src/engine/execute.ts::executeHooks()` runs sequential or parallel groups through `src/lifecycle.ts::runHookLifecycle()`. beforeHook may block or skip the handler; afterHook observes the raw result and cannot replace it. Hooks receive snapshot turn history and return result objects defined in `src/types/results.ts`. PreToolUse structured denies are votes, with deny greater than defer greater than ask greater than allow; ordinary blocking events stop the pipeline. Sequential patches are shallow, with null meaning explicit unset; parallel rewrites are contract violations. Errors use configured onError and failure counters. Keep these Claude semantics and separate policy rejection from hook crashes.

`src/engine/handoff.ts` can replace eligible model-facing text with a file pointer before reduction. `src/engine/turn-state.ts` stores per-hook raw outcomes by session, scope, epoch and generation; it advances on every UserPromptSubmit, preserves resume/compact, and resets startup/clear. It is best-effort history, not delivery confirmation or enforcement. Codex Stop continuation creates neither a prompt event nor a new native turn. Child prompts must not reset session history, and distinct root steering prompts can share a turn ID. Internal Review lacks the command-envelope discriminator needed for full genuine-user parity; the best-effort ordinary prompt boundary policy is now user-accepted with that limitation documented and nonblocking.

`src/commands/init-entrypoint.ts` generates project/global launchers. Missing binary and SKIP_CLOOKS=true intentionally exit 0; unexpected child failure currently becomes exit 2. C1 corrected exact registration ownership, atomic writes, all-agent cleanup before shared deletion, physical CODEX_HOME resolution and recoverable suppression receipts. Its code remains uncommitted and must be preserved. A receipt describes fresh registration bytes and home identity, not enabled/reviewed native hooks or exactly-once execution. Do not alter the receipt format or redesign suppression in D. Absolute project registration still requires re-init after relocation.

Here, wire means the agent's stdin JSON and stdout/stderr/exit response. A codec is a tool-specific reversible conversion between wire input and public tool input. Reduction combines several hook results into one decision. An invocation envelope is private data for one call, held separately from the object visible to hook authors. Delivery audience means the human, main agent, or child agent that receives a particular output field.

## Related Domain Knowledge Documents


Consulted `docs/domain/cross-agent-hooks.md`, `docs/domain/bash-entrypoint.md`, `docs/domain/cli-architecture.md`, `docs/domain/config/discovery.md`, and `docs/domain/global-hooks.md`. After implementation, update their adapter status, invocation-local discovery precedence, actual event-aware errors, private state namespaces, and unchanged registration limitations in the milestone that changes each behavior.

Consulted `docs/domain/config/execution.md`, `docs/domain/config/handoff.md`, and `docs/domain/turn-state.md`. Future updates must document auditing before effects, policy failure versus circuit-breaker errors, eligible recipient-aware handoff fields and inline fallback, raw history semantics, provider/scope partitioning, and only evidenced Codex turn boundaries. Keep documented storage failure/locking limitations.

Consulted `docs/domain/hook-type-system.md` and `docs/domain/hook-type-system/decision-methods.md`. Future updates describe the existing unknown-tool escape hatch, record-only supported Codex input subset, opaque key preservation, empty unavailable transcript sentinel, and unchanged decision methods. Do not describe new methods or regenerate public declarations without an actual public type change and semantic audit.

Consulted `docs/domain/testing.md` and `docs/domain/tools.md`. Testing needs future runtime replay, provider isolation, failure and codec coverage guidance with provenance distinctions; tools needs no update because this plan adds no host-installed tool. No new domain subsystem document is required.

Historical drafting-time domain alignment was documentation-only and preserved runtime-unimplemented status. Discovery's homedir()/options.homeRoot distinction from runtime CLOOKS_HOME_ROOT remains. M0 source-status updates to epic/domain/findings are parent-owned and separate from this two-file reconciliation; neither set of documentation updates satisfies future implementation-time knowledge updates.

## Related Findings


Read `docs/findings/index.md` and knowledge-gaps, code-quality, test-gaps, stale-docs, tooling-friction, and process-feedback category files. `docs/findings/codex-portability-readiness.md` directly motivates per-result capability auditing, turn/handoff isolation and honest native activation limits. D addresses the runtime portions only; Claude global launcher quoting and native activation evidence are not silently closed.

The knowledge-gap entry on permission-failure fixtures requires making the parent `.clooks` directory unwritable, because managed state directories repair their own modes; always restore permissions in finally. The tooling entries on compilation without typecheck, isolated Codex auth, and missing Docker coverage configuration require the existing typecheck-before-compile harness, explicit container binaries, and honest reporting of unresolved coverage failures. Do not weaken coverage thresholds to close D.

The process-feedback full-blast-radius entry requires both unit and compiled-binary E2E coverage for every changed shared path. The old agent-test state-isolation defect was corrected in C1; preserve fresh per-test homes and environment restoration. Existing stale event-count docs and the general new-event harness gap do not authorize catalog expansion. Future implementation follows the findings procedure and removes only issues actually resolved by accepted evidence.

## Plan of Work


Follow M0 through M4 in order. M0 is an evidence gate before any production changes. M1 preserves Claude while installing the policy boundary. M2 is a complete PreToolUse vertical path; it must already have the required root, failure, delivery and state isolation. M3 completes the remaining nine events. M4 verifies cross-event interactions and closes domain updates. No milestone may defer its own unit or compiled-binary Docker E2E to Plan E. Each production milestone needs independent code and QA review and a documented interface deviation check before completion.

There are two distinct gates. M0's production-entry gate requires immutable selected-source provenance and completed source/semantic decisions for all ten events, including the parser/failure, codec, identity and delivery questions named below. Missing critical source evidence blocks M1 as well as later production; this plan grants no scaffolding exception. Bounded executor attempts are recorded separately and must resolve any source ambiguity needed by enabled behavior. Where source proves that a capability cannot be supported, record its explicit refusal/limitation policy; unknown is not unsupported. The release gate separately requires Plan E's native confidence evidence or an explicit release-confidence decision before shipping the complete feature. Missing live proof alone does not prohibit production once M0's source gate passes, but it prohibits claims of verified native enforcement or a partial-feature release.

### M0: Bind the Ten-Event Contract to a Selected Release


Completed source-backed, with the user-accepted history limitation and recorded code/QA/Docs GO. The acquisition/probe procedure below is retained as the historical bounded procedure, not an instruction to repeat exhausted attempts. P/L remain absent; M1 entry is authorized.
Target Codex CLI `0.153.4`, exact ref `refs/tags/rust-v0.153.4`, verified to the tag object and peeled commit in Execution above. The two 4,177-file manifests match; retained git archive SHA-256 is `45375e73d04b57a0e36520f88c8ebfd22f0eb400410b5ce40ad9b3b2dcbf07a0`. Acquisition attempts and probe 1 are preserved in `docs/plans/feat-0044-cross-agent-portability/codex-runtime-m0-evidence.md`; wire/identity sidecars supply completed source semantics. Earlier acquisition-only pending-semantic status in that evidence record is historical, superseded by those sidecars. Source evidence, completed reviews and the accepted history decision now pass M0 production entry; they do not establish P/L. Scratch scripts belong under `tmp/codex-runtime-m0/` and remain parent-owned.

In disposable Docker only, resolve that exact tag to a commit, record its identifier and archive checksum, and inspect the hook input schemas, output parser, event dispatch, tool codecs and executor tests. Use one bounded acquisition attempt per official source route, at most two routes (tag archive then git tag fetch), with 60-second network deadlines. If both fail, record acquisition as unavailable and stop M0; do not substitute main. The executor budget was one initial run and one diagnosed retry, each with a ten-minute process-tree deadline; both are now consumed. The retry's locked dependency preparation refused an upstream Cargo.lock update. Do not modify the lock or make a third attempt; no tests ran and P remains unverified. No unbounded research loop and no credential fallback. A failed source fetch is not a new authentication-blocked live attempt.

Record every one of the ten events against required/optional/null input fields, accepted output object and unknown-field handling, stdout/stderr/exit behavior, intended recipient, and tool dispatch identity. Compare the selected source to the current website and label drift. Exercise the pinned parser/executor with synthetic no-model input if feasible; this is parser/executor evidence, not native live enforcement. Describe exact discovered Docker commands and retained sanitized output here before marking M0 complete. If parser/executor coverage cannot settle enforcement, keep the release gate closed; production scaffolding must not be presented as native confidence.

Resolve the specific identity questions using that source/executor: whether Stop/SubagentStop continuation generates UserPromptSubmit; whether native turn_id changes; which field distinguishes a genuinely new user prompt; which session owns child events; and whether repeat delivery has a stable identifier. Native turn_id alone is not a user-turn boundary. Do not fabricate IDs, parse transcript text, compare prompt strings, or use timestamps as substitutes. If no evidenced distinction exists, M0 records the exact compatibility limitation and D cannot claim once-per-user-turn parity. The user has accepted best-effort ordinary prompt history with the internal Review ambiguity documented and nonblocking; this satisfies the history disposition for persistent tracking without disabling history or adding a workaround. User input is required only if the evidence forces a weaker product promise, public API/scope change, or release-confidence exception; routine architecture choices are already specified here.

M0 source confirms transcript_path on all events, agent_transcript_path on SubagentStop, and last_assistant_message on Stop/SubagentStop are required nullable producer fields. Accepting their absence is explicitly broader Clooks compatibility, not strict native validation or a captured producer shape. custom_instructions and compact_summary are absent from these producers. The approved sentinel mapping accepts absent/null as empty, preserves supplied strings, and rejects other types; hooks cannot distinguish unavailable from genuinely empty. Preserve raw absence/null privately and never fabricate operational IDs, prompt text, stopHookActive, source, trigger or tool names. Acceptance requires recorded gate dispositions. Full ten-event scope remains.

The source input contract is explicit: all ten producers include string session_id, cwd, hook_event_name and model plus nullable transcript_path. All except SessionStart include turn_id; all except compact events include permission_mode. SessionStart adds source; compact adds trigger; UserPromptSubmit adds prompt. Tool events add tool_name/tool_input, PreToolUse/PostToolUse add tool_use_id, and PostToolUse adds arbitrary JSON tool_response. Stop variants add boolean stop_hook_active and nullable last_assistant_message. SubagentStart/SubagentStop require agent_id/agent_type; SubagentStop also adds nullable agent_transcript_path. Ordinary tool/prompt/compact events optionally emit paired agent_id/agent_type for ThreadSpawn, omitted at root. Preserve these private identities and source requiredness separately from public sentinel compatibility. Producer structs serialize these fields; generated schema requiredness is not an upstream stdin validator. Exact field schemas and symbols are in the wire sidecar's Input Producers table.

### M1: Carry Private Invocation Policy Through the Existing Engine


M1 is complete with final documentation approval. Its full units, compiled E2E and static checks passed on matching manifests; see docs/plans/feat-0044-cross-agent-portability/codex-runtime-m1-evidence.md. Codex was runtime-disabled throughout that milestone; M2 enables the separate PreToolUse path.
Extend `src/agents/types.ts`, `src/agents/index.ts`, `src/agents/claude-code/adapter.ts`, `src/engine/types.ts`, `src/engine/run.ts`, `src/engine/execute.ts`, and narrowly `src/lifecycle.ts` for the interfaces below. Keep `supportsRuntime: false` on Codex. Introduce a private invocation envelope and invocation-bound policy passed as executeHooks' optional tenth argument, preserving the existing nine arguments and legacy Claude default. No raw payload is spread into the new public context, lifecycle event.input, result object, or serialized diagnostics. The Claude implementation delegates to existing behavior; policy callbacks for Claude must not change patch merging, vote precedence, lifecycle observer rules, turn filenames, or output bytes.

Audit raw lifecycle outputs before any engine-controlled handoff, currentToolInput change, result replacement, vote reduction, or parallel short-circuit decision. In the parallel promise-settle path, check the result before it enters the logic currently near execute.ts:727; auditing only the later handoff path is insufficient. Preserve the existing parallel rule that every updatedInput request, including an empty object, is a contract violation. All started lifecycles must retain one raw-history record even when aborted or rejected. Preserve afterHook's raw handlerResult and observer-only passthrough contract; do not add an override method. Policy covers beforeHook early results, handler results, malformed returned shapes, generated load/error results and parallel violations. Capability failures are latched in invocation state outside the ordinary result reducer and cannot become allows through onError:continue, trace, maxFailures, or a later successful hook.

Use typed origins to distinguish raw handler and lifecycle meta-gate results from engine-generated diagnostics. Current blockedByBefore cannot distinguish beforeHook.skip from handler skip: add an explicit source on LifecycleResult. Audit the completed handler value after afterHook has finished, because afterHook receives that mutable value by reference despite its own return being ignored. Preserve observer-only return warnings and do not allow an attempted override return to replace handler output. Never reject an internal diagnostic merely because SessionStart authors have no allow method; route diagnostics independently rather than running a strict author validator over a final internally composed result.

Every accepted result and candidate input is deeply detached from hook-owned mutable JSON before effects; retain opaque key spelling. Close an invocation/group effect-commit token after timeout/abort/finalization. Record each started-but-unsettled lifecycle as an abandoned error exactly once at abort, using the existing invocation tracker; late settlement must not append or commit any history. A promise settling afterward may not audit into a second decision, create a handoff file, merge input, vote, or overwrite output. Check the token before asynchronous effects and after awaited handoff returns before committing results. Do not permit an already-started handoff write after cancellation to affect output; retain ordinary bounded cleanup for any file whose write had already begun. Tests distinguish that unavoidable in-flight write from starting a new post-abort effect. Auditing governs Clooks-controlled result effects only: trusted hook code can already perform arbitrary I/O, and JavaScript abort signals do not cancel uncooperative code.

Add focused units under `src/agents/` and `src/engine/` using an injected invocation policy to prove private metadata isolation, serial/concurrent invocation separation, audit-before-abort order with controlled barriers, beforeHook skip/block, afterHook observer behavior, sticky rejection and raw-history recording. These are unit proofs of the boundary, including Codex-specific C04 scenarios; they are not compiled Codex runtime evidence while supportsRuntime is false. M1 compiled acceptance exercises real Claude through `test/e2e/agent-adapter.e2e.test.ts` and existing execution, lifecycle, handoff and turn-state journeys: preserve explicit/default output, notify-only routing, ConfigChange downgrade, plugin behavior, PreToolUse vote/patch behavior and early exits. Add focused Claude compiled regressions to those journeys where necessary, without a test-only production selector or enabling Codex. Run M1 focused units and full Docker E2E using Concrete Steps. Codex compiled C04 and related event-specific acceptance belongs to the reachable M2/M3 paths. Update execution, cross-agent and CLI domain descriptions after implementation.

### M2: Execute and Translate Codex PreToolUse


Create `src/agents/codex/normalize.ts`, `src/agents/codex/policy.ts`, `src/agents/codex/translate.ts`, and `src/agents/codex/tool-codecs.ts`; replace the placeholder adapter with these functions only after M0's applicable gates pass. Keep all ten event names recognized, but until M3 completes, unimplemented event handlers return explicit event-aware unsupported-runtime failures rather than success. This is an internal development milestone, not a release or new CLI staging warning.

Normalize a valid PreToolUse invocation with an explicit field allowlist and private original envelope. Preserve canonical `apply_patch` identity; matcher aliases Edit/Write never rename a patch into Claude file-edit input. Bash command and patch text map only through evidence-backed object codecs; other record inputs preserve arbitrary key spelling recursively. Tool responses remain arbitrary JSON. Reject arrays/scalars/null tool input with a precise unsupported-shape failure when no honest existing public context mapping exists; never wrap input in an invented value/arguments field. Known tool names may use a Claude typed arm only when the validated input satisfies that arm; otherwise fail rather than lie about its type. Unknown record tools use the existing documented Unknown*Context escape hatch.

Implement the Codex patch algorithm separately from Claude's omit-all-null merge. Clone the current input; visit patch own keys only; ignore undefined values; delete keys explicitly set to null; replace other keys shallowly; preserve untouched nulls and opaque nested keys. Validate the full candidate with the selected tool codec before committing to sequential state. Return a detached full input for downstream hooks and final encoding, never a patch masquerading as replacement. A rejected candidate does not change originalToolInput or later contexts. Empty/no-op patches preserve content; removal of required command/input fields fails. Unknown tool codecs may observe records but may rewrite only if M0 proves the corresponding replacement transport and validation contract. No dependency on arbitrary transcript parsing.

Per-result policy maps PreToolUse allow without rewrite to no native decision plus optional context, retaining native approval/sandbox policy. Every allow reason, with or without rewrite, becomes human systemMessage with explicit original-recipient loss. Never emit permissionDecisionReason for allow: the parser discards it even when syntactically accepted with a rewrite. Only an approved full replacement emits permissionDecision:allow with updatedInput. Reject ask/defer and unsupported rewrites before effects, including losing votes. Audit blank required denial/continuation reasons before effects and generate a nonblank policy-failure reason. Success writes one JSON response when decision/context/systemMessage exists, otherwise empty stdout; failure cannot become warning-plus-allow.

Implement Codex filtered-environment discovery and provider-isolated failure/turn namespaces here, before enabling real hooks. Keep explicit CLOOKS_PROJECT_ROOT unchanged, ignore inherited CLAUDE_PROJECT_DIR only for Codex engine discovery, and call the existing discovery env option without global mutation. Include known-event config failures and unexpected exceptions in the same final-output path. Do not defer root correctness or failure handling to M4.

Add codec/normalization/policy/translation units and `test/e2e/codex-runtime.e2e.test.ts`. Through the compiled binary, configure real home/project/local hooks, prove positive hook marker writes, merge order/shadowing/local overrides, deny and context output, and accepted rewrite round trips. Pair each rejected-effect assertion with a reachable positive case: no handoff file on rejected result, no subsequent mutated input, and a losing unsupported result still rejects. Run parallel cases with completion order reversed and verify no unvalidated result triggers an abort or vote. For malformed input and non-record shapes, no hook marker appears after a known-good baseline. These are Clooks replay tests, not proof Codex executed or refused a command. Run units and full Docker E2E and update discovery, global, execution, cross-agent and testing domains in this milestone.

### M3: Complete the Remaining Nine Events Without Expanding Authoring


Extend the same normalizer, policy and translator for SessionStart, SubagentStart, PermissionRequest, PostToolUse, PreCompact, PostCompact, UserPromptSubmit, SubagentStop and Stop. Use the result sets and capability policies below; native common-output fields do not create new public methods. SessionStart/SubagentStart inject only from skip; PostCompact remains a pure observer. Audit beforeHook meta-gates separately. Refuse PermissionRequest rewrites, permission updates and interrupt; audit sessionTitle on every UserPromptSubmit arm; preserve PostToolUse's completed-action limitation. Validate missing/null optional context policy without inventing compaction text or child identity.

Each event requires a real TypeScript hook replay through the compiled binary, valid no-op, supported result variants, lifecycle-produced result, unsupported-field rejection/diagnostic, and malformed-envelope case. Add `src/agents/codex/events.test.ts` and extend normalization/translation/policy suites plus `test/e2e/codex-events.e2e.test.ts`. Cover SubagentStart child context and SubagentStop child continuation using actual encoded outputs and private recipient assertions, without claiming native delivery. PreCompact refusal occurs before compaction; PostCompact output cannot undo it. Cover native code-mode PostToolUse as a distinct documented transport case only if M0 establishes its input discriminator; never infer that mode from tool name.

Stop/SubagentStop block means continue working, not reject a completed answer. These handlers retain allow/block/skip; native continue:false precedence is a translator/parser concern, not a new stop method. Their error path must not accidentally create an endless continuation. Wire support that cannot be expressed through existing methods is outside the author surface. Run event units, full Docker E2E and Claude regression suites; update cross-agent, execution and relevant type-system domain explanations after behavior exists.

### M4: Verify Turn, Delivery, Failure and Provider Isolation Together


Complete `src/engine/turn-state.ts`, `src/engine/handoff.ts`, `src/failures.ts`, `src/engine/run.ts`, `src/cli.ts` and `src/commands/init-entrypoint.ts` integration only as required by the private policy interfaces. Keep Claude legacy state paths and behavior. Codex uses separate provider-qualified state paths; config/load/hook failure counters must not contaminate Claude even when hook names and session IDs match. Parent session identity and referenced child scope are different fields. Never use child agent_id as a fabricated session_id. Missing reliable scope/turn identity disables persistent Codex history for that invocation and returns the existing empty ctx.turn; it must not fall into main or reset unrelated scopes.

Use the accepted best-effort ordinary root/ThreadSpawn boundary policy. Reset all scopes on root UserPromptSubmit, including same-ID steering; preserve on child UserPromptSubmit and same-ID/no-prompt Stop continuation. Reset startup/clear and preserve resume/compact. Synthetic Review prompts without agent_id are indistinguishable from root prompts and can reset history; this is accepted, documented and nonblocking. Do not disable Codex history or add a Review workaround. No stable delivery UUID exists, so claim neither retry deduplication nor full genuine-user parity. Keep snapshot/commit epoch-generation checks and bounded locking; late prior-turn writers cannot append to new-generation state. Replay the ordinary reminder sequence with this explicit limitation.

Codex handoff defaults to inline, even when configuration requests handoff, until a particular field's intended recipient and file readability are evidenced. A single concise diagnostic explains that a requested handoff remains inline. Preserve shared content-addressed storage, thresholds and cleanup; eligible files are generated only after policy acceptance. Human-facing reasons/system messages/diagnostics remain inline. Child pointers require child-readable paths, not a parent's ability to read them. Code-mode, multi-tool, and spilled-output delivery require explicit evidence; inline fallback never changes the decision. Handoff failure remains inline with a warning.

Unify final output exits for success, advisory-only, no-match, degraded config, recognized-event failures and unexpected exceptions, propagating every translated exitCode. Audit CLI signal and exception handlers and launcher fallbacks. Preserve missing-binary/bootstrap bypass and Claude unexpected-exit behavior. Unknown-event process death uses local failure with no fabricated event or prevention guarantee. Source establishes PermissionRequest exit 2 plus nonblank stderr as denial; blank stderr yields Failed/no decision and ordinary review. Clooks still chooses explicit exit-0 JSON denial for recoverable failures. Never interpolate exception text into shell JSON or infer universal enforcement from exit 2.

Add/extend `src/engine/turn-state.test.ts`, `src/engine/handoff.test.ts`, `src/engine/run.agent-adapter.test.ts`, `src/failures.test.ts`, `test/e2e/codex-state-delivery.e2e.test.ts`, `test/e2e/fail-closed.e2e.test.ts`, `test/e2e/entrypoint.e2e.test.ts`, and `test/e2e/turn-state.e2e.test.ts`. Verify interleaved Claude/Codex equal IDs, two Codex sessions, parent/two children, duplicate/late/concurrent arrivals, no-match boundaries, storage failure, accepted/rejected handoff, timeout/crash/load/config failures, and advisory-only exit propagation. Use barriers/positive markers, not sleep-only timing. Run full unit and E2E gates, obtain independent review/QA, update all affected domains, and close only resolved findings. D completion still does not imply Plan E native conformance or Plan F public release claims.

## Concrete Steps


M0 acquisition commands are recorded before execution in `tmp/codex-runtime-m0/PROPOSED-COMMAND.md` and the durable `docs/plans/feat-0044-cross-agent-portability/codex-runtime-m0-evidence.md`. The delegated container uses the existing image by immutable ID, a read-only root filesystem and script mount, disposable homes, and a narrowly scoped scratch-output mount. Preserve the public source archive and its checksum there for later offline inspection/probes without repeating acquisition. No upstream source is executed during acquisition. Source verification must resolve `refs/tags/rust-v0.153.4` specifically, not an ambiguous branch-or-tag name.

Source/tests are frozen during M2's final validation; this writer currently edits only Plan D and ATTENTION. Work from `/opt/development/clooks` and let the parent-owned runner coordinate one Docker run at a time. The existing Docker entrypoint typechecks and compiles inside the container before running forwarded test arguments. These host commands orchestrate Docker; test code never runs on the host:

    bun run test:e2e src/agents/ src/engine/ src/lifecycle.test.ts
    bun run test:e2e

For M2/M3 focused validation after the named new tests exist:

    bun run test:e2e src/agents/codex/
    bun run test:e2e test/e2e/codex-runtime.e2e.test.ts test/e2e/codex-events.e2e.test.ts

For M4 and final regression:

    bun run test:e2e src/
    bun run test:e2e

The focused commands do not replace each milestone's full E2E gate. Expected results are typecheck/build success and zero test failures, with positive hook invocation markers and explicit parsed output assertions. Record actual counts and exit status when run; do not copy C1 numbers into D progress. The known coverage threshold issue is separate from these gates and remains reported until independently fixed.

All test subprocess environments explicitly set disposable HOME, CODEX_HOME, CLOOKS_HOME_ROOT, project roots and local PATH, restore inherited variables in finally, and impose deadlines. Do not mount host homes, auth, config/trust, or installed binaries into containers; source/test mounts remain read-only and outputs stay container-local. No deploy:local, host init/uninstall, host bun test, credential copying, global binary replacement, or guard bypass. Source/executor spikes use explicit container binary paths and local scratch scripts under repository `tmp/` during the completed M0 attempts; their commands and failure dispositions remain recorded. The bounded attempts are exhausted; no third attempt or lock modification is authorized. No scratch files are part of this drafting task.

## Validation and Acceptance


Final M4 supplied evidence: tmp/codex-runtime-m4/20260908T093707Z-units-g43QGI/output.log (2,192/0, 69 files, 8,229 assertions, 3.33 seconds); tmp/codex-runtime-m4/20260908T093440Z-e2e-4jueMy/output.log (787/0, 47 files, 9,268 assertions, 104.18 seconds); tmp/codex-runtime-m4/20260908T093645Z-static-JThIEb/output.log (typecheck/lint/format exit 0; lint 112 warnings/0 errors). Matching source-manifest hash: 11f1bc064814471e5c2edc41996eeae742d430ca06472c22717ae222f87e47eb. Source/wrappers are reported unchanged; image/cleanup audit is still parent-owned. Final documentation/milestone GO remains pending. All execution was parent-run disposable Docker; no host execution, native probe or P/L claim.

The M3 acceptance record below is historical; its final milestone GO preceded M4 authorization.

M0/M1/M2 acceptance is complete. M3 implementation/review and matching-snapshot validation passed: 2,168 units/0 failures, 774 full E2E/0 failures, typecheck/lint/format exit 0 with 112 lint warnings. Final documentation review remains before milestone GO. Earlier failed runs are retained with their actual resolved or unexplained dispositions. P/L remain absent; accepted Review ambiguity and recipient readability limits do not become live proof.

Final M3 evidence (parent-run disposable Docker, Bun 1.3.10): tmp/codex-runtime-m3/20260908T085912Z-units-xyOGuo/output.log; tmp/codex-runtime-m3/20260908T085938Z-e2e-TyxHcI/output.log; tmp/codex-runtime-m3/20260908T090136Z-static-edmPtE/output.log. Unit image sha256:1b2ea73774984ea7ece193dc991cf91fae846be7d55ebc2d4bade99947d77f53; E2E/static image sha256:645fb95d4cb3aa950717cf36d058d46fc999d43804a1933520abcca830abfb0c. All gates share source manifest 8224ddbd16af5dc77eb6e0cc29db315026378c2d0327c1166fbf5093c88e70ad before/after, with unchanged wrappers and no owned containers remaining. These are supplied execution results, not host-run tests or P/L evidence.

Future runtime acceptance requires positive hook execution for all ten agreed events, exact supported output with no unsupported fields, explicit event-aware failures, honest unknown-tool/nullable contexts, rejection before input/handoff/reduction, and preservation of Claude output/methods/state. Every negative assertion first establishes that the fixture can reach the relevant path. Test unsupported control on losing votes, overwritten sequential results, lifecycle early gates, parallel completion, generated errors, and after reducer diagnostics. Preserve unchanged opaque nested keys, null data and original input; reject malformed candidates before later hooks observe them.

Event-aware failure means a diagnostic plus a source-supported refusal when one exists, not a universal exit-code promise. PreToolUse/UserPromptSubmit reject pending work; PermissionRequest uses explicit JSON denial (native exit 2 also denies only with nonblank stderr); PostToolUse reports feedback after execution and cannot replace MCP output or undo effects. Compaction and Stop have distinct timing/continuation semantics. Required nonblank reasons must be audited before effects. The policy table controls known-event failures; unknown-event/process-death failures make no prevention claim. U/D/P/L verification remains pending.

Preserve merged home/project/local behavior and explicit-root precedence with both inherited-root variables set to distinct real fixture projects. Under Codex, only the intentional CLOOKS_PROJECT_ROOT wins, otherwise cwd discovery applies; Claude retains its CLAUDE_PROJECT_DIR anchor. No native hook activation, trust, exactly-once or live enforcement is established by compiled replay or C1 receipts. Plan E must test those separately with live evidence or the epic's explicit release-confidence decision.

## Idempotence and Recovery


Do not reset, clean, stage, commit, or overwrite concurrent changes. Future validation uses disposable Docker workspaces and container-only HOME/config/auth/trust state. Preserve all current C1 changes and TODO entries.

## Artifacts and Notes


Archived runtime attention: docs/plans/done/feat-0044-codex-runtime/ATTENTION.md. The active docs/plans/feat-0044-cross-agent-portability/ATTENTION.md is a pointer only. Shared artifacts remain at these explicit repository-relative paths:

docs/plans/feat-0044-cross-agent-portability/codex-capability-matrix.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-acceptance-matrix.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-contract-refresh.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-m0-evidence.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-m0-identity-delivery.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-m0-wire-contract.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-m1-evidence.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-m2-evidence.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-m3-evidence.md

docs/plans/feat-0044-cross-agent-portability/codex-runtime-m4-evidence.md

docs/plans/feat-0044-cross-agent-portability/continuation-review.md

Parent reports new Plan D, acceptance matrix, research and M0 sidecars are ignored artifacts requiring an explicit force-add when later staging is authorized. Scratch tmp remains ignored. This task stages nothing and makes no commits. Parent reports the baseline tracked src/test/schema/README diff fingerprint unchanged at supplied prefix bb767e..., unchanged untracked registration-state files, and clean direct whitespace scans; no full digest is invented here.

M0/M1 have final code/QA/documentation closure. M2 is implemented, source-frozen and validated by full units/E2E/static checks; final docs review alone remains. Lagrange owns the M2 evidence and acceptance matrix; Lorentz owns domains, epic and findings. This writer changes only Plan D and ATTENTION during the source freeze.
Evidence status: exact-release source acquisition and wire/identity audits are complete. Probe 1 stopped for missing Rust; the bounded retry stopped at cargo fetch --locked because upstream Cargo.lock required updating. Zero upstream tests/no P; no retry remains. M1 evidence is in docs/plans/feat-0044-cross-agent-portability/codex-runtime-m1-evidence.md. M2's current Clooks U/D results are tracked in Progress and the separately owned evidence record, not promoted to upstream executor or live-native proof. Release confidence remains unverified.

## Interfaces and Dependencies


The implementation retains existing libraries and engine helpers, with no agent SDK or general execution framework. The following private types live in `src/agents/types.ts` and are re-exported through `src/agents/index.ts` only for internal consumers, never `src/types/index.ts`:

    type JsonValue = null | boolean | number | string | JsonValue[]
      | { [key: string]: JsonValue }
    type ResultOrigin = 'before-hook' | 'handler' | 'engine-error'
      | 'parallel-contract' | 'load-error' | 'engine-diagnostic'
    interface NormalizedInvocation {
      eventName: EventName
      context: Record<string, unknown>
      private: InvocationMetadata
    }
    interface InvocationMetadata {
      provider: AgentId
      raw: Readonly<Record<string, unknown>>
      sessionId: string | null
      nativeTurnId: string | null
      referencedAgentId: string | null
      tool: ToolCodec | null
    }
    interface ResultPolicyInput {
      value: unknown
      origin: ResultOrigin
      hookName?: HookName
      parallel: boolean
      currentToolInput?: Readonly<Record<string, unknown>>
    }
    type CheckedResult =
      | { kind: 'accepted'; result?: EngineResult;
          nextToolInput?: Record<string, unknown>; diagnostics: string[] }
      | { kind: 'rejected'; failure: RuntimePolicyFailure }
    interface RuntimePolicyFailure {
      eventName: EventName | null
      hookName?: HookName
      capability: string
      message: string
    }
    interface ToolCodec {
      canonicalName: string
      decode(input: JsonValue): Record<string, unknown>
      applyPatch(current: Readonly<Record<string, unknown>>,
        patch: Readonly<Record<string, unknown>>): Record<string, unknown>
      encode(input: Readonly<Record<string, unknown>>): JsonValue
    }
    interface InvocationResultPolicy {
      validateRawResult?(value: unknown): RuntimePolicyFailure | undefined
      checkResult(input: ResultPolicyInput): CheckedResult
      handoff?: {
        isEligible(field: 'injectContext' | 'reason' | 'feedback'): boolean
        inlineDiagnostic: string
      }
    }
    interface ComposeDiagnosticsInput {
      eventName: EventName
      result?: EngineResult
      traceMessages: string[]
      degradedMessages: string[]
      debugMessages: string[]
    }
    interface ComposedDiagnostics {
      result?: EngineResult
      stderr: string[]
      systemMessages: string[]
    }

M1 introduces AgentAdapter.createResultPolicy(invocation: NormalizedInvocation): InvocationResultPolicy and AgentAdapter.composeDiagnostics(input: ComposeDiagnosticsInput): ComposedDiagnostics. The factory binds private invocation state; the composer is pure and performs no I/O. The engine emits returned stderr and carries systemMessages to final translation. ExecutionResult in src/engine/types.ts now includes optional policyFailure: RuntimePolicyFailure independently of lastResult. These are internal interfaces, not public hook methods.

The shared src/engine/result-policy.ts exports legacyResultPolicy and checkDetachedResult(policy, input, eventName): CheckedResult. It first invokes optional validateRawResult inside the protected boundary, then deep-clones policy input and accepted output and converts exceptions to typed rejection. Codex preflight inspects descriptors without evaluating accessors and rejects sparse/extra-property arrays, preventing lodash from erasing invalid shape before validation. Preflight cannot authorize effects; detached checkResult remains mandatory. Claude and existing injected policies omit preflight and retain their previous behavior. Lifecycle origin remains before-hook or handler, with raw handler results audited after the observer completes.

ToolCodec functions are pure. Their exceptions become typed normalization failures or rejected candidates, not hook crashes subject to onError. decode validates records, applyPatch validates the complete materialized candidate, and encode produces the native full replacement. Bash/exec_command and canonical apply_patch support command-only updates; unsupported command keys reject even when their patch value is null. MCP records preserve opaque keys and nested JSON under full replacement, without claiming downstream server acceptance. Other unknown record tools are observable but lack a rewrite codec. Known public tool arms validate required and optional input fields before use; incompatible shapes reject. No recursive key conversion, invented patch-to-Edit/Write mapping, singleton invocation state or public type expansion is introduced.

The adapter uses normalizeInvocation(payload, eventName): NormalizedInvocation and cheap readEventName recognition. discoveryEnvironment returns unchanged Claude semantics and a copied Codex view without CLAUDE_PROJECT_DIR. Final-output input carries the invocation and optional latched RuntimePolicyFailure. translateFailure accepts a null event for unidentified failures, never sending synthetic __parse__ upstream. CONFIG_ERROR_EVENT remains local accounting only. Known failure diagnostics include hook (runtime when no author hook applies), event, capability and the requested event-specific disposition; unsupported events are not silent allows.

Add optional tenth `policy?: InvocationResultPolicy` to `executeHooks`; its omitted/default implementation preserves Claude behavior. Add the latched policy failure to the execution return value. Supply currentToolInput into each audit and commit nextToolInput only after acceptance. The reducer receives detached accepted results; engine failure is finalized separately and dominates a would-be success. Extend the lifecycle result with an internal origin where necessary to distinguish beforeHook versus handler; afterHook remains observer-only. Trace/degraded/debug diagnostics use a provider-owned message composer rather than synthesizing unsupported author decisions after the audit.

Reviewed finalization rule: runEngineCore selects translateFailure itself whenever ExecutionResult.policyFailure is present; neither composeDiagnostics nor adjustResultBeforeFinalOutput can clear that selection. Configured no-hooks, no-matches and no-result paths invoke final translation even with empty diagnostics/messages and retain the adapter-selected exit. The intentional no-config bypass still returns without reading/normalizing unused input or invoking final translation. These are M1 control-flow clarifications consistent with the existing goals, not a new test selector or an enabled Codex path.

runEngineCore now wraps runEngineInvocation with invocation-local event/normalization state. Internal EngineCompletion carries an already-selected exit out to the single process-exit point; unexpected early-stage errors use adapter failure translation. Config loading establishes whether input is needed, then configured early-stage normalization precedes counter reads/writes/clearing as well as hook preparation/import. Accepted input is cached once. Claude retains its late parse/nonobject/unknown-event diagnostics and exits, and no-config malformed input remains a bypass. A rejected configured Codex envelope cannot import hooks or mutate failure/turn state.

Provider state namespacing is implemented in path helpers, never public sessionId rewriting. Codex project counters use `<projectRoot>/.clooks/.cache/agents/codex/failures.json`; home-only counters use `<homeRoot>/.clooks/failures/codex/<existing-project-hash>.json`; turn documents use `<homeRoot>/.clooks/turn-state/codex/<existing-session-hash>.json`. Before loadConfig returns ownership, config-error placement uses project clooks.yml presence excluding projectRoot === homeRoot; recovery uses that same selection. Claude config-error and ordinary paths retain their legacy behavior.

getFailureLocation returns the internal FailureLocation = string | { path: string; root: string }. Legacy callers keep strings; Codex carries the actual selected project/home root. readFailures/writeFailures and executeHooks' existing fifth argument accept that union. Managed Codex I/O validates each parent, refuses linked/nonregular final files, reads through a no-follow/nonblocking descriptor and publishes private atomic staging files; recovery deletion also validates the location. Provider parameters reach turn directory creation, commit, boundary, tracker and prune helpers. readTurnState(path, optional { homeRoot, provider }) adds read-only Codex parent validation before the existing bounded file read. Shared readBounded adds O_NONBLOCK so FIFOs cannot block either provider before fstat. This is stable managed-state isolation, not a generalized store framework or an adversarial-filesystem race guarantee.

A private turn policy computes scope and boundary from approved native metadata. Raw records still describe what a hook returned, not what was accepted or delivered. Handoff accepts a provider delivery eligibility predicate with Claude's existing eligibility as default; Codex defaults false until proof for each field/audience is recorded.

The concrete optional private adapter additions for this integration are inputStage?: 'before-hooks' | 'after-hooks' and resolveTurnPolicy?(invocation: NormalizedInvocation): InvocationTurnPolicy | null, where InvocationTurnPolicy is { sessionId: string; scopeKey: string; boundary: 'reset' | 'advance' | null; prune: boolean }. The omitted methods retain Claude compatibility for existing adapters and injected test adapters. A provided resolver returning null must not fall through to legacy main scope. Storage namespace branching remains inside shared path helpers, not event orchestration. Handoff eligibility stays on the existing invocation-bound result policy; its optional predicate defaults to legacy delivery, and a refused eligible request reports one invocation-local inline-fallback diagnostic before any filesystem action.

Actual M2 API deviation check: all changes are private to adapters/engine/state helpers. The implemented refinements are optional adapter inputStage/resolveTurnPolicy; InvocationPolicyError carrying structured failure; optional validateRawResult and handoff policy; FailureLocation in executeHooks' existing fifth argument; optional provider arguments throughout turn storage and read-only access metadata on readTurnState; and internal EngineCompletion finalization. executeHooks retains its optional tenth policy argument with no eleventh argument. Public contexts/results/methods, registration selectors/configuration, schemas and README are unchanged by M2. Home-only config-error placement is the documented routine clarification, including projectRoot === homeRoot. O_NONBLOCK is the reviewed incidental nonregular-history hang fix; no authorization or general storage framework was introduced.

### Existing Public Results and Codex Policy


SessionStart and SubagentStart handlers return skip with optional injectContext. Current documentation describes main developer context and child context respectively; SubagentStart ignores native continue:false. No allow/block method is added. A lifecycle block on these events is audited as a meta-gate and produces an explicit unsupported-enforcement outcome if the native event lacks that effect, not a silently successful skip.

These audiences and SubagentStart's lack of veto are now source-confirmed. SessionStart generated failure can stop pending work through continue:false; SubagentStart retains the explicit local-failure tuple. No new shared API or native async scope is introduced by this reconciliation.

PreToolUse allow without rewrite is no native decision with optional context and human systemMessage for any reason, disclosing original allow-reason recipient loss. Native policy remains in force; this differs from legacy Claude allow and from PermissionRequest's actual grant. An approved rewrite emits allow plus the complete replacement and optional human systemMessage for its reason, with the same explicit recipient-loss annotation. permissionDecisionReason is emitted only for deny; on allow the native parser accepts but discards it. Block carries a nonblank denial reason and optional context; skip is no decision. Preserve ctx.skip's debug-only surface. Reject ask/defer and unsupported rewrites before effects. Audit fields by membership, with undefined absent; false/empty/null values still require valid types and semantics.

PermissionRequest allow/block/skip maps only to supported allow/deny/no-op. updatedInput, updatedPermissions and interrupt are unsupported. PostToolUse skip/block may carry supported context, but updatedMCPToolOutput is unsupported. Rejection after a completed tool reports the failure honestly; no rollback is promised. Current docs separately describe code-mode block as promise rejection while continue:false does not do that; exact version/mode decoding remains M0 evidence.

PreCompact allow/block/skip retains its public meaning, with block requiring an evidenced pre-compaction refusal. PostCompact skip-only remains observation. Native false-after-compaction does not expand its methods. UserPromptSubmit allow/block/skip preserves supported context and prompt denial; sessionTitle is an unsupported mutation on all three arms and rejects the invocation even when another hook would overwrite it. No native rename field is invented.

Stop and SubagentStop allow/block/skip use block for continuation with reason and the correct recipient; no continue:false author API is introduced. A translator never emits both continuation and termination for the same supported result. Existing debugMessage is a local debug facility, not an arbitrary native JSON field. Unknown runtime fields from JavaScript reject as invalid result fields and are never forwarded. Well-typed unsupported injectContext follows only the explicit communication policy below. afterHook returns cannot introduce a new result override; existing observer warnings remain.

Source exit guidance: exit 2 with nonblank stderr denies PreToolUse/PermissionRequest, blocks the inspected UserPromptSubmit, gives PostToolUse feedback, and continues Stop/SubagentStop. Blank stderr produces Failed/no control. Start and compact exit 2 are Failed/no control; SubagentStart discards detailed stderr, compact retains it as error text. Other nonzero, signal/no-code, timeout and malformed output generally fail without a veto. These facts do not authorize forwarding invalid capabilities: Clooks emits the explicit supported refusal below. P/L remain unverified.

### Complete Result, Field and Delivery Policy


This table is the full-plan Clooks policy reconciled with 0.153.4 source consumers. M2 implements the PreToolUse rows and known-event failure translation; the other nine author-event rows remain M3 work. Current U/D progress is recorded above and no P/L claim is made. C means `hookSpecificOutput: { hookEventName: <exact event>, additionalContext: <text> }`; N means empty stdout, exit 0; D means inline top-level systemMessage for a native human warning. Successful stderr is discarded upstream; stderr debug is local-only. JSON responses exit 0, omit absent fields and retain strings except required nonblank validation and disclosed native trimming. No-decision plus diagnostic emits systemMessage JSON. Clooks enforces its own exact event/author contract instead of relying on native Serde permissiveness.

| Event and handler arm | Allowed optional fields and exact output | Recipient and intended native action | Unsupported or invalid fields |
| --- | --- | --- | --- |
| SessionStart skip | injectContext -> C; absent context -> N | Main developer context; startup/resume/compact observation | Every other semantic field rejects; debugMessage is local-only |
| SubagentStart skip | injectContext -> C; absent context -> N | Referenced child context; no startup veto | Every other semantic field rejects; debugMessage local-only |
| PreToolUse allow without rewrite | N or optional C; reason -> systemMessage human annotation stating original allow-reason recipient loss, never permissionDecisionReason/top-level reason | No native decision; native approval/sandbox policy retained; optional context reaches invoking model, reason only human | Bare native allow is invalid; never invent replacement/grant/denial to carry reason; foreign fields reject |
| PreToolUse allow with approved rewrite | hookSpecificOutput with exact event, permissionDecision:"allow", complete updatedInput; optional reason -> systemMessage with explicit original allow-reason recipient loss; context -> additionalContext | Replace supported pending input; reason is human annotation only, not decision-reason delivery; native policy still applies | Unapproved codec/keys reject before effects; no patch-only serialization |
| PreToolUse block | hookSpecificOutput with hookEventName, permissionDecision:"deny", permissionDecisionReason:reason; injectContext -> additionalContext | Current caller/model receives denial/context; pending call denied | updatedInput and every other semantic field reject |
| PreToolUse skip | When selected by existing reduction: injectContext -> C with no permissionDecision; absent context -> N | Selected skip delivers context to current tool caller/model; native default decision unchanged. Losing skip context is omitted by reduction, not unsupported capability handling | No method widening: ctx.skip remains debug-only; valid raw result-union context is supported; other semantic fields reject |
| PreToolUse ask/defer | Sticky failure tuple below; none of their fields enter merge/votes/handoff | Human diagnostic plus pending-call denial through failure translation | Whole unsupported control rejects even when another deny would win |
| PermissionRequest allow | hookSpecificOutput with hookEventName and decision:{behavior:"allow"} | Approval consumer permits pending operation | updatedInput, updatedPermissions, and all other semantic fields reject |
| PermissionRequest block | hookSpecificOutput with hookEventName and decision:{behavior:"deny",message:reason} | Human permission message inline; approval denied | interrupt, even false, and all other semantic fields reject |
| PermissionRequest skip | N | Normal approval flow remains | All semantic fields reject |
| PostToolUse skip | injectContext -> C; absent context -> N | Current caller/model receives context after execution | updatedMCPToolOutput, including null, rejects; all other semantic fields reject |
| PostToolUse block | top-level decision:"block", nonblank reason; optional C | Invoking model receives source-supported rejected-result feedback after effects; no rollback | updatedMCPToolOutput and all other semantic fields reject |
| PreCompact allow/skip | N | Continue compaction normally | All semantic fields reject |
| PreCompact block | top-level continue:false, stopReason:reason | Source-supported stop before compaction; no context delivery | All other semantic fields reject |
| PostCompact skip | N | Observe completed compaction | All semantic fields reject; no handler block/allow added |
| UserPromptSubmit allow/skip | injectContext -> C; absent context -> N | Main developer context; prompt proceeds/defaults | sessionTitle rejects on either arm; other semantic fields reject |
| UserPromptSubmit block | top-level decision:"block", reason; optional C | Human denial reason inline; main context as supported; prompt rejected | sessionTitle and other semantic fields reject |
| Stop/SubagentStop allow/skip | N | Finish normally | All semantic fields reject |
| Stop/SubagentStop block | top-level decision:"block", reason | Main or referenced child gets continuation prompt respectively | All other semantic fields reject; no raw continue/feedback/stop controls |

Every row permits well-typed debugMessage on debug-enabled local stderr only; successful native consumers discard it. Undefined/null lifecycle returns remain skip; arrays, other primitives, invalid tags/types and unknown own fields reject. Audit blank/whitespace denial and continuation reasons before any effects, emitting a generated nonblank failure reason rather than source-invalid refusal. Native PreToolUse/Stop reasons trim; prompt/post block reasons validate nonblank but retain text. PermissionRequest blank message has a native default, but Clooks validates block reasons consistently. Field membership matters: undefined is absent; false/null/empty values still require valid types and semantics. Raw continue is not a Clooks tag.

Per-result capability support does not promise final selection. Preserve `reducePreToolUseVotes()` exactly: allow winners accumulate allow context only; deny winners retain their own context plus eligible allow/ask loser context, not skip context; equal-rank all-skip votes select the last vote in execution/configured order. Do not add context aggregation or reinterpret discarded skip context as unsupported delivery. A single raw skip+injectContext or a selected last skip+injectContext emits C. A losing skip's context produces neither additionalContext nor an unsupported-capability warning. With Codex handoff inline by default, these cases create no handoff files.

M2 unit and compiled Docker regressions must assert these exact tuples with debug disabled, handoff false, no advisories and positive markers from every hook. Use raw skip objects for context because ctx.skip remains debug-only. Unit assertions inspect accepted and reduced results; Docker assertions inspect literal JSON/empty stdout, empty stderr, exit 0 and ordered markers. Native semantics are source-backed; U/D/P/L remain pending:

| Ordered PreToolUse hook results | Selected result and exact stdout | Diagnostic, recipient, exit and intended action |
| --- | --- | --- |
| skip with injectContext:"A" alone | skip with A; `{"hookSpecificOutput":{"hookEventName":"PreToolUse","additionalContext":"A"}}` | No diagnostic; A reaches tool caller/model; exit 0; native default decision |
| skip with A, then plain allow | allow; empty stdout | No diagnostic or context; exit 0; no native decision, ordinary native policy retained |
| skip with A, then block with reason:"deny" | block; `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"deny"}}` | No diagnostic/context; denial reason reaches tool caller; exit 0; deny pending call |
| skip with A, then plain skip | last skip; empty stdout | No diagnostic/context; exit 0; native default decision |
| skip with A, then skip with injectContext:"B" | last skip with B; `{"hookSpecificOutput":{"hookEventName":"PreToolUse","additionalContext":"B"}}` | No diagnostic; only B reaches tool caller/model; exit 0; native default decision |

For a plain allow reason R, pin `{"systemMessage":"clooks: PreToolUse allow reason (human annotation only; original allow-reason recipient unavailable; native policy retained): R"}`, exit 0, with optional C separately. This human annotation discloses loss of the original reason recipient; it is not model context or permission approval. Blank block reasons pin the nonblank generated reason and diagnostic `clooks: invalid blank block reason`, using the event's failure tuple below. Keep detailed hook/event/capability attribution for other policy failures. These are proposed literal U/D expectations, not observed output.

Run these tuples sequentially and in parallel with controlled completion orders, preserving the existing configured vote ordering. No new aggregation abstraction is authorized. Losing accepted context is composition behavior; unsupported fields must still be audited before that reduction can discard them.

Bash/exec_command and canonical apply_patch PreToolUse expose only an object with required string command. Reject patches adding any other key: native consumers extract command and ignore added keys, so cwd/timeout/hidden-option mutation cannot be claimed. Exec preserves original internal options independently; patch text remains opaque and apply_patch stays an Unknown public tool, never Claude Edit/Write shape. A string does not prove valid patch syntax. MCP/default local Function paths serialize complete JSON replacements; Clooks retains its honest record-only subset, with downstream handler/server validation still required. PermissionRequest uses separate approval payloads (Bash description is nested, no top-level description or tool_use_id); do not reuse command-only validation blindly for approval metadata. write_stdin skips PreToolUse and may report original Bash completion. Hosted/specialized interception and exactly-once completion remain unproven. Unapproved rewrites reject; arbitrary PostToolUse response JSON is preserved. Validate complete candidates after every patch without legacy null stripping.

The communication-loss rule is deliberately narrow. If the existing author surface admits injectContext but the selected event/arm consumer does not deliver it, retain the otherwise valid decision, omit only injectContext, create no handoff file for it, and append D. Exit stays the retained decision's exit (0 for every successful Codex JSON mapping above); the native action remains that decision's documented action, with no delivered context. The diagnostic names hook, event, capability, requested recipient and actual resulting action, for example:

    clooks: hook "note" on <event>: unsupported injectContext on <arm>; dropped context intended for <recipient>; retained <decision> and <resulting action>. No context was delivered.

Unknown or malformed fields and injectContext on an invalid handler arm do not qualify for this rule. Current docs explicitly support PreToolUse additionalContext without permissionDecision, so a valid raw skip+injectContext maps to C; the narrower ctx.skip method does not justify dropping the raw result-union field. None of the currently documented supported context cells is preemptively classified as lost. Apply the communication-loss tuple only when M0 establishes an unsupported event/arm delivery, and record that exact cell before implementation. On supported arms its recipient is current main/tool caller or the referenced child as specified above; unproven child identity never redirects text to the parent. Missing evidence remains gated rather than being silently classified as unsupported. Custom/vendored origin makes a hook eligible to run; it does not make every capability portable.

### Failure Routing and Sticky Rejection


Use these exact proposed outputs for configured known-event policy/normalization/generated-block failures after M0 verifies the selected consumer. Include D for policy rejection, with hook name when known (otherwise "runtime"), event, failed capability, and the resulting action. Messages never say an emitted local refusal proves native prevention. Invalid field/arm, sessionTitle, unsupported control/mutation and codec failure latch a failure before effects, regardless of onError/maxFailures. The first policy rejection immediately latches abort; no later accepted result can clear it. Select the primary failure in configured hook order only among failures already audited before abort closes the effect-commit token. Ignore all late settlements: do not audit them, add their diagnostics, replace the primary, commit history, merge input, hand off text or vote. Scheduling can change which failure is available first, so there is no promise of the same primary across completion orders. Sort any already-available failure diagnostics by configured order only within that bounded set. Barrier tests in both completion orders must assert a valid latched pre-abort failure, the appropriate primary among available failures, and no late effects; they must not require an unavailable lower-index failure to become primary.

| Known event | Failure JSON/exit/channel | Explicit action and limitation |
| --- | --- | --- |
| PreToolUse | deny shape above, reason naming capability; D; exit 0 | Request pending-call denial; no unsupported value executes through Clooks-controlled effects |
| PermissionRequest | deny decision shape above; D; exit 0 | Explicit Clooks refusal. Native non-null updatedInput/updatedPermissions or interrupt:true instead produce Failed/no decision and ordinary review, not fail-closed; another native handler's allow can win. Null Option/interrupt:false native permissiveness does not weaken Clooks field rejection |
| UserPromptSubmit | top-level decision:"block", reason; D; exit 0 | Request rejection of prompt, including rejected sessionTitle mutation |
| PostToolUse | top-level decision:"block", reason; D; exit 0 | Report failed policy by post-tool feedback; completed side effects remain; code-mode rejection is after execution |
| PreCompact | continue:false, stopReason naming failure; D; exit 0 | Source-supported stop before compaction; P/L unverified |
| SessionStart | continue:false, stopReason naming failure; D; exit 0 | Generated runtime failure requests stopping further work; does not add handler block/allow methods |
| Stop/SubagentStop | continue:false, stopReason naming failure; D; exit 0 | Stop the respective continuation path; never convert a runtime failure into repeated block/exit-2 continuation |
| SubagentStart/PostCompact | No stdout; one detailed local stderr diagnostic; exit 2 | Preserve local failure. Native Failed/no control: SubagentStart discards detail and reports generic exit failure; PostCompact retains stderr as error text. No child-start veto/rollback. Do not substitute successful warning JSON merely to deliver detail; PostCompact false continue could stop subsequent work but is not this selected failure tuple |

For the final row, explicit lack of native enforcement is a compatibility limitation, not an alternate success path. A user-facing guarantee that these events prevent work remains forbidden. Normal observer/context operation may be implemented once its own M0 cells pass; no claim that errors enforce those events is permitted. If selected-source evidence contradicts a proposed refusal in another row, production for that behavior remains gated until the plan records the exact supported replacement or an explicitly accepted narrower promise. Do not silently switch to allow.

Ordinary hook errors follow existing onError/circuit-breaker configuration; configured continue/degradation is intentional no-decision, not capability rejection. Map generated block failures with the table. Native human diagnostics use systemMessage on successful JSON paths; debug stderr is local-only. Nonzero local failure uses the explicit table limitations. No diagnostic handoff or illegal author arms; preserve Claude injection. Provider-local config accounting and explicitly configured degraded recovery remain.

Malformed JSON/non-object/unknown event, adapter selection failure, or unrecoverable invocation emits no stdout and local stderr, exit 2. Never fabricate __parse__/event/provider. CLI exceptions, signals and launcher unexpected exits use this unidentified failure when safe finalization cannot run. No exception-text shell JSON. Stop exit 2 with nonblank stderr requests continuation, not termination; PermissionRequest denies only with nonblank stderr, so an exit number alone proves nothing. Preserve bootstrap no-ops.

Codex orchestration order is prescribed, while Claude keeps its existing ordering:

    select explicit adapter; create invocation-local environment view
    discover project root; attempt config read without importing hook modules
    if config is absent: preserve existing no-config/noop behavior
    otherwise read/cache stdin exactly once, recognize event, normalize envelope
    if envelope invalid: translate known/unknown failure, no user imports/state boundary
    if config read failed: update provider-local config counter, translate its disposition
    otherwise clear recovered provider-local config failure state
    prepare config using adapter (Codex no-op for Claude plugins)
    load/match hooks; apply evidenced private turn boundary and pruning before early exits
    run lifecycles; audit completed values before serial effects or parallel abort/votes
    commit accepted codec input/handoff; reduce accepted detached results
    compose diagnostics separately; final failure dominates any reduced success
    emit exactly one translated response and honor its exit code

An unexpected discovery failure lacks a normalized invocation and uses the unidentified failure channel. No-config malformed stdin stays a no-op; this does not exempt invalid configured input from the pre-import gate. In tests, import markers and persisted state bytes must independently prove this ordering.

### Milestone-to-QA Mapping


`docs/plans/feat-0044-cross-agent-portability/codex-runtime-acceptance-matrix.md` defines stable IDs; every alternative within a row requires its own identifiable test case and literal expected output independent of the translator. M0 owns S/P evidence for E01-E10, all applicable F01-F09 and C01-C14 assumptions, plus the evidence dispositions for N01-N07. S is pinned source inspection, P is a pinned executor, U is a unit test, D is a compiled Docker replay, and L is a live native session. These labels are separate confidence classes.

M1 requires injected-policy U proofs for C01/C04/C05/C06/C14 and F06/F07 boundary behavior. Its D acceptance consists of reachable real Claude compiled regressions for the modified engine/interfaces, plus preservation of the disabled Codex placeholder; it does not claim Codex compiled C04 or other unsupported runtime paths. M2 supplies actual Codex U+D E03, F01-F09 for its vertical path, and C01-C09/C11/C14 prerequisites, including PreToolUse C04 and the exact skip-context tuples above. M3 supplies Codex U+D E01/E02/E04-E10 with every applicable F case and C02/C04/C05/C08-C12, including PermissionRequest/title/PostToolUse C04 cases. State, failure and handoff correctness accompany the first path that needs them. M4 requires full U+D parameter coverage E01-E10, F01-F09 and C01-C14, emphasizing C07/C10-C13 and cross-event integration. Every production milestone retains actual compiled regression acceptance and full Docker E2E. N01-N07 are Plan E native conformance/release-confidence gates; ordinary U/D may not be deferred to them.

For PreToolUse skip, matrix E03's raw result-union injectContext case is distinct from the actual ctx.skip debug-only method: test a single or selected context-carrying skip as context-only JSON without permissionDecision, the exact losing/all-skip composition tuples above, and the unchanged method type regression. UserPromptSubmit title follows sticky mutation rejection on every arm, consistently across the plan and matrix.

The compulsory ordinary-path Stop sequence is first Stop with zero prior interventions -> reminder block -> native continuation preserves history -> next Stop sees its raw prior block and skips -> next ordinary root UserPromptSubmit resets all scopes, including same-ID steering -> its Stop may remind again. Repeat for child scope and handoff, preserving history on child prompts. Synthetic Review without agent_id may also reset history under the accepted best-effort mapping; document this nonblocking limitation without a workaround or full genuine-user-parity claim. Synthetic replay is U/D only; actual P/L remains absent. Missing identity/storage failure retains the existing documented best-effort limits.

## Danger Zones and Guardrails


Before production changes, update `docs/plans/feat-0044-cross-agent-portability/ATTENTION.md` to spotlight shared adapter/execution contracts, root selection, failure translation and CLI/shell exits, provider-specific failure/turn storage and pruning, mutable lifecycle results, parallel late settlements, and handoff recipient routing. Review why each change is needed and how it could silently discard enforcement, expose metadata, erase another provider's history or deliver instructions to the wrong agent. The archived C1 ATTENTION describes completed registration work and must remain historical.

No permission/trust redesign is authorized. Preserve all uncommitted C1 changes and TODO.md; do not stage, commit, reset, clean, overwrite generated registration, or deploy while drafting. Future tests use container-local runtime/config/state and preserve host files. A missing native refusal channel is an evidence/product limitation, never justification to emit a guessed field or silently claim fail-closed support. Domain updates and independent review are part of every production milestone, not optional cleanup.

## Instruction Prompts


2026-09-08 post-completion instruction (verbatim):

> Usual retrospective. What went wrong? Where could your tooling or documentation have been improved?
>
> Regarding PLan D: I thin kyou can commit. Do you need to document the two test failures? That will just be dead stale documentation in no time. Are there more events Codex can cover?

Cleanup decision: isolated failed attempts belong in historical evidence, not indefinitely repeated active findings. Preserve their unknown causes and subsequent passing attempts; retain only actionable remaining gaps in living documentation. The coordinating agent owns the retrospective, current event research and authorized commit.


M4 assignment, recorded verbatim:

    FINAL M3code+QA GO; AUTHORIZE M4 NOW. Own ALL production+unit changes +PlanD/ATTENTION; firstmarkM3complete/M4inprogress +recordplannedwork BEFORE edits. Read M4fullscope vsactualcoverage. Minimal integration only if concretegap, otherwise unitsverify existinginterfaces. Remaining: Codex 2sessions/root2children/Claude sameIDs counters+history; no-matchUPS/SessionStart boundary beforeearlyexits; nullresolver neverlegacyfallback; lateepoch/generationwriter cannotcommit afterrootboundary vspositive noreset control; duplicate/sameid/concurrent acknowledgedwrites, storagefail emptyturn/no foreigndeletion; inlinehandoff actualhistory preserve decisions, no rejected delivery; CLI/signal/unexpected exits audit only proposeprodchange ifneeded. PreserveClaude defaults/APIs,statepaths,bootstrap andtrustedmergedconfig. No nativeprobes/workaround/research/newpublicAPIs. E2E Lagrange, domainsLorentz. All executionBooleDocker ONLY; nohosttests/build/install/uninstall/stage/commit. Plan beforechanges/domainupdates samemilestone. Independentreview/QA then fullunit/E2E/static matchingfreeze. Earlycoherentfreeze please. AtPlancompletion retain shared A/B/C artifacts when archiving perprocess, coordinate beforemoves.

M3 assignment, recorded verbatim:

    FINAL M2milestoneGO granted; START M3 now. Own ALL production+unit implementation and PlanD/ATTENTION only; firstmarkM2complete/M3inprogress +plannedinterfaces beforecode. Read M3+completepolicy/inputtables inPlanD and retained M0sidecars/source (NO newfetch/probes). Enable remaining9 events through existingnormalize/policy/translate; preserve allM2PreTool/Claude. No public authorAPI/config/registration/README changes. Required common session/cwd/model; permission_mode exceptcompact, turn_id exceptSessionStart; designatednullable transcript/messages only getapproved empty sentinel, not fabricatedoperationaldata. PermissionRequest has NOtool_use_id; Bashapproval description is INSIDE tool_input, do notblindlyreuse PreToolcommand-onlydecoder. Posttoolresponse arbitraryJSON preserved includingfalse/null/arrays. SessionStart has nativeTurnIdnull but muststill own reset/prune with validsession; actualrootUPS advances allscopes even sameid, childUPS preserves, Stopcontinuationpreserves, startup/clearreset resume/compactpreserve. Reviewambiguityaccepted/no workaround. Adapterresultsets exactlyPlan table incl beforeHookmetagate vs handler; Permission rewrites/updatedPermissions/interrupt includingfalse failsticky; UPSsessionTitle everyarm fails; PostMCPoutput includingnull fails. Generated diagnostics/trace/degraded must event-aware inlinehuman when no contextchannel, notuniversalinjectcontext or unsupportedauthorarm. Stopblock requestscontinuation; runtimefailure terminates via continuefalse, never repeatblockloop. Other knownfailtuples sourceexact incl SubStart/PostCompactstderr2 no veto claims. Add focusednormalization/policy/translation/events/turnpolicy units and everyproductionedge. No E2E/domainownership (Lagrange/Lorentz). Request earlycoherentcompilefreeze; all executionparentDocker runner, neverhosttests. PreserveunrelatedC1/TODO, no commits/staging. Finalreview+QA+fullunits/E2E/static+docs beforeM4.

Accepted history policy prompt, recorded verbatim:

    Then ignore it and move on.

This accepts best-effort ordinary prompt history with the internal Review limitation documented and nonblocking, not disabled history or new workaround scope. M0/M1/M2 are complete; M3 is implemented, reviewed and validated pending final documentation review only. Earlier prompts below are historical.
Execution continuity instruction:

    so... are you doing anything? why do you keep stopping ffs. I'm asking you for updates, not to keep stopping your owork

M0 execution approval:

    Let's go.

Latest user prompt, recorded verbatim as instructed:

    Okay. You can conitinue.

## Revision Notes


2026-09-08: Recorded final matching-source M4 units/E2E/static passes, test-only/no-production deviation, strengthened-oracle retrospective and unexplained prior held-lock timing failure. Proposed selective archive only after final GO and coordinated reference review, preserving shared A/B/C/downstream artifacts. Parent image/cleanup audit remains pending; source/tests unchanged and no archive move authorized.

2026-09-08: Recorded final M3 matching-manifest Docker units/E2E/static passes, images and artifacts, actual private deferRuntimeErrorAudit deviation, resolved test/compiler issues and retrospective. Final documentation review alone remains before M3 milestone GO; M4 has not started and P/L remain absent. Source/tests unchanged by this close-out.

2026-09-08: Final M2 milestone GO received. Recorded M3 scope and planned reuse of private adapter contracts before production edits, including approval decoding, event-specific diagnostics, generated failures and session/turn boundaries. M3 validation remains unrun; M4 is not authorized.

2026-09-08: Final M1 implementation/U/D/static gates passed on matching frozen source manifests; code/QA conditional GO covers fixes. Added docs/plans/feat-0044-cross-agent-portability/codex-runtime-m1-evidence.md with exact commands, final images, artifacts and proof limits; marked completed tasks done and retained final documentation review as the sole M1 closure step. M2 not started, no P/L upgrade or staging. Earlier failed attempts remain in the evidence record.

Historical revision records below describe earlier snapshots, including then-pending tests and gates; current Progress and M1 evidence supersede those statuses.
2026-09-08: Recorded parent-reported focused Docker pass (typecheck/compile, 467 tests, 0 failures, 18 files, 1,526 assertions, exit 0) and retained log path in Progress. Source unchanged by execution and container removed. Limited focused evidence excludes the three QA tests now being added and does not satisfy full M1 acceptance; full unit/E2E runs remain unrun, M2 not started, no P/L upgrade. Documentation-only update.
2026-09-07: M1 working-tree interface log records the invocation policy factory, structured pure diagnostics, ExecutionResult.policyFailure and shared detachment helper. Reviewed engine-owned failure selection and unconditional configured-noop finalization preserve no-config bypass. Matrix names new U/D cases without claiming execution. First focused Docker attempt failed typecheck on three branded HookName test errors; hn fixes are saved and parent rerun is underway. M1 acceptance remains incomplete, M2 not started, P/L absent. Only plan/matrix edited for this update.
2026-09-07: Recorded the user's verbatim history decision and accepted best-effort ordinary prompt history with internal Review documented/nonblocking. Updated current gates, M4, acceptance matrix and status: source-backed M0 complete; M1 authorized/in progress, not complete. P/L remain absent. No production/test edits, executions or commits by this writer.

Historical revision records below retain the earlier pending states; current status and the accepted Decision Log supersede those gates.
2026-09-07: Recorded confirmed final QA GO with no findings and split completed source, bounded probe dispositions and reconciliation from pending code review/history decision/final summary. Preserved the parent's baseline fingerprint and ignored-artifact checks as reported evidence. No force-add, scratch staging, test execution or commit; bundle remains frozen pending code review.
2026-09-07: Resolved QA P2/P3 by source inspection. parse_pre_tool_use accepts rewrite-allow permissionDecisionReason syntactically but retains a reason only for deny. Both plain and rewrite allow therefore use the same human systemMessage annotation disclosing original-recipient loss; neither emits a decision reason. For command B and reason R, literal output is {"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"allow","updatedInput":{"command":"B"}},"systemMessage":"clooks: PreToolUse allow reason (human annotation only; original allow-reason recipient unavailable; native policy retained): R"}, exit 0. Corrected matrix C02/C03 source path to core/src/tools/handlers/unified_exec/exec_command.rs. Bundle refrozen for review; no tests, no P, no further retry and no presumed history decision.
2026-09-07: M0 reconciliation replaces historical current-contract assumptions with exact-release source facts throughout the plan and matrix: no-decision plain allow, rewrite-only native allow, explicit PermissionRequest denial versus reserved-field no-decision, human systemMessage/local stderr, blank-reason auditing, exact Clooks validation versus permissive native parser, producer-nullable compatibility, command-only codecs, and source-derived identity limitations. Parent owns epic/domain/findings and the bounded retry. History choice remains unanswered; review must not infer approval or pass M0/M1. Historical drafting/review entries below remain dated evidence.
2026-09-07, Joe-Degler: Recorded the user's M0 execution approval before probe work. Delegated bounded Docker-only source acquisition and evidence gathering; existing C1 changes, the runtime placeholder and all production gates remain preserved. Author identity was reverified through a read-only GitHub request after sandbox networking failed.

2026-09-07, Joe-Degler: Created the complete ExecPlan skeleton immediately after reading the required process documents, recorded the requested prompt, and retained explicit pending work so drafting cannot be mistaken for implementation or verified upstream behavior.

2026-09-07, Joe-Degler: Consolidated the documentation-only 0.153.4-target refresh, source audit and acceptance matrix into a frozen review candidate. Added per-result/pre-abort policy, opaque validated codecs, unchanged public result methods, explicit unavailable-string compatibility, event-aware failure tuples, provider storage isolation, inline-first recipient-aware handoff, bounded M0 and per-milestone Docker acceptance. Replaced tentative title stripping with sticky rejection. Separated current documentation corrections from future implementation updates and removed transient coordination details. No implementation or executable validation occurred.

2026-09-07, Joe-Degler: Corrected raw PreToolUse skip+injectContext to documented context-only output while preserving the narrower ctx.skip method; specified one abandonment record at abort and no late history/effect commits. Recorded completed coordinating documentation alignment and the final inventory-only documentation-summary requirement.

2026-09-07, Joe-Degler: Recorded confirmed completion of drafting-time domain/findings updates and reported diff/source-fingerprint checks. The candidate remains frozen with independent code/product/QA review pending; no substantive design changes or executable validation accompanied this status update.

2026-09-07, Joe-Degler: Recorded product/code GO for gated M0 and completion of the inventory-only, no-lookup documentation summary. QA remains outstanding, so final approval is not claimed. Status-only update; no design changes or tests.

2026-09-07, Joe-Degler: Applied three final QA clarifications for re-review: preserved current PreToolUse skip-context reduction with exact unit/Docker output tuples; separated M1 injected-policy unit evidence from real Claude compiled regressions and assigned reachable Codex compiled cases to M2/M3; specified first-rejection abort and configured ordering only among pre-abort audited failures, with no late-settlement effects or cross-schedule primary guarantee. Product/code GO for gated M0 remains historical review evidence; QA/code re-review is pending. Only this plan changed; no tests ran.

2026-09-07, Joe-Degler: Final status update records product/code/final QA GO with no blockers and all three QA clarifications resolved. Drafting, domain/findings alignment and the inventory-only summary are complete. Latest reported diff checks are clean and source/test fingerprints unchanged. Approval is to start M0 only; M1 remains gated and no release confidence is claimed. This update changes status only: no implementation, tests or commit.

2026-09-08: Final milestone GO closes Plan D. Selectively archived only this plan and runtime ATTENTION under docs/plans/done/feat-0044-codex-runtime/, retaining the active ATTENTION pointer and all shared A/B/C/sidecar artifacts. Shared references are explicit repository-relative paths. No source/test/staging changes or P/L claims.

2026-09-08: Bounded post-completion cleanup removed isolated timing incidents from active findings and repeated living status text, preserving original M2/M4 evidence and archived observations. Readiness now lists remaining native conformance, recipient readability and Claude quoting gaps; tooling friction retains one actionable subprocess-diagnostics gap grounded in `test/e2e/helpers/sandbox.ts`. No runtime bug is declared fixed. Only documentation changed; no tests, staging or commit were performed.
