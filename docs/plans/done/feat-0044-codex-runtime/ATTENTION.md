# Attention Required

Archived runtime record: Plan D is complete with final M4 code/QA milestone GO. Final units 2,192/0, E2E 787/0 and static checks passed; image/cleanup audit is complete. Earlier pending milestone/archive statements below are historical. Shared A/B/C plans and sidecars remain in docs/plans/feat-0044-cross-agent-portability/; its ATTENTION.md is a brief pointer here. No P/L or native enforcement claim.

EPIC-0044 Plan B introduced an agent adapter boundary around the Clooks hook engine while preserving Claude Code behavior. All Plan C1 registration, deletion, active-home and recoverable-state corrections are complete and independently reviewed. The completed C1 plan and focused review snapshot are archived under `docs/plans/done/feat-0044-cross-agent-portability/`; this shared file remains active for downstream runtime work.

## Registration Corrections

### Registration Ownership and Writes -- `src/agents/codex/settings.ts`, `src/settings.ts`, `src/registration-file.ts`

**What changed:** Recognize Clooks-generated commands precisely and reject malformed registration structures without overwriting them. Publish valid edits atomically, with replacement temporaries no more permissive than the destination before writing bytes. Shared strict reading also covers uninstall inspection counters; deletion control flow is unchanged.
**Why:** Substring ownership matching can remove unrelated commands; shape coercion and direct writes can lose user configuration.
**Review focus:** Preserve unrelated hooks and metadata, recognize existing generated commands including quoted paths, and leave original bytes intact on validation or write failure. This does not introduce a new permission model.

**Validation:** Independent code review and QA approved; 266 focused unit tests and 477 compiled-binary Docker E2E tests pass. Typecheck, formatting, lint and ShellCheck pass, with only existing lint warnings in untouched files. Native Codex execution and Claude raw-global-path quoting are not established by these checks.

### Active Codex Home and Shared Entrypoint -- init, uninstall, and launcher generation (complete)

**What changed:** Resolve the active Codex configuration home consistently, retain its cleanup identity before registration, publish suppression receipts after successful selected-Codex/all setup, and reject stale, mismatched or unusable state in new project launchers.
**Why:** Hard-coded home paths and prematurely written markers can point registration at the wrong configuration or suppress execution after failed setup.
**Retry ordering:** Selected Codex/all init validates identity without writes, persists recovery identity, and retires any old receipt before shared launcher repair. Only successful registration publishes a replacement. Test failure after launcher repair with normal checksum PATH restored. Claude-only init remains independent and can restore an existing matching Codex receipt through shared launcher repair; this bounded exception must be documented and tested.

**Validation:** Independent code and QA GO; final full Docker units passed 1,957 tests and full E2E passed 600 tests. Typecheck, formatting, lint and ShellCheck passed with 112 existing warnings. The receipt/header, physical-parent path, FIFO/file-type, checksum metadata, recovery, pathful PTY and real Claude merge regressions are covered. The CLI checksum subprocess is bounded to ten seconds. Five domain docs are current; no native Codex runtime verification is implied.
**Review focus:** Preserve Claude defaults and the merged global/project pipeline; test marker migration and failure recovery. The recovery record tracks one physical configuration home, while a separate suppression receipt uses POSIX `cksum` only for stale-file detection. Failed publication or remaining unknown-event references must retain the home identity and prevent a switch. Review absolute-path constraints, symlink aliases, missing utility fallback, legacy project scripts, and cleanup of both selected and recorded homes before shared deletion. Installation state cannot establish that native Codex hooks actually execute. Do not add trust authorization, runtime translation, or a universal exactly-once claim.

### Shared Runtime Deletion -- `src/commands/uninstall.ts` (complete)

**What changed:** All-agent cleanup follows the final deletion decision, including interactive decisions. Additional consent precedes writes, strict preflight rejects ambiguous or unsupported references, and a final inspection precedes shared-directory deletion. Each global flag clears after its corresponding successful unregister. Failures retain runtime for retry; completed files or prior scopes are not rolled back.
**Why:** A retained registration must not point at the runtime being deleted.
**Review focus:** Explicit deletion confirmation, cancellation without premature writes, selected-agent-only unhook behavior, and cleanup failure preventing shared-directory deletion. Cover both project and global scopes and document the boundary of active-home cleanup.

**Execution boundary:** All uninstall tests, including mocked units and real PTY smoke, run inside disposable Docker containers. No host uninstall command, global binary replacement, or real home/configuration mutation is permitted. Active Codex-home tracking subsequently completed in the home/receipt milestone above.

**Validation:** Independent code and QA approved; 231 focused unit tests, 44 uninstall journey tests and 510 full E2E tests passed in Docker. Typecheck, formatting, lint and ShellCheck passed. CLI and testing documentation describe per-scope cancellation and partial-failure limits.

Each implementation milestone requires unit tests, compiled-binary Docker E2E or smoke coverage through `bun run test:e2e`, independent code review, QA review, and same-milestone domain updates. Do not overwrite the working user's global binary or real configuration while validating.

## Danger Zone Changes

### Cross-Event Integration Verification -- run, turn storage, failure state, handoff and CLI

**Final audit and closure status:** Final code/execution QA GO; no owned M4 containers remain. Separate source and wrapper manifests match before/after: source 11f1bc064814471e5c2edc41996eeae742d430ca06472c22717ae222f87e47eb; wrappers 6ed9aa7f7fe33319d8fa656fcb2580d40e064a5f788425d2cc6a15fdfcaa6990. Unit image sha256:3fe2f24dd9d7a515277d3b3f50cfe42ca935f97c2b7b610ad4ee5ed2f3de33f5; E2E/static image sha256:93365cd1bc4b9222702b2bec14a250a83d05ea098132c8f82f5ffde99c318b53. Only final documentation/milestone closure remains; earlier pending image/cleanup descriptions below are superseded by this final audit. Source/tests and owned docs are frozen; no archive moves yet.

**Implemented and validated, test-only:** M3 has final milestone GO. M4 verifies existing private interfaces without production or API changes. Added unit coverage for Codex no-match boundaries/pruning, explicit-null resolver behavior with both-provider sentinels, two sessions/root/two children/provider isolation, generation/epoch stale writers, concurrent commits and inline handoff with actual history. CLI signals/unexpected exits/bootstrap audit found no demonstrated production defect requiring a change. E2E coverage is owned by Lagrange.
**Review focus:** Preserve Claude paths/defaults and configured error accounting; never use legacy main scope when an adapter explicitly disables tracking. Invalid configured identities still refuse before effects, while valid but unavailable storage yields best-effort empty history. No late/foreign writes or deletion, no new public APIs, native probes, trust framework or Review workaround. Parent Docker runner owns all execution. Final documentation/milestone GO and parent image/cleanup audit remain pending; source/tests are frozen.

**Final M4 evidence:** Units 2,192/0 (69 files, 8,229 assertions, 3.33 seconds), E2E 787/0 (47 files, 9,268 assertions, 104.18 seconds), static checks exit 0 with 112 lint warnings/0 errors. Logs: tmp/codex-runtime-m4/20260908T093707Z-units-g43QGI/output.log, tmp/codex-runtime-m4/20260908T093440Z-e2e-4jueMy/output.log, tmp/codex-runtime-m4/20260908T093645Z-static-JThIEb/output.log. Parent verified matching source-manifest 11f1bc064814471e5c2edc41996eeae742d430ca06472c22717ae222f87e47eb; source/wrappers unchanged. Images/cleanup remain under parent audit. No P/L evidence.

**Unexplained held-lock timing result:** Initial full units were 2,191/1: existing turn-state.io.test.ts:404 measured 38.53868899999998 ms against unchanged >50 ms. Date.now() controls the deadline while performance.now() measures the test, but no clock jump or other cause was proved and no M4 clock/timer mock leak was found. One QA-approved unchanged-source full-unit confirmation passed 2,192/0. Keep the original artifact tmp/codex-runtime-m4/20260908T093414Z-units-CpazQA/output.log; no assertion weakening, production fix or flake claim.

**Archive proposal only:** After final GO, coordinate selective Plan D archival and reference updates. Never move the entire shared active folder: preserve A/B/C artifacts, shared ATTENTION and downstream source/evidence consumers. No archive moves are authorized during this close-out.

### Completed Milestone History

M1-M3 records below retain their validation and review context; their then-pending closure statements are historical. M4 current status and final results are above.

### Remaining Event Runtime -- Codex normalization, result policy, output and turn boundaries

**Implemented, reviewed and validated:** M2 has final milestone GO. M3 implements the other nine events using private adapter contracts, with no public API/configuration/registration changes. Final code review has no findings. Final matching-snapshot units passed 2,168/0 (69 files, 8,035 assertions), E2E 774/0 (46 files, 8,781 assertions, 98.17 seconds), and typecheck/lint/format exited 0 with 112 lint warnings/0 errors. Final documentation review alone remains before M3 milestone GO. Source/tests are frozen and M4 has not started.

**Final execution evidence:** Parent-run disposable Docker, Bun 1.3.10; source before/after manifest 8224ddbd16af5dc77eb6e0cc29db315026378c2d0327c1166fbf5093c88e70ad matches across all gates and wrappers are unchanged. No owned containers remain. Logs: tmp/codex-runtime-m3/20260908T085912Z-units-xyOGuo/output.log, tmp/codex-runtime-m3/20260908T085938Z-e2e-TyxHcI/output.log, tmp/codex-runtime-m3/20260908T090136Z-static-edmPtE/output.log. Exact image IDs are recorded in Plan D. No host execution, P/L or native enforcement evidence is implied.
**Review focus:** Validate each event before imports or state effects. PermissionRequest has no tool-use ID and preserves nested approval description; PostToolUse preserves arbitrary JSON responses. Only designated nullable strings receive empty sentinels. Separate lifecycle meta-gates and generated failures from handler result sets, reject unsupported fields before effects, and distinguish Stop continuation blocks from terminating runtime failures. Route generated diagnostics inline to humans when context delivery is unavailable.
**State boundaries:** SessionStart has no native turn ID but must reset/prune on startup/clear with valid session identity; resume/compact preserve. Root UserPromptSubmit advances all scopes even for same-ID steering; child prompts and stop continuation preserve. Keep existing provider isolation, epoch checks and inline handoff; accepted Review ambiguity gets no workaround. Claude and M2 PreToolUse semantics must remain unchanged.

**Ordinary-error correction and actual interface deviation:** Early generated-block rejection bypassed configured continue/trace and failure thresholds. Optional private policy deferRuntimeErrorAudit now defers only ordinary crash auditing until the engine selects a blocking outcome after accounting. This refines the first snapshot's expectation of no new private signatures. Parallel captured-outcome accounting finishes before this rejection closes effects; genuinely late outcomes remain ignored. Omitted Claude/injected defaults, immediate sticky author-capability checks and parallel-contract auditing stay unchanged. Real executor units distinguish runtime Stop termination from author continuation, including continuation modes, thresholds, recovery and preserved pre-abort failure counts. Load errors retain their separate threshold-first path. Final matching-snapshot gates passed these regressions. The optional tenth executeHooks argument is retained; no public author/configuration interface or eleventh argument was added.

**Resolved validation history:** Readonly test tables caused the first compiler-only failure; mutable copies retain event literal types. Stale M2 event-gate tests now have valid SessionStart and malformed controls with plugin/no-sniff assertions preserved. The prior full-unit 2,167/1 failure was a mocked ExitCalled(0) caught by runEngine, not a production exit defect; the test selects/asserts Codex and uses the core exit trap. Final units pass that correction. The separately recorded unexplained M2 timeout remains unexplained.

### Codex PreToolUse Runtime -- normalization, policy, run, failure and turn storage, handoff

**Completed M2 milestone:** M2 enables Codex PreToolUse with private normalization, validated command/MCP replacement codecs, per-result capability refusal, copied discovery environment, provider-isolated state and inline-only handoff. M2 has final milestone GO. Its then-unsupported nine events are now implemented by M3 above. No public API, registration, README or Review-history workaround is included.
**Review focus and resolved corrections:** Configured Codex input is validated before counter read/write/recovery clearing or hook imports. Config-error placement uses project clooks.yml presence excluding projectRoot === homeRoot, consistently on failure and recovery; Claude retains legacy ordering and paths. Descriptor-first preflight rejects accessors and lossy arrays before cloning; known typed optional inputs and command patch key presence are validated. MCP full-record transport preserves opaque keys and detached candidates. Requested file delivery stays inline with one diagnostic; parallel capture and late completion retain the raw-history/effect barrier.
**Actual private interfaces:** Optional inputStage and resolveTurnPolicy are adapter-owned, with Claude defaults when omitted. InvocationResultPolicy adds optional validation-only validateRawResult and handoff eligibility. FailureLocation carries a Codex path plus actual selected root in executeHooks' existing fifth argument; the optional tenth policy argument remains, with no eleventh argument. Provider metadata reaches turn paths, directory creation, snapshots, commits, boundaries and pruning. InvocationPolicyError and EngineCompletion keep recognized failure translation and exit ownership in the engine. Public author types/methods, configuration, selectors and schemas are unchanged by M2.
**Completed M2 validation:** Parent-run disposable Docker full units passed 2,047/0; final full E2E passed 674/0, 45 files, 6,714 assertions, exit 0 in tmp/codex-runtime-m2/20260908T081921Z-e2e-5bJ5J5/output.log. Typecheck/lint/format all exited 0; lint 112 warnings/0 errors. Parent confirms matching frozen source across those gates (supplied fingerprint prefix e8a92...). Final code/QA/documentation milestone GO was granted before M3. These are Clooks U/D checks, not upstream executor or live-native evidence.

**Storage isolation correction completed:** Code review found provider paths could alias Claude state through symlinked managed directories or files. Codex failure I/O now carries the actual root alongside its path, validates components and final file, reads no-follow and publishes atomically. Codex turn snapshots validate managed parents before the bounded read. Legacy string failure I/O and Claude snapshot defaults remain. Regular-file sentinels behind linked parents and final-file links cover read/write/clear isolation; real-policy parallel capture has sparse/extra-property regression coverage. This uses existing filesystem patterns, not a new trust policy or a race-proof filesystem claim.

**Incidental nonregular-file correction completed:** The shared bounded turn reader now adds O_NONBLOCK to O_NOFOLLOW so opening a FIFO cannot hang before fstat. Regular-file semantics remain unchanged for both providers. Deadline-killed Docker subprocess tests include regular-file controls and unchanged FIFO identity. Prune tests age foreign sentinels past TTL and prove owned-file deletion; parallel cleanup is also bounded.

**Unexplained prior full-suite timeout retained:** The initial full E2E run failed an existing real-GitHub smoke (673/1); its focused rerun passed 11/0. A subsequent full run failed circuit-breaker-advanced.e2e.test.ts:257 (673/1), reporting mapped exit 2 after 15.7 seconds and Bun's 5-second test timeout, with no original child stderr/raw signal captured: tmp/codex-runtime-m2/20260908T081120Z-e2e-CpEi3W/output.log. The focused file passed 9/0 and final full E2E passed unchanged source. Read-only comparison found no M2 change to M1's parallel capture/abort/accounting code, but the cause remains unestablished. Do not label it a flake or proven infrastructure issue; preserve the finding and any separately recorded reproduction evidence. No production fix was inferred from this failure.

### M1 Private Invocation and Result Auditing -- adapters, execute, lifecycle, run

**What changed:** M1 carries invocation metadata separately from public contexts and checks each completed lifecycle result before handoff, input updates, votes or parallel short circuit. Explicit before-hook/handler origins preserve early skip identity; afterHook remains an observer whose mutations are audited. Accepted JSON is detached, policy failure is latched outside reduction, and late settlements cannot mutate the closed batch or append history.
**Why:** A final translator cannot recover capabilities discarded by reduction or undo an earlier result effect. Every started lifecycle needs one raw outcome, including an abandoned error at abort.
**Review focus:** Preserve Claude normalization, patch/null rules, vote ordering, observer warnings, output bytes and storage paths. Verify raw history exactly once, no post-rejection handoff/input effects, invocation isolation and adapter-selected final exits. Discovery environment and failure translation interfaces must not change Claude root selection or CLI/shell fallbacks. Provider storage/pruning and recipient-specific handoff routing remain later milestones; M1 must neither touch another provider's state nor enable Codex. The user accepted the internal Review limitation with "Then ignore it and move on." M0 source audit/reviews are complete, P/L remain absent, and no probe retries are authorized.
**Interface refinements:** Diagnostic composition returns structured result/stderr/systemMessages, leaving emission in the engine. The engine itself selects failure translation from the latched failure. Configured empty outcomes always reach normal translation; no-config bypass remains. Claude retains mutable context input for compatibility, while injected policies receive detached tool-input views. A narrowly scoped result-policy helper shares detached audit and exception handling between the two runners.
**Completed M1 validation:** Parent-run Docker gates passed 2,000 units/0 failures, 7,207 assertions across 64 files, and 603 E2E/0 failures, 5,369 assertions across 44 files. Typecheck/build, lint and format passed with 112 lint warnings/0 errors. Evidence remains in tmp/codex-runtime-m1/20260908T065006Z-units-EYbymd/output.log, tmp/codex-runtime-m1/20260908T065029Z-e2e-gtCAyf/output.log and tmp/codex-runtime-m1/20260908T065232Z-static-k02BdQ/output.log. M1 received final code/QA/documentation milestone GO before M2 was assigned. Codex was disabled in M1; the separately implemented M2 path is described above. No staging, commits, host tests or installation changes occurred.

**Validation history:** The first focused typecheck found three branded-name expectation errors before tests ran. After correction, focused Docker validation passed 467 tests. The first full-unit pass then reported 1,997 passes and one failure in existing parallel failure accounting (`tmp/codex-runtime-m1/20260908T064014Z-units-rQPz5R/output.log`); E2E/static checks did not run in that failed sequence. The correction below preserved the old assertion and added positive/late-settlement distinctions. The final verified 14-file formatting export (`tmp/codex-runtime-m1/20260908T064833Z-format-export-czD70T/export/formatted-owned-files.tar`) was applied before the successful final gates. Throwing-result-getter, handler-counter, timeout/late-settlement and overlapping-invocation regressions are included in the final unit result.

**Parallel accounting correction:** Both synchronous crash outcomes had already been captured by promise callbacks, but the first final callback closed the batch before the second final callback could store its outcome. The correction captures/audits outcomes before ordinary crash/block abort consumption, retains only the pre-closure snapshot, and still closes immediately on policy rejection. Post-closure captures remain ignored; there is no timer grace period or reopened result array. The existing both-failures assertion is unchanged. New barrier cases distinguish available ordinary failures from a sibling rejection released after abort and pin counters, raw history and terminal output. Final full-unit validation passed both the retained assertion and new regressions. This validates the Clooks boundary, not native Codex execution.

### Remaining Codex Runtime Policy -- shared engine, adapter, failure and turn boundaries

**Selected-source corrections:** The pinned source rejects bare PreToolUse allow without a rewrite; implemented plain allow preserves native policy. Reserved PermissionRequest fields do not guarantee denial, so known-event failure uses the explicit deny channel. Successful stderr is not native-delivered diagnostic text. Stop continuations preserve native turn identity, while internal Review prompts lack an origin discriminator; full once-per-human-turn parity remains unsupported. Source-backed mappings are not live verification.

**What remains:** M1 installed the private policy boundary, M2 implemented the complete PreToolUse path and prerequisites, and M3 implemented and validated the other nine author-event paths. Final M3 documentation review remains. M4 has not started; its cross-event acceptance work requires final M3 milestone GO. The accepted Review/history limitation stays documented and nonblocking without a workaround.
**Why:** Final translation cannot catch a capability that already changed later input, triggered parallel cancellation or wrote a handoff before being discarded by reduction.
**Review focus:** Preserve Claude defaults and existing storage behavior. Resolve public-field compatibility without fabricated tool shapes or transcript content. Keep the retained exact-release source contract, trusted-repository merged execution, the agreed ten events and Docker-only validation. No new upstream probes or README release-support claims are authorized.

### Shared Runtime Abstraction -- `src/engine/run.ts`, `src/engine/types.ts`, `src/agents/*`

**What changed:** Plan B split the current Claude-shaped `runEngine()` path into a selector wrapper and `runEngineCore(adapter, deps)`, with agent-specific adapters under `src/agents/*`.
**Why it changed:** Codex support needs a clean translation boundary without scattering Codex conditionals through the existing Claude runtime.
**Why it matters:** `runEngine()` is on every hook invocation. A small mistake can break config loading, hook execution, circuit breaker behavior, or all engine-mode output.

### Fail-Closed Error Handling -- engine-mode exits and stderr

**What changed:** Plan B routes adapter selection and unsupported-agent failures through engine-mode diagnostics.
**Why it changed:** Unknown `CLOOKS_AGENT` values and unimplemented Codex support must fail clearly instead of silently falling back to Claude or allowing actions.
**Why it matters:** Exit 2 is not a universal upstream refusal; Stop can interpret it as continuation. M2 uses explicit event-aware JSON where source establishes a refusal or termination channel, and local failure for unidentified events or events lacking the chosen native veto. A emitted request is not proof of native enforcement.

### Agent Wire Translation -- Claude output translator ownership

**What changed:** Plan B moved Claude Code output translation ownership behind the Claude adapter while keeping the existing translator behavior.
**Why it changed:** The shared engine core must not pretend Claude's wire JSON is agent-neutral.
**Why it matters:** Claude Code relies on exact output shapes such as `hookSpecificOutput.permissionDecision`, `decision: "block"`, and continuation stderr. Regressions here are user-visible and safety-sensitive.

### Event Recognition and Early Routing -- `hook_event_name`, `NOTIFY_ONLY_EVENTS`, and `ConfigChange`

**What changed:** Plan B made adapters own event-name validation and Claude-specific early/late routing behavior, including notify-only stderr routing and `ConfigChange` `policy_settings` downgrade semantics.
**Why it changed:** Codex has a different valid event set and different wire-output rules, while the current engine validates against Claude's event set and contains Claude-only behavior in the shared path.
**Why it matters:** Leaving these checks in the shared core would keep the runtime Claude-coupled. Moving them incorrectly can change when warnings appear, whether stdout is dropped, or whether policy-setting blocks are downgraded.

### Claude Plugin/Settings Advisories -- Claude-only behavior

**What changed:** Plan B keeps Claude plugin vendoring and stale-plugin / enable-without-install advisories on Claude adapter-owned helper paths only.
**Why it changed:** FEAT-0044 defines plugin-delivered hooks as Claude-Code-only, and Codex has a separate plugin/trust model.
**Why it matters:** Leaking `.claude/` settings reads or plugin advice into Codex runs would couple providers incorrectly. The implemented Codex adapter keeps preparation/advisory methods free of Claude plugin discovery across all ten events. Valid SessionStart reaches its handler without plugin discovery or vendoring; malformed configured input refuses before imports.

## Residual Review Notes

- All ten Codex events are implemented and M3 matching-snapshot validation passed; final documentation review remains. M4 has not started, and native enforcement/activation/readership remain unverified.
- Do not add Codex registration or README support claims from Plan B alone. Plan C owns registration; Plan F owns user-facing support docs.
- Plan and epic files under this directory may be ignored by git. If committing Plan B closure docs, use `git add -f` for the plan/epic files that need it, but do not force-add unrelated scratch files.
