# Codex Runtime Acceptance Matrix

Status: M0 source-backed complete; M1/M2 final GO. M3 implementation and final local validation complete: units 2,168/0, E2E 774/0, static all exit 0 on matching frozen manifests. M3 is complete with final code-review milestone GO. Final QA GO is confirmed; M4 test implementation and local validation are complete (U 2,192/0, D 787/0, static all exit 0); final code/execution QA GO and runner audit are confirmed; M4 is complete with documentation closure authorized. Historical failures remain recorded, including M2's unexplained circuit-breaker exit. P/L remain absent. Artifacts: [M1 evidence](codex-runtime-m1-evidence.md), [M2 evidence](codex-runtime-m2-evidence.md), [M3 evidence](codex-runtime-m3-evidence.md).

Related plan: [Plan D](../done/feat-0044-codex-runtime/PLAN-FEAT-0044D-codex-runtime-capability-policy.md). Context: [epic](../../epics/EPIC-0044-cross-agent-hook-portability.md), [continuation review](continuation-review.md), [historical capability matrix](codex-capability-matrix.md), and [PLANS.md](../PLANS.md).

The fixed event set is SessionStart, SubagentStart, PreToolUse, PermissionRequest, PostToolUse, PreCompact, PostCompact, UserPromptSubmit, SubagentStop, and Stop. Trusted-repository assumptions, merged home/project/local execution, and the existing public decision methods remain in force. No permission-model redesign, additional events, or universal exactly-once guarantee is proposed.

## Evidence Rules

Each case below has a stable ID. A later implementation must associate that ID with a concrete test file and test title, input fixture, expected output, and evidence artifact. A table row containing several alternatives requires separately identifiable parameter cases, not one test that samples one alternative. Missing evidence remains pending even when the desired behavior is specified.

| Evidence class | What it can establish | Required provenance | Current status |
| --- | --- | --- | --- |
| Local static inspection | Present source contracts and assertions already written | Repository-relative file, function/test title, and revision or reviewed working-tree snapshot | Inspected for the files cited below; no execution claim |
| U: unit | Decode/encode rules, per-result policy, ordering, controlled faults, state transitions | Test ID/title, exact command, source revision, container/tool versions, result artifact | Final formatted M2 full units passed 2,047/0, 7,495 assertions, 68 files, exit 0; M1 and earlier attempts retained separately |
| D: compiled Docker E2E | Generated launcher or engine stdin through the compiled Clooks binary, real config loading and real TypeScript hooks, emitted wire output and local effects | Test ID/title, build revision, Docker image/tool versions, isolated environment, stdout/stderr/exit and markers/files | Final full E2E passed 674/0, 45 files, 6,714 assertions, exit 0. Earlier 673/1 attempts, focused retries and scratch non-reproduction are retained separately; original circuit-breaker failure root cause unknown |
| S: pinned upstream source inspection | Selected release's parsing and consumer behavior, including callers and recipients | Release/version plus immutable commit, file/line or symbol, and the traced consumer | Complete for audited ten-event producer/parser/consumer, codec and identity paths: tag 042fb41b7c813ac7999105e886b2b7aa715b5081 -> commit 3d2ee51ca2d5db578f328aa75e20aa22c0197c9a; 4,177 manifest files match; see source ledger below |
| P: pinned upstream executor/harness | Actual selected upstream parser/executor behavior for supplied cases | Exact source commit, harness/test entrypoint, invocation, input/output/exit and consumer assertions | Probe 1 blocked before cargo: missing cargo/rustc, zero tests. Retry exhausted: cargo fetch --locked refused an upstream Cargo.lock update during dependency preparation. Zero tests/no P; no lock modification or third attempt |
| L: live native session | Native activation, actual tool effects, permission outcomes, recipient delivery and continuation sequence | Resolved binary/version/digest, config and command, isolated environment, event capture, outcomes and sanitized artifacts | Pending; no live success claimed |

M1 final evidence: units 2,000 pass/0 fail, 64 files, 7,207 assertions; compiled E2E 603 pass/0 fail, 44 files, 5,369 assertions; Docker typecheck/lint/format:check all exit 0, lint 112 warnings/0 errors. All six before/after manifests match 5cbfed02f56ad973d59e9338de7ba4116dd1ace81a687526dd8ee3a7c648cb23 (manifest-file fingerprint, not Git commit). Source was unchanged across those runs and their owned containers were removed. [M1 evidence](codex-runtime-m1-evidence.md) preserves exact commands, versions/images, logs, first focused typecheck failure and failed full-unit attempt. Final code/QA/docs GO confirmed. M2 requires separate acceptance; no P/L upgrade.

### M1 Named Acceptance Cases

The U cases below are included in the final 2,000/0 source-unit run; D cases in the final 603/0 compiled E2E run. Parameterized titles retain source placeholders. U injected-policy proof does not establish Codex compiled behavior; D uses real Claude hooks and default/explicit selection without a test-only selector. This passes M1's allocated acceptance, not every future Codex E/F/C cell.

| Class / IDs | File and named cases | Intended oracle |
| --- | --- | --- |
| U C01 | src/agents/claude-code/adapter.test.ts: "invocation envelopes retain separate raw data without exposing metadata in context" | Private raw envelope and public normalized data remain separate. |
| U C14 / F06 | Same adapter file: "diagnostics are composed separately with unchanged Claude ordering and no input mutation"; "a latched failure dominates an internally composed allow result" | Structured composition preserves ordering/input; failure remains dominant. |
| U C03/C06 | src/engine/execute.policy.test.ts: "omitted policy equals explicit Claude policy for patch/null merging and configured votes"; "validated nextToolInput is detached and never merged again as a patch" | Legacy merging/votes and accepted full-candidate detachment. |
| U C06/C07 / F05 | src/engine/execute.policy.test.ts: "ordinary crash counters: sibling before abort"; "ordinary crash counters: sibling after abort"; preserved src/engine.test.ts "circuit breaker updated after all settle" | Already-settled ordinary crashes retain counters; late settlements cannot change terminal effects/history. Included in final 2,000/0 U. |
| U C06 / F06 | src/engine/execute.policy.test.ts: "a throwing result getter resolves as a policy failure without orphaning the parallel batch" | Bounded completion, typed refusal, one history commit, no handoff/unhandled rejection/late effects. Included in final U. |
| U C05/C06 / F05 | Same execute file: "lifecycle timeout ignores a later fulfilled result, parallel=${parallel}" | Serial/parallel late results after timeout cannot commit effects or history. Included in final U. |
| U C01/C06/C14 | Same execute file: "overlapping invocations keep policies, candidates, diagnostics and raw histories separate" | Concurrent invocation state remains isolated. Included in final U. |
| U C04/C10 / F06 | Same execute file: "${event} rejects present ${field} before handoff or later hook"; "positive accepted handoff writes a file, the same rejected payload writes none" | Audit before effects for PreToolUse control, PermissionRequest updates/interrupt, prompt title and post-tool mutation, with a positive handoff control. |
| U C05/C06 / F06 | Same execute file: "parallel ${resultTag} with empty updatedInput is audited as a contract violation before handoff"; "beforeHook ${tag} has its own origin and no handler or observer"; "afterHook mutation is audited while its attempted override remains ignored" | Empty-patch presence, lifecycle origins and observer mutation precede effects. |
| U C05/C06 / F05/F06 | Same execute file: "malformed handler result ${JSON.stringify(value)} is audited and records raw error"; "${first} rejects first; late ${lateThrows ? 'rejection' : 'fulfillment'} cannot change effects or history"; "policy throw bypasses ${mode}, parallel=${parallel}, and maxFailures degradation"; "generated engine errors and degraded load errors retain typed audit origins" | Raw history, first rejection, ignored late settlements and non-degradable policy failure. |
| U F07 | src/engine/run.agent-adapter.test.ts: "configured ${path} invokes final translation with no messages and retains its exit" (no-hooks/no-matches/no-result); "no config preserves bypass without normalizing or translating malformed input" | Configured finalization is unconditional; no-config bypass is preserved. |
| U C14 / F06/F07 | Same run file: "latched rejection selects failure translation even when diagnostics and adjustment return allow" | Engine chooses failure path regardless of adapter-generated allow; normal translation is not called. |
| D C01/C14 | test/e2e/agent-adapter.e2e.test.ts: "handler and lifecycle inputs retain normalized public data without the private envelope" | Exact public key sets and normalized nested fields in before/handler/after, no envelope/provider/codec leakage; fresh JSON each invocation and exact Claude output. |
| D C05/C14 | Same E2E file: "afterHook mutation is retained while its returned replacement remains ignored" | Positive lifecycle markers, retained handler mutation, ignored override and exact warning/output under both Claude selectors. |
| D C03/C06 | Same E2E file: "a later hook cannot mutate an accepted nested rewrite through the author-held result" | Deterministic later mutation visibly changes author data but not accepted input/output; untouched zero/empty keys, nested null/false and original input preserved. Fresh observation JSON and ordered markers. |
| D C14 / F01/F07 | Same E2E file: existing default/explicit Claude and unknown-selector tests; strengthened "CLOOKS_AGENT=codex fails closed with not implemented diagnostic" | Real positive Claude import/handler control, no additional import/handler under disabled Codex, unchanged selection/output semantics. |

The reviewed M1 implementation introduces createResultPolicy(invocation), pure ComposeDiagnosticsInput -> ComposedDiagnostics with result/stderr/systemMessages, ExecutionResult.policyFailure and shared checkDetachedResult in src/engine/result-policy.ts. Engine-owned failure selection and configured-noop finalization are covered above. Existing PermissionDenied rename, ConfigChange downgrade, notify-only/plugin and early-noop regressions remain part of M1's baseline; these additions do not replace or duplicate the full baseline.

The C1 baseline of 1,957 unit tests and 600 E2E tests is historical registration/launcher evidence only, as recorded in `docs/findings/codex-portability-readiness.md`. It satisfies none of the new U/D/S/P/L cells automatically. The historical authentication failure captured no hook payloads. Synthetic fixtures remain synthetic until independently revalidated; do not relabel them as captures.

Before freezing executable wire assertions, record S evidence for the selected release for every event, tool codec, result arm, failure channel, audience and identity assumption. Record unsupported and unresolved separately. Exact stdout JSON, forbidden keys, stderr routing and exit code must then be literal expected values, not predicates copied from the translator under test. P and L remain separate pending gates; a release-confidence decision must identify precisely which live claims remain unverified.

### M0: Contract and Semantic Evidence Gate

Release-bound source audit is complete in [wire contract](codex-runtime-m0-wire-contract.md) and [identity/delivery](codex-runtime-m0-identity-delivery.md), with [acquisition/probe provenance](codex-runtime-m0-evidence.md). Source exit 2 plus nonblank stderr denies PreToolUse/PermissionRequest, rejects the inspected prompt, gives PostToolUse feedback, and continues Stop/SubagentStop; blank stderr yields Failed/no control. Start/compact exit 2 fails without control. SubagentStart loses stderr detail; compact retains it. Successful stderr is discarded for all ten. Clooks keeps explicit JSON refusal and native human systemMessage on successful JSON paths; local debug stderr is not delivered.

The selected source is verified Codex 0.153.4 at the tag/commit above. Generated schemas differ from the actual Serde parser: another recognized event discriminator passes, Option nulls become absent, and schema const/null declarations are not executed validation. Clooks enforces its own exact event/author-field contract. Required nullable producer fields include transcript_path on every event and last_assistant_message/agent_transcript_path where applicable; accepting absence is broader Clooks compatibility. Compact custom_instructions/compact_summary are absent, not captured empty values. These Codex-specific U/D contracts and P/L remain unverified; completed M1 U/D covers the separate allocation above.

Use the public empty-string unavailable sentinel for mapped string fields whose native value is missing or null, including unavailable transcript and compaction context strings. This preserves string-valued public fields without inventing content. Test absent, null, empty and nonempty string separately: absent/null map to `''`, empty remains empty, and a valid nonempty string is preserved. A number, boolean, array or object is a type error, not an unavailable string. Apply this policy only to explicitly identified mapped string fields; it does not waive required identity/tool-field validation. Document that hooks cannot distinguish genuine empty text from unavailable text. Never invent a file path or recover missing identity by parsing transcripts. Private metadata cannot make a fabricated public value correct.

The user's exact prompt "Then ignore it and move on." accepts best-effort ordinary root/ThreadSpawn prompt history with the internal Review limitation documented and nonblocking. History stays enabled; no workaround scope is added. Reset all scopes on root UserPromptSubmit, including same-ID steering; preserve on child prompts and same-ID/no-prompt Stop/SubagentStop continuation. Synthetic Review without agent_id can be treated as root input and reset history; full genuine-user parity and delivery deduplication are not promised. Do not invent IDs or use prompt text/transcripts to distinguish Review. This accepted source-derived policy satisfies M0 production entry and authorized M1; its implementation and U/D/static validation are now complete pending final documentation review. No executed capture or P/L evidence is implied.

## Event Matrix

### Source Evidence Ledger

All paths below are relative to `codex-rs/` at verified commit `3d2ee51ca2d5db578f328aa75e20aa22c0197c9a`; immutable file/line citations are in the two M0 sidecars. S means inspected source semantics, never executed acceptance. M1-M4 local U/D allocations are complete and recorded separately; final M4 code/execution QA GO is confirmed; M4 is complete. All N experiments remain P/L pending.

| IDs | Completed S evidence and remaining limits |
| --- | --- |
| E01/E02, F03-F08, C10/C14, N01/N05/N07 | `hooks/src/events/session_start.rs::parse_completed` and `core/src/hook_runtime.rs::run_pending_session_start_hooks`: root/child context and universal warnings; SubagentStart cannot veto. Exit-2 local failure loses detailed SubagentStart stderr; native generic failure only. File readership and activation unknown. |
| E03, F03-F08, C02-C06, N02/N03 | `hooks/src/engine/output_parser.rs::parse_pre_tool_use` and `unsupported_pre_tool_use_hook_specific_output`; `hooks/src/events/pre_tool_use.rs`: no-decision/context accepted, bare allow invalid, rewrite allow requires non-null full updatedInput, deny requires nonblank reason. parse_pre_tool_use retains permissionDecisionReason only for deny; accepted rewrite-allow reasons are discarded and Clooks must route them to human systemMessage. Native concurrent rewrite winner is last completed unless deny wins; Clooks internal reducer stays unchanged. |
| E04, F03-F08, C04, N04/N07 | `hooks/src/engine/output_parser.rs::parse_permission_request`; `hooks/src/events/permission_request.rs::parse_completed`: actual allow/deny grants, reserved fields fail with None then ordinary review, exit 2 needs nonblank stderr to deny. Other handler allow can win over a failed handler. |
| E05, F03-F08, C02/C10, N05 | `hooks/src/events/post_tool_use.rs` and `core/src/tools/registry.rs`: context/rejected-result feedback after effects; MCP output replacement unsupported. Parsed continue:false precedes semantic unsupported flags; wrong types/unknown keys fail earlier. Clooks never emits that mixed invalid tuple. |
| E06/E07, F03-F08, C13, N07 | `hooks/src/events/compact.rs`, `core/src/hook_runtime.rs`: PreCompact false continue stops before compact; PostCompact false continue stops subsequent work, cannot undo compact. Selected Clooks PostCompact failure remains local exit 2, native Failed/no control with stderr error text. No context wrapper. |
| E08, F03-F08, C11-C13, N06 | `hooks/src/events/user_prompt_submit.rs`, `core/src/hook_runtime.rs::inspect_pending_input`, `core/src/session/turn_input.rs::start_or_steer`: prompt context/block, no title, distinct same-ID prompts. Review limitation is accepted and nonblocking under best-effort ordinary prompt history. |
| E09/E10, F03-F08, C11-C13, N06 | `hooks/src/events/stop.rs`, `core/src/session/turn.rs:510-565`: nonblank block reason continues stopping root/child in same turn, no prompt dispatch; continue:false ends path and dominates block. Child/user boundary is not turn_id. |
| F01/F02, C01 | `hooks/src/schema.rs`, generated ten input schemas, `hooks/src/engine/output_parser.rs`: required nullable producer fields, strict unknown control keys, shared enum discriminator without runtime exact-event comparison. Clooks exact schema/absence compatibility is its own validation policy. |
| F01/F03-F09, N07 | `hooks/src/engine/command_runner.rs::run_command`, ten event consumers: successful stderr discarded; empty success accepted; malformed JSON and process failures usually Failed/no control. Timeout begins after stdin writing. Launcher bypass/provider selection are local facts, not proven native enforcement. |
| C02/C03, N03 | `core/src/tools/handlers/unified_exec/exec_command.rs:499-544`, `apply_patch.rs:438-505`, `mcp.rs`, `core/src/tools/registry.rs`: command-only Bash/patch; MCP/default Function full JSON replacement. Added command-only keys ignored natively and must be rejected by Clooks. Handler/server validity, exhaustive interception and completion counts remain unknown. |
| C11-C13, N06 | `core/src/session/session.rs::session_id`, `core/src/hook_runtime.rs:1013-1033`, `core/src/tasks/review.rs`, `core/src/codex_delegate.rs`: shared root session, ThreadSpawn agent scope, synthetic Review without agent_id/origin discriminator. Ordinary root-boundary compatibility is user-accepted with Review documented/nonblocking; no full genuine-user parity or delivery UUID. |
| C10, N05 | `core/src/hook_runtime.rs::record_additional_contexts`, `core/src/context/hook_additional_context.rs`, `hooks/src/output_spill.rs`: developer-role context, root/child continuation text and file writing. Recipient reading remains unknown; inline-first required. |
| C01/C04-C09/C13/C14 | Upstream S supports wire/identity premises only. Current M1 private interfaces, audit/detachment, lifecycle and finalization have the named U/D evidence above. Codex provider storage/runtime requirements belong to later milestones; do not borrow M1 Claude proof for them. |

The normal cases are derived from `src/types/results.ts` and `src/engine/context-methods.ts`, not from upstream fields or the plan wishlist. `injectContext`, `updatedInput`, and other optional fields need both absent and present cases. Every event also gets void/null handler returns, no matching hooks, and an actual skip handler with an execution breadcrumb. These are distinct execution paths even if they emit the same bytes.

U and D are required for every row by the milestone implementing that Codex behavior; this does not require Codex compiled reachability in M1 while its runtime remains disabled. The milestone allocation below distinguishes injected-policy units from reachable compiled paths. S requirements in the last column are covered for the audited native paths by the source ledger below; P/L remain pending; final M1-M4 local U/D is recorded separately; final M4 review and native acceptance remain distinct. Handler/server acceptance, exhaustive interception and recipient file readability stay unresolved. `F` means all applicable failure cases defined in the next section; an observational outcome cannot be described as preventing an already completed action.

| ID / event | Normal public result cases | Unsupported capability / malformed-result cases | Early error | Handoff, turn and provider assertions | Upstream proof required |
| --- | --- | --- | --- | --- | --- |
| E01 SessionStart | `skip`; `skip + injectContext` | Handler `allow`/`block` are invalid arms, not new methods. Separately audit universal `beforeHook.block`. Inject foreign fields by raw return to exercise runtime validation. | F, especially no-hook startup and advisory-only paths | Main-recipient context; eligible context pointer or exact inline fallback. Startup/clear reset; resume/compact/unknown source preserve according to settled Codex boundary evidence. Prune before no-hook/no-match exit. No Claude plugin discovery or settings effects. | S/P context and errors; L activation and main-recipient delivery |
| E02 SubagentStart | `skip`; `skip + injectContext` | Invalid handler arms; universal lifecycle block; unsupported fields | F | Child receives context; parent and sibling do not receive it. Referenced child identity is not assumed to be emitting-session identity. Child can read pointer or receives inline text. No main-turn reset. | S/P audience and identity; L child delivery/access |
| E03 PreToolUse | `allow` without rewrite -> no native decision, optional context and reason as human systemMessage with disclosed original-recipient loss; `block + nonblank reason` with optional context; `skip` with optional context. Public `ask` and `defer` are exercised as unsupported below. `allow + updatedInput` -> native allow plus complete replacement only for approved codecs; optional reason uses the same human systemMessage/recipient-loss policy, never permissionDecisionReason; native policy retained. PermissionRequest plain allow, unlike this no-decision path, is an actual grant. | `ask`, `defer`; rewrite for unapproved tool/shape; malformed rewrite; raw `suppressOutput`, `continue`, `stopReason`; unsupported field on a losing vote. Never warning plus accidental permission allow. | F | Deny vote dominates later allow; no rewrite reaches later hooks before audit. Human denial reason stays inline; eligible model context handoff does not alter denial. Tool identity and opaque input preserved. | S/P exact decision consumer and every enabled codec; L baseline command creates sentinel, denial invokes hook and prevents sentinel, rewrite changes actual command effect |
| E04 PermissionRequest | Plain `allow`; `block + reason`; `skip` | Public allow `updatedInput`/`updatedPermissions`; public block `interrupt`, including present false. Unsupported allow followed by plain allow must not erase refusal. Empty objects/arrays also test presence policy. Native non-null reserved updates/interrupt:true cause Failed/no decision and ordinary review, not fail-closed; Clooks instead emits explicit JSON deny. Native null/false permissiveness does not authorize Clooks fields. | F | Human permission messages remain inline; no handoff files for rejected updates. No rewritten input passed onward; no permission changes. No invented tool-use identity. | S/P permission consumer and failure channels; L actual allow/deny outcome without inventing a new trust model |
| E05 PostToolUse | `skip` or `block + reason`, each with optional context | Public `updatedMCPToolOutput` on both arms, including null and empty values; malformed/foreign fields. `allow` is not a normal public arm. | F; post-action semantics explicit | Preserve opaque response and response nullability. Any feedback/context targets the documented recipient. Existing tool side effect remains; do not claim rollback. Eligible pointer verified independently of file creation. | S/P response consumption and feedback; L result feedback with pre-existing sentinel still present |
| E06 PreCompact | `allow`; `block + reason`; `skip` | Source supports pre-compaction continue:false veto; reject unsupported raw decision/context forms. Raw context/rewrite fields are invalid handler capabilities. Audit lifecycle block separately. | F; cannot infer prevention from exit 2 | Preserve history through compaction. No handoff for current handler fields. Do not equate stopping an agent with successfully vetoing compaction. | S/P exact compaction consumer; L any advertised veto/control behavior |
| E07 PostCompact | `skip` only | Raw `allow`/`block`/context/control fields; universal lifecycle block and generated failure policy. No handler method widening. | F; observation after compaction | Hook breadcrumb proves execution. No control output invented to make an empty-output test pass; no handler payload handoff. Preserve turn history. | S/P observational/error semantics; L event firing if claimed |
| E08 UserPromptSubmit | `allow`; `block + reason`; `skip`, each with optional context | Public `sessionTitle` on every arm, both alone and combined with context; raw foreign fields. Title is unsupported unless a separate scope decision establishes support. Test later-hook overwrite and falsy/invalid title values. | F | Human block reason inline; context handoff only for evidenced recipient. Accepted ordinary root prompt advances all scopes even on same-ID steering; child prompt and Stop continuation preserve. Review ambiguity is documented and accepted as nonblocking; history stays enabled without a workaround; no retry-deduplication claim. | S/P prompt rejection, title absence, identity/boundary rules; L prompt outcome and continuation sequence |
| E09 SubagentStop | `allow`; `block + reason`; `skip` | Raw `continue` result/feedback, context, rewrite, stop controls: not public handler arms | F; continuation is not rejection | Block asks the child to continue. Eligible reason pointer must be readable by that child. Parent/sibling counters remain separate; nullable child transcript handled honestly. | S/P child continuation and audience; L child continues without continuing parent |
| E10 Stop | `allow`; `block + reason`; `skip` | Raw `continue` result/feedback, context, rewrite or stop controls: not public handler arms | F; never equate failure with successful termination | Block continues current work. On the accepted ordinary path, reminder history survives continuation and resets at the next root prompt, including same-ID steering; Review may also reset it under the documented nonblocking limitation. Inline/pointer modes give same raw history. | S/P precedence and continuation identity; L observed prompt/turn-ID sequence and resumed work |

Unsupported public capabilities and invalid raw result shapes are separate tests. A handler cannot gain a new method merely because a native output field exists. Lifecycle gates and engine-generated failures also require policy coverage even on skip-only events. The frozen plan must specify the exact supported refusal for invalid capabilities, including `sessionTitle`, and the explicit diagnostic/failure behavior when no such refusal exists. An invalid capability must not become allow via onError continue/trace, a caught policy exception, failure-threshold degradation or later reduction. Where source establishes no refusal channel, retain the explicit M0 local-failure limitation and make no enforcement claim; this does not reopen the completed M0 entry gate.

### Field-Level Output Contract

For each event/result arm and each relevant tool, the frozen plan must fill an explicit behavior tuple: retained decision, retained/dropped/rejected field, exact output keys, diagnostic recipient/channel, exit code, resulting native action and evidence status. A single event-level supported/unsupported label is insufficient. Native behavior below is source-backed for audited paths. M2 PreToolUse, M3 remaining-event and M4 integration local U/D allocations are complete; upstream executor/live checks stay pending.

| Case | Required assertion |
| --- | --- |
| Plain PreToolUse allow | With no context/reason/diagnostic: empty stdout, empty stderr, exit 0, no decision. With context A: `{"hookSpecificOutput":{"hookEventName":"PreToolUse","additionalContext":"A"}}`. Native policy retained, no grant. |
| Plain allow reason | Reason R without rewrite: `{"systemMessage":"clooks: PreToolUse allow reason (human annotation only; original allow-reason recipient unavailable; native policy retained): R"}`, exit 0. Optional context is a separate supported wrapper. No permissionDecision, permissionDecisionReason or top-level reason; do not convert R into model context, denial or synthetic rewrite. |
| Approved rewrite allow | Command candidate `{"command":"B"}`: `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"allow","updatedInput":{"command":"B"}}}`, exit 0. For reason R, expect `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"allow","updatedInput":{"command":"B"}},"systemMessage":"clooks: PreToolUse allow reason (human annotation only; original allow-reason recipient unavailable; native policy retained): R"}`, exit 0. No permissionDecisionReason or top-level reason; the native parser accepts but discards allow decision reasons. Full replacement and native policy retained. |
| Blank denial/continuation | Empty/whitespace required block reason fails Clooks audit before effects, even on a losing vote. For PreToolUse hook H, the current attributed diagnostic is `clooks: Codex PreToolUse hook "H" capability "reason": clooks: invalid blank block reason; result effects refused. Pending call denial requested.` Emit it as both nested permissionDecisionReason with deny and top-level systemMessage, exit 0. The earlier context-free literal `clooks: invalid blank block reason` is a historical draft superseded by the required hook/event/capability/action attribution. Native blank refusal yielding Failed/no decision is not success. |
| Diagnostic transport | Successful systemMessage is a native human warning, successful stderr is discarded. debugMessage remains debug-enabled local stderr only. SubagentStart failure keeps empty stdout/local stderr/exit 2 with generic native failure and lost detail; PostCompact failure keeps exit 2 with stderr error text, no rollback/control. |
| Supported injectContext | For a single result or context retained by shared reduction, assert exact text or eligible pointer in the documented context channel, the preserved decision and exit, and main versus child recipient. Test context on every public arm that admits it, including skip and block; do not test only allow. A supported field on a losing result is still subject to the unchanged reduction rules specified below. |
| Unsupported injectContext | If the frozen policy intentionally drops unsupported context, assert the valid underlying decision remains unchanged, the field is absent from emitted JSON, no handoff file was created for that text, and the diagnostic identifies the loss in the explicitly chosen human/main/child channel. Pin exact exit and native action, including the no-decision/skip case. This explicit non-control-field treatment is not permission to downgrade invalid safety capabilities via onError or degradation. A malformed field or invalid result arm remains a separate validation failure. |
| Unsupported control/rewrite/title | Assert the declared rejection/refusal and no forbidden keys or effects, including when accompanied by supported context. An emitted warning or removal of a control field followed by allow is not acceptance. Where refusal cannot be established, retain the explicit source-backed local-failure limitation and do not claim enforcement. |
| Handoff readership | Human-facing permission/prompt reasons and diagnostics stay inline. Main-facing payloads require main access; child-facing payloads require child access. Test recipient access separately from local file existence; unproven/inaccessible delivery uses exact inline fallback. |

The accepted ordinary-path acceptance sequence is mandatory: first Stop reminds; same-ID continuation without UserPromptSubmit preserves history; next Stop sees the intervention and skips; next root UserPromptSubmit, including same-ID steering, resets every scope and makes the reminder eligible again. Child prompts preserve history. Synthetic Review without agent_id may also reset history; this documented limitation is accepted and nonblocking, without workaround scope. Repeat with handoff; U/D replay does not establish P/L or full genuine-user parity.

## Failure Matrix Applied Across Events

Every known-event F case is parameterized over E01-E10 at unit level and through compiled Docker engine execution where reproducible. Exact fault injection stays in units when real Docker permissions can prove only an earlier stage. Unknown-event cases form their own rows, with no fabricated event or universal blocking claim.

| ID | Trigger and observable acceptance | Evidence required (S complete; M1-M4 U/D allocation passed; P/L pending) |
| --- | --- | --- |
| F01 | Malformed JSON, null, array, scalar, absent/non-string/unknown event, and out-of-scope event. Assert exact process channel/exit, zero hook-import/handler markers and no result-caused files. Never infer provider/event from another provider's payload or stderr. | U+D; S/P unknown-event/error consumer behavior |
| F02 | Recognizable event with invalid/missing required envelope/tool fields. Reject before user modules load, lifecycle starts, state boundaries change or handoff runs. Inspect import marker, handler marker, state bytes and directory listing. Required nullable producer fields follow the broader explicit absent/null Clooks compatibility policy, with separate fixtures and no fabricated paths. | U+D; S field contracts |
| F03 | Config parse/schema/read failure with identifiable event versus unreadable/unknown event. Check initial failures, threshold transition and recovery separately; state counters and actual hook execution accompany channel assertions. No synthetic `__parse__` event is emitted upstream. | U+D; S/P per-event failure policy |
| F04 | Missing ordinary hook, syntax/import failure, dangling plugin source, invalid order and disabled/no-match hooks. Distinguish intentional skip from import failure; assert import/handler counts, persisted failure counters and permitted next-group execution. | U+D; S/P emitted failure/diagnostic behavior |
| F05 | Handler throw/reject/timeout and each lifecycle phase throw/reject/timeout, in sequential and parallel groups. Test effective `onError` block/continue/trace, threshold-minus-one/threshold/already-degraded, `maxFailures: 0`, recovery and load-error recovery. An executed degraded hook still leaves a breadcrumb and raw error history. | U+D; S/P output policy on every event |
| F06 | Policy validation/codec/translation throws or rejects; invalid tag, number/string/array result, wrong field types, mixed valid+invalid fields, and blank/whitespace denial or continuation reasons. Reject blank reasons before effects and emit a generated nonblank policy-failure reason; native rejection without a veto is not acceptable refusal. Repeat under onError block/continue/trace and below/at/above failure thresholds: invalid capabilities never become degraded allow or continuation-by-configuration. No result-derived mutation, handoff, or accidental allow; preserve raw-history semantics without counting policy refusal as a delivered user intervention. | U+D; S/P selected failure channels; completed M0 refusal/limitation disposition |
| F07 | No config, no hooks, no matches, all skip, dangling-only, advisory-only and result-undefined paths. Pin intended no-config compatibility separately. For every translation path, assert adapter-selected exit and output survive to process exit. Boundary maintenance where configured must still occur without matching handlers. | U+D; S/P no-op and diagnostic channels |
| F08 | Engine-mode SIGINT/SIGTERM, uncaught exception/unhandled rejection, child unexpected nonzero and killed binary. Assert bounded completion, exact stdout/stderr/exit and no duplicate/partial decision JSON. Compare direct binary with generated launcher; test recoverable known-event handling separately from failures too early to know an event. | U+D; S/P event-specific upstream treatment; L before advertising enforcement |
| F09 | Generated launcher missing binary and `SKIP_CLOOKS=true`. Preserve documented bootstrap/bypass exit 0; establish positive normal invocation first, then prove no binary/hook marker increments. These are deliberate exceptions, not fail-closed proof. | D; S/P activation context if used in native claims |

Validation before effects refers to effects the runtime can control: loading user modules on invalid input, consuming a returned patch, generating a handoff file, selecting later groups and emitting output. Already executing trusted hook code can perform arbitrary side effects; a post-return policy cannot undo those. A parallel abort/timeout likewise cannot guarantee cancellation of uncooperative hook code. Tests must not imply either guarantee.

## Cross-Cutting Acceptance Cases

| ID | Explicit cases and required assertions | Evidence required (M1-M4 U/D allocation passed; M4 complete) |
| --- | --- | --- |
| C01 Private invocation | Interleave two invocations through the same adapter with different provider/session/turn/tool identities. Enumerate public handler and before/after input keys; private raw payload, codec, provider policy and native identity envelope do not leak. Attempts to mutate public nested input cannot mutate the retained original wire payload or another invocation. No singleton invocation cache. | U+D; S native identity definitions |
| C02 Opaque codecs | Bash/exec_command and `apply_patch` command-only cases (reject any patch adding cwd, timeout or other ignored keys); MCP and unknown tool cases; string/object/null/array response variations according to each contract. Preserve `snake_key` and `camelKey`, nested arrays/objects, existing nulls, false/zero/empty string, Unicode and multiline patch text. Do not rename patch text to Claude Edit/Write fields. A native command-only replacement preserves hidden original exec options but cannot mutate them; patch-string validity still needs downstream validation. PermissionRequest approval input is a distinct codec with nested metadata. Unknown public toolInput is Record-shaped: native scalar/array input without an established honest codec must be refused as unsupported shape, not wrapped in a fabricated object. Audit `Unknown*Context` escape hatches; type fixtures must not pretend unknown tools are built-ins. Assert structural key equality and exact opaque strings, not JSON property-order equivalence. | U+D per enabled codec; S/P each upstream decoder; L each advertised rewrite class |
| C03 Patch materialization | No patch, empty patch, one-field patch retaining untouched fields, two sequential disjoint patches, conflicting patches, explicit null removal, undefined-valued patch entry, restoration after removal, nested object replacement, invalid required-field removal and malformed full replacement. An original opaque null survives an unrelated patch; explicit null removes that key; undefined ignores that patch entry and preserves the prior value. Validate the full candidate before committing pipeline input or exposing it to the next hook. Invalid candidates leave original/current/private inputs unchanged. Verify next hook input, original input, private wire original and full final encoded input independently. Preserve current Claude semantics in separate regression cases rather than silently changing the shared merge. | U+D; S/P full-replacement shape and tool validation |
| C04 Audit before effects | Unsupported PermissionRequest update then plain allow; unsupported PreToolUse vote losing to another vote; unsupported `sessionTitle` then another prompt result; unsupported MCP output with skip. Add long eligible text and handoff enabled. Assert each result audited before handoff/input merge/reduction, zero rejected-payload files and no unsupported value observed downstream. Presence checks include empty object/array, false and null rather than truthiness alone. | U+D; S/P capability/refusal mapping |
| C05 Lifecycle | Universal before block/skip prevents handler and after invocation; before passthrough continues. After is observer-only: returned attempted override remains ineffective; thrown error uses failure policy. Audit the completed lifecycle result after the observer: mutate the handlerResult reference in afterHook, return an ignored value, and assert any resulting invalid capability is caught before effects. Preserve existing observer API; no after override methods. If policy treats beforeHook skip differently from handler skip, preserve explicit private result-origin metadata and test both identical-tag paths; do not guess origin from the tag. Preserve raw per-hook history and define invalid-return handling explicitly. | U+D; local public contract plus S/P event-level delivery |
| C06 Reduction/groups | Sequential, parallel and mixed groups; supported deny followed by allow; equal-rank ties; losing contexts; all-skip context; supported rewrite followed by deny; parallel rewrite including `{}` and invalid tagged skip+rewrite. Exercise both completion orders with controlled barriers, not sleeps. Preserve shared reduction, including losing-context exclusion and last-all-skip selection; exact skip-context cases appear below. Assert configured-order reduction for completed accepted votes, expected next-group markers, abort signals and no late result commit/handoff after short circuit. Failure-message precedence applies only to pre-abort audited failures, not across schedules. Policy must run before the raw-result shouldShortCircuit decision near execute.ts:727, not only before handoff near execute.ts:879. Use a policy-rejected result that would otherwise trigger a different abort/group outcome and assert the selected policy controls it. Preserve Claude deny > defer > ask > allow and existing crash-versus-vote distinction. | U+D; S/P emitted winner semantics |
| C07 Failure state | Repeat F03/F05 with matching hook/event names and identical project/home roots across both providers. Codex execution, load and synthetic config-error failures must not advance or clear Claude counters or degrade Claude enforcement; test the reverse direction too. Seed existing Claude history, drive Codex over each threshold, then verify Claude's first failure and stored counters. Isolate failure-state identity as well as turn identity, retaining Claude compatibility. Test lock, prune, recovery and cleanup ownership for each applicable store so one provider cannot remove or lock the other's state. Do not conflate `.failures` with `ctx.turn`. | U+D; explicit paths/ownership policy required |
| C08 Real config merge | Use generated Codex command with real compiled Clooks and real home/project/local YAML/hooks. Verify home-only execution, project atomic shadow replacement, hook origin paths, order concatenation, local replacement and effective config/error/handoff overrides using ordered hook markers and returned values. Compare explicit Claude and default Claude. Global invocation must still execute trusted repository hooks. | U+D; P/L native global/project coexistence separately |
| C09 Root/provider isolation | Global Codex with inherited `CLAUDE_PROJECT_DIR` pointing to decoy project, changed cwd and explicit `CLOOKS_PROJECT_ROOT` variants. Pass discovery a filtered copy through its existing env option, ignoring the Claude anchor for Codex while preserving explicit Clooks root. Assert source environment unchanged, chosen project by positive hook markers and no decoy invocation; retain Claude anchor behavior. Prove Codex success path does not read/vend Claude plugin settings with read spies and filesystem sentinels, not merely missing warning text. Alternate provider invocations must not mutate process-global selection state. | U+D; S/P actual native cwd/environment; L registration workflow |
| C10 Handoff delivery | For every eligible event/field test false/true/N with exactly N and N+1 UTF-16 units, empty text, mixed eligible/ineligible fields, global/hook/event override, repeated content, write failure and recipient-inaccessible destination. Verify pointer, exact file bytes, modes, reuse, inline fallback and unchanged decision. Human messages and engine diagnostics stay inline. SessionStart no-hook pruning remains bounded; preserve existing file-protocol hardening tests. File existence is not proof the intended recipient can read it. | U+D; S/P audience; L recipient reads or explicit inline-only limitation |
| C11 Turn identity | Equal session strings across providers, distinct sessions, emitting parent versus referenced child, two siblings, and missing identity. Assert each hook sees only its own ordered history; current-event counters differ from cross-event prior list. Preserve existing Claude storage path/history. No fabricated common session or transcript-derived identity; missing identity yields documented empty history without store writes. | U+D; S/P native identity origin |
| C12 Duplicate continuation | Establish Stop reminder breadcrumb 0 and block; replay source-defined same-ID continuation with no UserPromptSubmit; next Stop sees prior intervention and skips; next ordinary root prompt, including same-ID steering, resets all scopes. Child prompts, even with new child turn IDs, preserve all session history. Include the synthetic Review/no-agent_id counterexample as an accepted documented limitation: it may reset root-scoped history; do not require a workaround. Repeat with handoff and child scope. Assert generation/history and positive invocation count at every step. Distinguish repeated delivery from separate genuine prompts with identical text. Native input cannot fully distinguish synthetic Review from genuine root work; the user accepted this limitation for best-effort ordinary prompt history, not disabled history or full genuine-user parity. | U+D replay; S/P boundary evidence; L actual sequence, not synthesized assumptions |
| C13 Boundary/storage failures | No-hook/no-match boundaries; startup/clear versus resume/compact/unknown; concurrent arrivals; stale pre-boundary commit; prune/recreate epoch; malformed/unwritable store; snapshot and boundary failure separately. Assert monotone generation, stale-write rejection, raw tags including degraded errors, excluded never-started hooks and preserved prior intervention under caps. Retain documented cold-start loss behavior; best-effort state is not a safety gate. | U+D; S/P event sequence mapping |
| C14 Diagnostics and regression | Debug on/off, traces, degradation, dangling and shadow advisories cannot add unsupported context/control keys to an otherwise valid output. Audit final wire output separately from strict author-result validation: the engine currently synthesizes internal allow+context for diagnostics even on author skip-only events. Test those generated results on SessionStart, SubagentStart and PostCompact without widening author methods or falsely rejecting all legitimate engine diagnostics. Preserve any prior policy refusal through augmentation and translation. Pin Claude wire outputs, method availability, plugin behavior, handoff and turn history against existing tests. Unknown provider fails before runtime effects. Keep exactly the ten registered Codex events. | U+D; S/P diagnostics consumers |

The no-effects tests need two probes: an import-time marker to catch loading before valid invocation parsing, and a handler/downstream marker to prove which execution phase actually ran. Handoff tests inspect directory contents and exact bytes before/after. Turn tests inspect both hook-visible counts and persisted state. A warning, empty stdout, missing decision key, successful registration or unchanged sentinel by itself does not establish acceptance.

For E03/C06, retain the existing shared reducer without new context aggregation. Require these exact U+D cases in both sequential and parallel execution, reversing parallel completion with barriers:

| Case | Expected Codex reduction and wire result |
| --- | --- |
| Single raw skip with context A | Context A, no permissionDecision, exit 0; ctx.skip method types remain unchanged |
| Raw skip with context A plus plain allow | Allow wins, context A is absent, empty stdout with no native permission decision, exit 0; native policy retained |
| Raw skip with context A plus deny with reason R | Deny wins with reason R, context A is absent, exit 0 |
| Raw skip with context A followed in configured order by empty skip | Last skip wins; no context or permissionDecision, empty stdout and exit 0 with diagnostics/debug disabled |
| Raw skips with contexts A and B in configured order | Last skip wins; context B only, no permissionDecision, exit 0 |

Each case must prove all expected hook invocations with markers. These are reduction outcomes, not unsupported-native-context diagnoses; no new communication-loss warning or aggregation is required. A handoff file from an accepted losing result is not proof that its pointer was delivered.

For rejection cases in C06, the first audited rejection aborts the group. Select the primary message in configured order only among failures audited before that abort; do not guarantee the same primary across schedules. Use barriers to let B reject while earlier-configured A is pending, then reverse the schedule. Both runs must retain refusal; each primary must come from its own pre-abort audited set. Deliberately release the pending promise after abort: its late fulfilled or rejected settlement must not be audited or change votes, final output, current input, handoff files or committed history. Assert the terminal snapshot before and after release. A policy refusal is sticky through both ordinary onError handling and degraded execution; later allow results or generated diagnostics cannot erase it. These assertions concern runtime consumption of late results, not cancellation of arbitrary hook-authored side effects.

## Upstream Proof Scenarios

| ID | Required experiment and success oracle | Status |
| --- | --- | --- |
| N01 Native activation | Disposable Docker home/config/project, explicitly resolved selected Codex binary, generated registration. Capture command argv/environment/cwd and exact event payload; show the matching Clooks hook ran. Check fixed ten-event discovery without expanding registration. | S: audited path in ledger; P/L pending |
| N02 Denial | Same harmless sentinel-producing command succeeds with control/no denial, then matching hook invocation is captured and sentinel remains absent under denial. Retain upstream decision outcome as well as filesystem result. No arbitrary later test command may substitute for the native tool call. | S: audited path in ledger; P/L pending |
| N03 Rewrite | Original command would create A; accepted supported rewrite creates B and not A. Capture original input, Clooks merged output and native executed arguments/effect. Repeat per enabled codec with a domain-appropriate effect oracle. | S: audited path in ledger; P/L pending |
| N04 Permissions | Trigger actual PermissionRequest; distinguish allowed operation from denied operation and waiting for user input. Unsupported permission updates/interrupt are refused by declared policy without effect. | S: audited path in ledger; P/L pending |
| N05 Delivery | Unique context/feedback tokens identify main versus child audience; child can read the exact handoff content. PostToolUse feedback leaves completed side effects intact. Pure observation rows prove event firing and hook execution. | S: audited path in ledger; P/L pending |
| N06 Continuation | Capture Stop/SubagentStop block, resulting resumed work, native prompt events/turn IDs, duplicate delivery and next real prompt. Run the once-per-turn reminder oracle C12; report ambiguity instead of inventing identity rules. | S: audited path in ledger; P/L pending |
| N07 Failures/coexistence | Event-specific exits, malformed output and native timeout behavior; global/project enabled coexistence and actual counts. Activation controls may explain missing hooks, but neither persisted receipt freshness nor registration counts establish execution. This checks the existing native model, not a new authorization layer. | S: audited path in ledger; P/L pending |

All future experiments use disposable Docker-only HOME, CODEX_HOME, CLOOKS_HOME_ROOT, project, logs and state. Do not copy real auth/trust state or replace the host/global binary. Pinned executor proof must identify the actual upstream consumer it reaches; a locally reimplemented parser is U evidence only. Authentication failure is recorded once as blocked live evidence; it does not turn a source/harness result into live proof.

## M4 Compiled Evidence

M4 full local gates passed U 2,192/0, D 787/0, static all exit 0 on manifest fingerprint 11f1bc064814471e5c2edc41996eeae742d430ca06472c22717ae222f87e47eb. M4 is test-only, with no production changes. Final code/execution QA GO and runner audit are confirmed; M4 is complete with documentation closure authorized. Historical focused Docker D passed 213/0 across seven files (4,134 assertions, 39.17 seconds, exit 0); earlier focused U passed 925/0. `codex-state-delivery.e2e.test.ts` now supplies focused C07/C11-C14 state proof, including two sessions/root/two children/provider isolation, unmatched boundaries, malformed-identity byte preservation, held generation-crossing commits with no-reset control, and three genuinely overlapping processes with distinct readiness/PID markers and exact acknowledged per-scope writes. C04/C10/C12 delivery cases retain long root/child reminders inline with warnings/history and refuse unsupported control before delivery. F07-F09/C08/C14 cases execute actual generated project/global commands and assert literal denial/local-failure/advisory channels; real-binary signals/exception/rejection assert raw exits and completion well before timeout. The earlier 211/1 run's missing global fixture observation directory is fixed, not a production defect. Exact cases, commands, image/manifests and all prior attempts are in [M4 evidence](codex-runtime-m4-evidence.md). Code/QA conditional GO is recorded; final formatted full U/D/static gates passed. The earlier full-unit held-lock timing failure (38.538ms against >50ms) remains unexplained after one unchanged-source passing confirmation; no assertion change, production fix or flake claim. Final code/execution QA GO is confirmed; M4 is complete with documentation closure authorized. E2E stays frozen. No native P/L claims.

## M3 Compiled Evidence

Final `m3-final-5-testtrap` validation passed U 2,168/0 (69 files, 8,035 assertions), D 774/0 (46 files, 8,781 assertions, 98.17 seconds), and typecheck/lint/format:check each exit 0 (112 lint warnings, 0 errors). Six source manifests match `8224ddbd16af5dc77eb6e0cc29db315026378c2d0327c1166fbf5093c88e70ad`; source/wrappers were unchanged. Exact commands, distinct image IDs and resolved historical failure dispositions are in [M3 evidence](codex-runtime-m3-evidence.md). The private `deferRuntimeErrorAudit` refinement preserves ordinary failure accounting before selected-error translation; author/parallel-contract audits remain immediate, with no new public API. M3 is complete with final code-review milestone GO; final QA GO is confirmed and M4 is complete with final U/D/static, code and QA GO; documentation closure is authorized.

Historical preliminary `m3-final-4-formatted`: D passed 774/0 (8,784 assertions, 98.45 seconds), but U had 2,167 pass/1 test-trap failure. The test-only correction and final full gates above resolve that failed snapshot without deleting its record. QA migration GO concerns measured plugin discovery/vendoring, not a direct Claude settings-read spy. No native P/L evidence is added.

Historical second-snapshot focused Docker E2E passed 178/0 across `codex-events.e2e.test.ts`, `codex-runtime.e2e.test.ts` and `agent-adapter.e2e.test.ts` (3,503 assertions, exit 0). Its U run had 898 pass/3 stale pre-M3 expectation failures; an earlier readonly unit-table typecheck failure ran zero tests. These failures were corrected and final units passed. Their separate manifest fingerprint and artifacts remain in [M3 evidence](codex-runtime-m3-evidence.md).

The first snapshot covers E01/E02/E04-E10 no-op handlers with fresh module/handler markers; supported context and event-specific decisions; beforeHook skip and post-observer mutation rejection; missing model and event-specific required fields before imports; designated nullable compatibility; SessionStart's public model; nested PermissionRequest Bash description without a fabricated tool-use ID; arbitrary PostToolUse JSON response and non-record-input rejection; and falsy/null unsupported fields, including permission updates/interrupt and title on all prompt arms. Exact source-shaped envelopes omit PermissionRequest tool_use_id and compact permission_mode. Every replay has an isolated CODEX_HOME and 10-second subprocess limit.

The passing focused snapshot also includes actual config/import/crash/continue/trace/degraded/recovery journeys and lifecycle block refusal. Every event's selected crash requests its appropriate failure outcome, including termination rather than Stop/SubagentStop continuation; successful threshold degradation keeps accounting. Literal context trace, non-context fallback and stderr-only channels are asserted. Sequential downstream markers and bounded parallel variants cover pending stronger blocks and an already-settled PermissionRequest allow followed by unsupported skip. The persisted ordinary reminder sequence covers root UPS with the same native ID, child preservation, repeated stop_hook_active:true, startup/clear versus resume/compact, and interleaved Claude state. These supply focused D for E01/E02/E04-E10, applicable F01-F07, and C01/C02/C04-C07/C10-C14; M4's broader integration allocation remains separate.

Only M2's nine obsolete unsupported-runtime tests were migrated; all PreToolUse/storage/provider tests and Claude preservation cases remain. Failure literals retain the M2 hook/event/capability/disposition contract. No native execution or P/L evidence is implied. Full M3 U/D/static validation and final code-review milestone GO are complete; final QA GO is confirmed. E2E files stay frozen until the next authorized edit.

## M2 Compiled Acceptance (Frozen Historical Snapshot)

`test/e2e/codex-runtime.e2e.test.ts` contains source-shaped compiled-binary replay assertions, not native Codex probes. Its cases passed in the final 674/0 full Docker E2E run. Two earlier full runs failed one existing test each; focused retries and 60 bounded scratch reproduction calls passed. The earlier circuit-breaker failure's original child stderr is missing and its root cause remains unknown, not an established flake. Local validation gates are complete; final documentation review remains pending. This writer inspected artifacts without executing tests. Every new run has a 10-second subprocess bound and disposable CODEX_HOME; invocation markers/observations are cleared before reuse. No production translator imports or test-only provider selectors supply the expected output. Failure literals pin hook/event/capability/requested-action attribution, including runtime attribution before hook loading.

Full units passed 2,047/0; final full E2E passed 674/0; Docker static typecheck/lint/format:check each exited 0 (112 lint warnings, 0 errors). Final unit/E2E/static before/after manifests hash to `e8a92eeffb2ce09ec459e781e743da17306c7b8d7209beb3856825ee9fcb2586` (manifest-file fingerprint, not Git commit). [M2 evidence](codex-runtime-m2-evidence.md) retains exact commands, image distinctions, earlier failed attempts and the final full-suite pass.

| IDs | Named tests / parameter families | Designed oracle |
| --- | --- | --- |
| E03, C01/C02/C03 | `plain allow retains native policy without emitting a permission decision`; `allow reason is a human annotation only, rewrite=...`; `record codec preserves opaque input and canonical identity: ...`; `opaque sequential patches produce a full replacement and preserve untouched nulls`; `MCP own __proto__ and constructor keys survive an unrelated patch without changing original input`; `command-only ... rewrite reaches the next hook as a full record` | Literal empty/JSON output, native policy retained, command-only versus opaque data, full replacement, unchanged original and positive downstream observations; no MCP server acceptance claim |
| C04/C05/C06, F03/F06 | `blank block reason becomes a nonblank policy denial before a later hook`; unsupported arm/field cases after a stronger deny; command-only codec violation parameters; `parallel empty rewrite aborts the pending sibling before later groups` | Sticky attributed refusal despite continue/maxFailures, no later input observation or rejected handoff, controlled sibling start/abort |
| C06 | `sequential`, `parallel-forward`, `parallel-reverse` parameters for all five skip-context tuples | Promise-controlled completion, configured-order literal winner/context, fresh positive markers and no handoff |
| C08/C09, F01/F02 | `home/project shadowing is atomic and local order overrides project order`; `Codex ignores inherited Claude root but honors the explicit Clooks root`; paired identity, required-field, nullable compatibility, non-object envelope and configured non-record-input cases; `known-event config failure refuses without inventing a parse event or importing hooks` | Real layered configs, exact wire event, no import on invalid envelope after a positive control; no-config bypass retained |
| C07/C11, F02/F05 | `home and project failure counts are isolated from Claude and each other`; `home/project config failure and repair touch only Codex counter state, preserving Claude bytes`; `tool history isolates providers and child scopes without resetting on native turn ID changes` | Separate storage and history; malformed/invalid envelopes cannot write or clear existing state under good/bad config; valid recovery clears only Codex |
| C07/C11, F02/F05 | `home config accounting refuses a linked ... without touching foreign counters`; `ordinary failure refuses a ... linked by a reached hook before counter commit`; `turn history ignores a linked ... snapshot and never commits into foreign storage` (directory/file parameters) | Six runtime-linked storage cases, positive good-path controls, actual hook reachability or pre-import denial, empty history on linked snapshots and unchanged foreign bytes |
| C09/C14 | `eligible context stays inline with handoff requested=...` | Exact context and one explicit requested-handoff diagnostic, no pointer/file or readability claim |
| E01/E02/E04-E10, F03 | Nine `... emits an explicit event-aware unsupported failure, not success` parameters | Valid source-shaped event envelopes receive their failure tuples until M3; not nine working event handlers |

The M2 change to `test/e2e/agent-adapter.e2e.test.ts` replaces only its disabled-Codex control with `CLOOKS_AGENT=codex enables valid PreToolUse and rejects configured invalid input before import`. Existing default/explicit Claude and compiled preservation assertions remain and passed in the first final-formatted full E2E run. The historical early 716/0 focused run predates the enabled runtime and does not establish these D cases.

## Local Evidence and Review Findings

Resolved M1 full-unit preservation regression (C06/C07/F05): the final 2,000/0 M1 full-unit run includes the unchanged src/engine.test.ts "circuit breaker updated after all settle" and restores its expected counter behavior. The earlier 1,997/1 run remains historical failed evidence; final M1 E2E/static passes are recorded above.
The following implementation observations describe the reviewed M1 snapshot, not the M2 tree. M1 U/D/static and final review gates passed; Codex was runtime-disabled in that tested snapshot. M2's separate U/D/static proof is recorded above.

Resolved M1 code-review P1 (C06/F06): the throwing raw-getter path resolves as a policy failure without orphaning the parallel batch; its named regression passes in final M1 U. F07 finalization, structured pure diagnostics and engine-owned failure selection are resolved, with final M1 U/D/static and review gates passed.

- `src/types/results.ts` and `src/engine/context-methods.ts` define the exact arms used above. SessionStart/SubagentStart expose skip with optional context; PostCompact exposes skip only; UserPromptSubmit includes sessionTitle; PostToolUse exposes skip/block with optional MCP output update. `src/types/contexts.ts` retains a string transcriptPath and explicit Unknown tool context escape hatches.
- The M1 `src/agents/codex/adapter.ts` set supportsRuntime false. `src/agents/types.ts` defines NormalizedInvocation with private metadata, createResultPolicy(invocation), and structured pure diagnostic composition. The earlier lack of private interfaces is a pre-M1 snapshot, now superseded. `src/normalize.ts` retains Claude recursive normalization; the M2 tree is adding separate opaque Codex codecs and runtime handling without extending M1's proof.
- Current `src/engine/run.ts` finalizes configured no-hooks/no-matches/no-result paths through the adapter even without messages and retains its exit; earlier hardcoded-success claims are superseded. The engine selects translateFailure for latched policyFailure and owns output from structured composition. Claude's existing loading/normalization order and no-config bypass remain; Codex's F02 pre-import normalization belongs to its later runtime implementation.
- Current `src/engine/execute.ts` audits completed lifecycle results through shared deep detachment before accepted handoff/merge/reduction. The raw-getter parallel-classification P1 is fixed with passing final U regression. Claude's legacy shallow/null merge stays unchanged; Codex's candidate policy must preserve untouched nulls in M2. Passing M1 U/D covers the allocated boundary cases; future Codex C03-C06 acceptance remains distinct.
- The current shallow spread also permits an undefined patch value to overwrite an existing value. Codex candidate construction must ignore undefined entries and preserve untouched nulls before tool-specific validation, while keeping Claude behavior independently covered. Unsupported-capability refusal must remain outside configurable ordinary crash degradation so repeated invalid results cannot eventually become allow.
- `src/lifecycle.ts` exposes afterHook as an observer, passes handlerResult by reference, and races a timeout against lifecycle execution. Requirements must preserve the observer API and audit mutation/late completion without promising cancellation of arbitrary hook side effects.
- `src/config/merge.ts` and `src/config/discovery.ts` define the actual merge/root behavior. `test/e2e/entrypoint.e2e.test.ts`, test "compiled Claude global launcher preserves merged ordering, shadowing and local overrides", already uses real hooks and ordered markers; use that strength for Codex runtime acceptance, without borrowing its result.
- `src/codex-fixtures.test.ts` checks ten filenames, event names and basic docs-shaped fields. The M1 `test/e2e/agent-adapter.e2e.test.ts` includes the named Claude envelope/lifecycle/detachment cases and strengthened disabled-Codex control above. Those compiled Claude cases pass in final M1 D; the M2 draft replaces the disabled control with valid PreToolUse and configured invalid-input checks. Neither M1's result nor historical fixtures proves native interception, Codex codecs or recipient delivery.
- `src/engine/execute.updated-input-patch.test.ts` supplies existing patch/null/parallel cases; `src/engine/execute.turn.test.ts`, `src/engine/run.turn-boundary.test.ts`, and `test/e2e/turn-state.e2e.test.ts` supply raw-history/boundary cases and positive reminder breadcrumbs. They do not establish native Codex continuation identity.
- `src/engine/handoff-integration.test.ts` and `test/e2e/handoff.e2e.test.ts` inspect pointer/file behavior and inline fallback. Their filesystem assertions do not establish native child access.

Domain review included `docs/domain/bash-entrypoint.md`, `docs/domain/cli-architecture.md`, `docs/domain/global-hooks.md`, `docs/domain/testing.md`, `docs/domain/cross-agent-hooks.md`, `docs/domain/config/discovery.md`, `docs/domain/config/execution.md`, `docs/domain/config/handoff.md`, and `docs/domain/turn-state.md`. Runtime changes must update affected domain descriptions after implementation. Registration baseline descriptions stay historical; current domain prose is not version-pinned upstream evidence.

## M0 Completion and M1 Entry

Code structural GO, final QA GO and final Docs GO are confirmed with no findings; source audit, bounded probe dispositions, reconciliation and fresh no-lookup summary are complete. The user accepted best-effort ordinary prompt history with Review documented/nonblocking, completing source-backed M0. M1 implementation, validation and final review are complete; M2 is in progress. Historical parent preservation checks remain: unchanged baseline diff fingerprint prefix bb767e..., unchanged untracked registration-state files and clean direct whitespace scans. New ignored documentation artifacts require explicit force-add only when staging is authorized; tmp stays ignored. This writer stages nothing.

Map E/F/C/N IDs into each production milestone before the draft is frozen. Every production milestone includes its own U+D acceptance; identity/failure/handoff prerequisites accompany the first path that needs them. Plan E adds upstream workflow confidence and cannot absorb ordinary Plan D Docker coverage.

M1 supplies injected-policy unit proof for the new boundary and compiled Docker regression proof through reachable Claude paths while Codex remains runtime-disabled. Its U and D evidence must name the actual cases each reaches; an injected policy is not compiled-CLI evidence. Codex-specific compiled rejection and translation cases belong to M2 for PreToolUse and M3 for the other events, with their corresponding units. The U+D labels above describe acceptance for implemented behavior, not an unconditional requirement to reach every Codex case through the M1 binary. Full Docker regressions remain required for M1; this allocation does not defer ordinary implementation coverage to Plan E.

M0 production entry is complete on source evidence and accepted history. M1/M2 have milestone GO. M3 is complete with implementation/U/D/static and final code-review milestone GO; final QA GO is confirmed. M4 is complete with final U/D/static, code and QA GO; documentation closure is authorized. No P/L evidence, additional upstream retry or lock modification. The Review limitation remains accepted/nonblocking; native recipient readability remains unverified.
